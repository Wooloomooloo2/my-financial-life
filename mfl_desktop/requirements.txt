# mfl_desktop dependencies.
#
# NOT the source of truth. The canonical, fully-pinned desktop runtime set lives
# at the repo root in `requirements-desktop.txt` (ADR-099). This file re-exports
# it so `pip install -r mfl_desktop/requirements.txt` keeps working and can never
# drift out of sync — there is exactly one list to maintain.
#
# Prefer installing the canonical file directly:
#     pip install -r requirements-desktop.txt

-r ../requirements-desktop.txt
