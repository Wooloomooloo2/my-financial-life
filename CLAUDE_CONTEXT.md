# CLAUDE_CONTEXT.md
# My Financial Life — Developer Context for AI Assistance

This file gives a new Claude session full context to continue development
without needing the original conversation transcript.

**Last updated:** 2026-07-05 (budget funding model + no credit-limit-as-funds [ADR-138]; note: ADR-132–137 landed via a collaborator pull — see docs/adr/README.md + git; sidebar Today/Projected balance toggle; transaction status lifecycle ADR; net-expense reimbursements in reports; consistent rounded bar corners; lockable API-key field; frozen-app TLS CA-bundle fix; macOS → Mac App Store decision; Budget matrix rollover-display fix; Distribution → MS Store-only) — **(ADR-138) Budget funding model (balances vs income) + drop credit-limit-as-funds.** Owner feedback on budget setup. (1) New per-budget `budget.funding_mode` — **`balances`** (default: pool = perimeter accounts' balances) or **`income`** (pool = only `kind='income'` transactions into the non-excluded perimeter accounts over the whole budget period, incl. future-dated income; ignores starting balances). Chosen via a radio pair ("Fund this budget from: Account balances to start / Income only") on the setup dialog's Accounts tab; persisted via `set_budget_funding_mode`. (2) Dropped the `budget_account.contribution = 'available_credit'` mode (= `credit_limit + balance`, i.e. a card's headroom — it counted the **credit limit as funds**, which the owner flagged as not good practice): a credit card now contributes its **signed balance**, so its debt *reduces* the pool (owner chose this over excluding the card entirely). Migration **0035** adds `funding_mode` (CHECK 'balances'/'income') + rebuilds `budget_account` to migrate `available_credit`→`balance` and tighten the CHECK to `('balance','excluded')` (ADR-032 recipe + legacy_alter_table). `compute_perimeter_pool` branches on the mode (income mode = income into perimeter over start_month→last-month, FX-converted); its signature is unchanged so callers (budget window, home card ADR-136) are unaffected. Setup dialog: funding radios + the account combo drops "Available credit" (only Balance/Excluded); account_dialog credit-limit help updated. Excess-balance sweep (to savings / a budget category) explicitly deferred to a later ADR. `tests/test_budget_funding_mode.py` 6/6 (default/persist/create; available_credit rejected; balances-mode £3,500 cash − £318 card debt = £3,182 not limit-based; excluded card → cash only; income mode counts in-period income only, ignores balances + out-of-period) + suite 28/28; setup dialog screenshotted. **NOTE:** ADR-132–137 (splash-escaping dialogs; retire Payee report; Category&Payee rollup; Net-Worth-over-time bars; home budget-card monthly-plan fix; Compact-file VACUUM) landed via a collaborator pull between ADR-131 and this — full detail in docs/adr/README.md + git log; not re-summarised here. — **(ADR-131) Sidebar balance: Today vs Projected toggle.** Owner-reported: the left-panel account balances sum the whole ledger, so future-dated ("forwarded") transactions inflate the figure — they wanted today's actual balance vs projected. A segmented **Today | Projected** pill on the ACCOUNTS header row switches every balance at once (owner's pick over show-both); **default Today** (`posted_date <= today`, excludes future-dated rows), remembered app-level in QSettings (`sidebar/balance_mode`). `compute_account_balances`/`compute_account_values` gained `as_of_date` (cash sum + investment holdings both filtered to that date; prices always latest). Sidebar owns the mode (with a `saved_balance_mode()` static so the register computes the initial balances before the widget exists) + emits `balance_mode_changed`; register recomputes + reloads via a `_sidebar_balances()` helper. A `minimumSectionSize` bump keeps the pill un-clipped even with tiny balances. No schema/migration — a date filter + a toggle. Net Worth + the register running-balance are intentionally unchanged. `tests/test_sidebar_balance_mode.py` 3/3 (QSettings-hermetic) + suite 27/27; screenshotted light/dark. — **(ADR-130) Transaction status lifecycle (confidence ladder) + status-driven reconciliation + precise OFX matching [Phases 1–3 core shipped].** Prompted by the June HSBC reconcile audit (a £16.17→£13.39 variance that was entirely status/selection, not arithmetic: `pending` 30-June purchases + duplicate entries were reconcilable and got ticked). Replaces `Pending/Uncleared/Cleared/Reconciled` (copy-pasted in ~8 UI files + a 0001 `CHECK`) with a lowercase confidence ladder — **pending** (spent) → **cleared** (saw it post) → **matched** (OFX download matched/added it) → **reconciled** (statement-locked); mostly a rename (`Uncleared→cleared`, `Cleared→matched`). Reconcile candidate = **matched**, with an **optional include-cleared** toggle (owner's call — some banks offer no downloads, ADR-050); `cleared`-in-range flagged; `pending` never eligible. Adds `bank_posted_date` (keeps the spend date) so reconcile aligns to the bank and the 22/23-June date-scramble stops. Import becomes a reviewable precise matcher (amount+date-window+canonical-payee → auto / needs-review [amount-differs → adopt-bank-amount, would've caught the £13.39] / new-from-bank), reusing FITID/`import_hash` dedup. Centralises the enum into `mfl_desktop/txn_status.py`. Phased: (1) status foundation + migration 0033 (rename + `CHECK` rebuild via ADR-032 recipe) + register colours/legend; (2) reconcile-by-confidence (no schema change); (3) `bank_posted_date` migration 0034 + import review screen. Phases 1–2 alone make the June bugs structurally impossible. **Full detail in docs/adr/ADR-130.** **PHASE 1 SHIPPED 2026-07-02:** new `mfl_desktop/txn_status.py` (centralised enum + `label`/`key_for_label`/`labels`/`is_valid`/`is_locked`) replacing the ~8 duplicated `STATUSES` tuples; migration **0033** rebuilds `txn` with the lowercase `CHECK` + maps the data (ADR-032 recipe + `PRAGMA legacy_alter_table=ON` so the `RENAME` doesn't corrupt the `txn_category_line` view), FK-clean, indexes preserved; ~16 modules repointed (repository/import/parsers/reconcile/dialogs); combos show Title-case labels but read/write keys (`StatusDelegate` userData, register model DisplayRole=label/EditRole=key); account-summary balance labels relabelled **Cleared/Uncleared → Confirmed/Unconfirmed** to avoid clashing with the new `cleared` status (grouping unchanged). Register colour swatches/legend deferred to a Phase-1 follow-up (per-status `token` already in `txn_status.py`). `tests/test_txn_status_ladder.py` 6/6 (helpers + DB round-trip + CHECK-rejects-old + grep-guard) + full suite 23/23 + offscreen smoke. **PHASE 2 SHIPPED 2026-07-03** — reconcile-by-confidence: `list_reconcilable_txns` now gated by the ladder (matched always eligible, cleared only with a new `include_cleared` flag, **pending never** — the June-mess guard — plus rows already ticked into the resumed/viewed statement via `statement_txn` so ticks survive); removed the dead `list_cleared_unreconciled_txns`; new `count_cleared_in_period`. Reconcile wizard gained an **"Include cleared (seen at the bank, not yet downloaded)"** checkbox (balances page) that feeds both the candidate gate and the auto-select preset (matched + cleared-in-period when on), a **warning** on the check-off page counting excluded cleared-in-period rows with a nudge to include them, live re-gating on toggle, and disabled-in-read-only-view. `tests/test_reconcile_confidence.py` 4/4 + offscreen wizard smoke (gate → warning → include-cleared auto-select) + suite 24/24. **PHASE 3 CORE SHIPPED 2026-07-03.** 3a (backbone): migration **0034** adds `txn.bank_posted_date`; `merge_into_manual_transaction` advances a hand-entered row `pending`/`cleared`→`matched` and records the bank date when a download matches it (reconciled untouched); `insert_transaction`/`insert_split_transaction` gained `bank_posted_date` and imports stamp it on new-from-bank + matched rows; `list_reconcilable_txns` ranges/sorts/displays on `COALESCE(bank_posted_date, posted_date)` so reconcile aligns to the statement dates (fixes the 22/23-June scramble). 3b (precision): `dedupe.match_duplicates` gained an opt-in **amount-mismatch tier** (`fuzzy_amount_pct`, default 20%) — after exact pairing, a still-unmatched download row pairs with a same-sign, payee-overlapping existing row within tolerance as a **weak `amount_differs`** review item (the £8.99-for-£8.25 class); the import review dialog shows the discrepancy + an **"Adopt bank amt"** checkbox (default on, amount-differs rows only), and on confirm+adopt `commit_import` overwrites the mis-entry with the bank figure. `tests/test_import_bank_date.py` 4/4 + `tests/test_import_amount_mismatch.py` 7/7 + suite 26/26. **Remaining (optional follow-ups):** the unmatched-entries panel ("your entries the download didn't confirm") + widening the fuzzy window ±2→±5. Header tag flipped to [Phases 1–3 core shipped]. — **(ADR-129) Net-expense definition — reimbursements/refunds reduce spend across the flow reports.** Owner-reported: a "Reimbursed" category showed £1,730.79 on the Spending chart but the drill-down netted to −£45.04. Five aggregates (Spending, Payee, Category & Payee, Sankey, Income & Expense) used a **strict-outflow** definition that dropped positive refund lines, so the bar showed gross money-out and disagreed with drill-downs + each other. Fix: **net** — every expense line contributes its signed amount (refunds reduce the category), category/payee/cell that nets ≤£0 clamped to £0; `income_expense_series` regrouped by category so its expense bar equals the Spending total (verified reconciling to the penny on the demo). Income side left strict-inflow (documented asymmetry). SQL-only; no migration; `tests/test_net_expense_refunds.py` 5/5. — **(ADR-128) Consistent rounded bar corners across report charts.** Owner-reported: on Spending Over Time some bars round nicely at the top and others are square, inconsistently. Cause: each chart rounded its top *segment* only when tall enough, so a thin stacked cap fell back to a square top; `investment_income` never rounded; three charts used three radius formulas. Fix: round the *whole bar* with one shared radius via new `chart_helpers.bar_corner_radius()` + `round_bar_corners()`, which carves the corner wedges to the plot-background colour (works at any height, crisp under AA, composes over a painted stack). Wired into spending/income_expense/investment_income; payee (horizontal) left as-is. View layer only; `tests/test_bar_corners.py` 7/7 pixel-level; verified all three reports light + dark. — **(ADR-127) Lockable API-key field — read-only until "Change".** Owner-reported: the API key sits editable at the top of the Securities edit screen and looks too easy to change by accident. A stored provider key now opens **locked** (read-only + greyed via ADR-076 tokens, so it reads non-editable in light and dark) with a small **Change** button that unlocks it (focus + select-all for a clean paste-over); a fresh/empty field opens unlocked so a first-timer pastes straight in. Shared `mfl_desktop/ui/secret_field.py` `LockableSecretField` reused by **both** the Securities (Tiingo) and Currencies (OpenExchangeRates) dialogs (mirrors ADR-096's `transfer_chips` extraction); `.text()` proxied + `_key_edit = _key_field.line_edit` so the refresh/save handlers are unchanged and a locked field still submits the stored key. `setReadOnly` not `setDisabled`. View/dialog layer only; no migration; no change to key storage. `tests/test_secret_field_smoke.py` 8/8; screenshotted light + dark. — **(ADR-126) Frozen-app TLS trust store — point OpenSSL at certifi's CA bundle.** Owner-reported: in the packaged macOS `.app`, Update Prices failed for every ticker with `Could not reach Tiingo for … [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: unable to get local issuer certificate (_ssl.c:1010)`. Every outbound HTTPS call in the app is stdlib `urllib.request.urlopen` on the **default** SSL context (prices `prices.py`, FX `fx.py`, and the `feeds/*.py` bank clients). Inside a PyInstaller bundle OpenSSL's default CA search paths resolve to the *build machine's* prefix, which does not exist inside the signed bundle, and macOS OpenSSL never falls back to the Keychain — so there is no CA to verify against and every TLS handshake fails at verification. **Windows was unaffected** (Python loads the OS certificate store), which is why this only ever bit the Mac build. certifi's `cacert.pem` was in fact already carried in the bundle (`Contents/Resources/certifi/cacert.pem`, transitively) but nothing pointed OpenSSL at it. **Fix:** new `mfl_desktop/net_certs.py` `ensure_ca_bundle()` sets **`SSL_CERT_FILE=certifi.where()`** as the first statement of `__main__.main()`, before `QApplication` and before the background FX/price refresh runnables — so `ssl.create_default_context` (and therefore every `urlopen`) loads that bundle. One chokepoint covers prices, FX, and all bank feeds, current and future. It is **override-safe** (only sets `SSL_CERT_FILE` when unset, so a user/admin value wins), idempotent, exception-safe (a trust bootstrap must never stop the app launching), and a silent no-op in dev / on Windows / when certifi is absent — it only ever *adds* trust where the frozen build had none. certifi promoted from transitive to an **explicit pinned desktop dep** (`requirements-desktop.txt`, `certifi==2025.10.5`; data-only, not an API dep) and deterministically bundled via `collect_data_files("certifi")` + `hiddenimports=["certifi"]` in `mfl.spec`. **Verified:** reproduced the exact `_ssl.c:1010` error against `api.tiingo.com` by pointing `SSL_CERT_FILE` at a nonexistent path, then confirmed `certifi.where()` makes the handshake succeed (server answers at the HTTP layer); `SSL_CERT_FILE` alone suffices (no `SSL_CERT_DIR`). New `tests/test_net_certs.py` 5/5 (sets-from-certifi / respects-override / idempotent / no-op-when-missing / never-raises), green on both the base interpreter (no certifi → real no-op path) and the miniforge interpreter. Fixes prices + FX + feeds in the sandboxed MAS build (ADR-125) too, which carries the `network.client` entitlement. — **(ADR-125) macOS distribution: Mac App Store-only (sandboxed), reversing ADR-123's "macOS out of scope".** Owner is on a Mac and chose to pursue the **Mac App Store** as the macOS channel — bringing macOS back in scope and implementing the macOS leg of ADR-078 K3 (the sandbox path both ADR-078 and ADR-123 flagged as the hard one). Owner sub-decisions: **MAS-only** on Mac (Windows stays MS Store per ADR-123; no parallel direct DMG); **first-run "choose a folder"** so the working `.mfl` lives in a real/visible location held via a **security-scoped bookmark** (not the hidden container Documents); **paid up-front** (no IAP — the Store is the entitlement; ADR-079 offline key + 30-day trial go dormant in the store build via a build flag). **The defining constraint is the App Sandbox** (mandatory on MAS), which a 2026-06-30 code audit found conflicts with the file model in three concrete spots: (1) reopen-last-file stores a **plain path string** in QSettings (`app_session.last_db_path` → `launch.resolve_database`) — inaccessible under sandbox without a **security-scoped bookmark**; (2) the **Data Library** (`data_library.py`) writes `Library/` **beside the live file** — a file-scoped bookmark doesn't grant sibling-folder writes, so relocate it to the container (snapshots already moved there in ADR-109); (3) `cloud.py` shells out to **`brctl`** via subprocess (sandbox blocks spawning helpers; already no-ops on failure). Plus MAS packaging differs from ADR-104's Developer-ID notarised DMG: needs App-Sandbox/user-selected-file/bookmark/network entitlements, **Apple Distribution** signing of every embedded Qt/Python dylib, an embedded provisioning profile, a `.pkg` via `productbuild`, and Transporter upload. **Engineering arc A–F** (recorded in ADR-125): A `mfl_desktop/sandbox.py` foundation (`is_sandboxed()` via the `APP_SANDBOX_CONTAINER_ID` env var + a PyObjC security-scoped bookmark store; new **macOS-only** runtime dep `pyobjc-framework-Cocoa`, import-guarded; degrades to plain-path when unsandboxed so dev/non-Mac runs are unaffected) → B bookmark-based working-file persistence in `app_session`/`launch` → C relocate Data Library to the container → D sandbox-aware degradation (cloud no-op + cwd dev-bridge skip + legacy beside-file reads guarded) → E MAS packaging (`packaging/MyFinancialLife.entitlements` via `mfl.spec`'s existing `entitlements_file` hook + new `build_mas.sh`) → F store build flag making the licence nag/trial dormant. **Note — same source still runs UNSANDBOXED in dev** (`python -m mfl_desktop`), so the code branches on a runtime `is_sandboxed()` check, not a build target. **A–D + the entitlements plist are writable + unit-testable on a Mac TODAY without the Apple Developer account**; only the signing/upload steps of E need Program enrolment (org, gated on the B1 company). **— FIRST REAL AD-HOC SANDBOX BUILD (2026-06-30), uncommitted follow-ups + a major finding.** Built + ran an actual ad-hoc-signed sandboxed `.app` (no Apple account needed: `codesign --sign -` + the sandbox entitlements; the OS enforces the sandbox for any signed app carrying `com.apple.security.app-sandbox`). `packaging/build_mas.sh` gained a **`--adhoc`** mode (skips `.pkg`/upload, drops the app in `~/Applications`) and a fix for the **iCloud xattr blocker**: the repo is under iCloud-synced `~/Documents`, so PyInstaller output carries `com.apple.FinderInfo`/`com.apple.fileprovider` xattrs the fileprovider re-applies in place and `codesign` rejects ("resource fork… not allowed") → the script now stages the `.app` to a local `$TMPDIR`, `xattr -cr`s it there, signs inside-out, then places it (applies to the real build too). `mfl_desktop/__main__.py` gained `_open_repository_with_fallback()` (logs the resolved path; if a sandbox first-run location can't be opened, **falls back to the container** with a warning instead of crashing) + path logging in `_prompt_first_run_location`. **KEY FINDING — a live SQLite DB cannot run on iCloud Drive under the sandbox:** verified twice from the container log — the first-run picker pointed at the owner's iCloud `…/com~apple~CloudDocs/Banktivity Exports/` and File ▸ Open targeted the real `mfl_dev_windows5.mfl` there (14 MB, fully downloaded, NOT evicted); both → `sqlite3.OperationalError: unable to open database file`, no sandbox-denial logged. A LOCAL copy of the same file opens fine (28 accounts). iCloud paths are fileprovider-managed; SQLite WAL sidecar creation + POSIX locks aren't permitted there under the sandbox even with the user-selected-file entitlement, and **no entitlement fixes it**. (Generally true: a live SQLite/WAL DB on any cloud-synced folder risks corruption; the sandbox makes it a hard failure.) **→ the "first-run choose a folder" sub-decision is RE-OPENED** (the picker is a trap for this owner — their visible folders + real file are all in iCloud, where SQLite can't run). Live working file MUST be local. Owner to choose: (1) **container by default** [recommended] + Reveal-in-Finder + an explicit "import existing file" copy-to-local flow + iCloud only for exported backups; (2) folder picker that **refuses cloud paths**; (3) container default + optional local move. **Code changes (`build_mas.sh`, `__main__.py`) are in the working tree, NOT committed** (A–F arc was committed in `4761dfc`). **Testing-with-real-data workaround in place:** the owner's file was copied into the container (`…/Containers/life.myfinancial.app/Data/Documents/mfl_dev_windows5.mfl`) to open it locally; the test app is at `~/Applications/My Financial Life.app` (rebuild via `./packaging/build_mas.sh --adhoc`). **Status: decision + plan recorded; increment A SHIPPED (2026-06-30) — new `mfl_desktop/sandbox.py` (`is_sandboxed()` + `bookmarks_supported()` + `create_/resolve_security_scoped_bookmark` PyObjC primitives + `ResolvedLocation` scope handle + `encode_location`/`decode_location` policy: real bookmark only when sandboxed, tagged plain path otherwise, bare legacy paths still decode; degrades to plain paths off-Mac/no-PyObjC/unsandboxed, never raises). `pyobjc-framework-Cocoa==12.2.1` added under a `sys_platform=="darwin"` marker. `tests/test_sandbox.py` 10/10 on both PyObjC-capable + base interpreters. Increment B SHIPPED (2026-06-30) — bookmark persistence wired *additively* into `app_session`/`launch`: `remember_last_db` also stores a security-scoped bookmark (`session/last_db_bookmark`) when sandboxed (plain `session/last_db_path` stays primary, so `last_db_path()`/resolver logic unchanged); new `begin_main_file_access()` resolves+starts scoped access → live `ResolvedLocation`; `resolve_database` uses the bookmark's (authoritative, move-tracking) path, carries it in new `Resolution.location` (held by `main()` across `app.exec()` so access lasts the session), releases it on recovery/quit. File▸Open needs no change (powerbox grants session access; `remember_last_db` re-bookmarks for next launch). Tests: `test_launch_resolution` 11/11 (+2 bookmark cases), new `test_app_session_sandbox` 6/6, `test_sandbox` 10/10, compileall clean, real app boots offscreen with no dev regression (`location=None` unsandboxed). **First-run "choose a folder"** is a small follow-on UI step on FirstRunDialog (not part of B). Increment C SHIPPED (2026-06-30) — Data Library moved off beside-the-file to the app-data root (mirrors ADR-109 snapshots): new `app_session.library_root()` (default `AppDataLocation` → container under sandbox); `data_library.library_dir()` resolves under it via a Qt-free `_library_root()` indirection, written folder renamed `Library`→`MFL Library`, `list_saved()` unions the legacy beside-file `Library/` (dedupe prefers new) so nothing is orphaned; `test_data_library_current` 6/6 both interpreters, DataLibraryDialog builds offscreen against the relocated folder. (No relocation UI yet — optional follow-on.) Increment D SHIPPED (2026-06-30) — sandbox-aware degradation: `cloud.request_download` skips the `brctl` subprocess when sandboxed (falls through to read-to-hydrate; a sandboxed app can't spawn helpers); `launch.resolve_database` skips the cwd dev-bridge (`mfl_dev.mfl`) when sandboxed (cwd unreadable). Legacy beside-file union-reads need no guard (`.is_dir()` returns False without access). `test_cloud` 8/8 (+brctl-skip, darwin-gated), `test_launch_resolution` 12/12 (+legacy-skip); no import cycle, Qt-free imports intact. Increment E SHIPPED (scaffold, 2026-06-30) — MAS packaging: new `packaging/MyFinancialLife.entitlements` (minimal sandbox set: app-sandbox + user-selected files + app-scope bookmarks + network.client; sidecars in container so no beside-file entitlement) + new env-gated `packaging/build_mas.sh` (reuses `mfl.spec`; embeds provisioning profile; signs **inside-out** then the bundle with `--entitlements` [no `--deep`, disallowed for MAS]; `productbuild` signed `.pkg`; `altool` upload when API-key vars set). Entitlements applied at codesign time in the script (not via spec's `entitlements_file` hook — matches `build_macos.sh` post-build signing). Verified without Apple account: `plutil -lint` OK, `bash -n` OK, spec compiles, script +x. **Needs owner's Mac + Apple Developer enrolment to actually run** signing/`.pkg`/upload (and confirm PyInstaller's Qt/Python frameworks sign cleanly — the fiddly part). QA item: confirm report exports use a save panel (powerbox) else add `files.downloads` entitlement. Increment F SHIPPED (2026-06-30) — store-build licensing dormancy: new `version.is_store_build()` (stamped `_build_info.STORE_BUILD` flag via `stamp_build_info.py --store`, which `build_mas.sh` passes; falls back to `sandbox.is_sandboxed()`); `license_service.current_status()` short-circuits to unlocked `STATE_LICENSED` ("Purchased via the App Store") **without starting the trial clock**, so the launch nag + trial title-cue go silent automatically (single chokepoint). Help-menu Enter-License + Buy hidden; About box shows a plain store line with buttons hidden (Apple forbids external purchase links). `test_store_build` 6/6; offscreen real-stamp smoke confirmed all UI surfaces. **ENGINEERING ARC A–F COMPLETE + first-run folder-picker DONE (2026-06-30):** `Resolution.first_run_default` flag (set only on the unattended default branch); `__main__._prompt_first_run_location()` shown over the splash only when `first_run_default and sandbox.is_sandboxed()` (info box + powerbox folder picker; file created there + bookmarked; cancel → container default); dev/direct build untouched (guard skips it). `test_launch_resolution` 12/12 (asserts the flag) + offscreen picker smoke. **Remaining = account-gated only:** Apple Developer enrolment → certs/profile → run `build_mas.sh` (confirm Qt/Python frameworks sign cleanly for MAS) + QA on a signed sandboxed build.** Backlog (`docs/RELEASE_1.0_BACKLOG.md`) Workstream K got a supersession banner; its K0/K1/K3 text is now historical (read for the sandbox/effort analysis, not the current plan). — **(ADR-124) Budget annual matrix: rolled-over amount was cut off — drop the inline annotation.** Owner-reported: in the **annual** budget matrix a Budget cell carrying a rollover rendered `1,100.00  (+205.00)`, which overflowed the fixed **86px** month column (`budget_window.py` `setColumnWidth(c, 86)`) and was elided on the right → the carry (and sometimes the figure) was clipped. The cell already had three *un-clipped* rollover cues: the yellow `_rollover_bg()` background, a reconciliation tooltip ("Budgeted X + Y rolled over = Z available"), and the ↻ glyph in the label column. Fix (owner's pick, chosen over auto-fitting only the rollover columns [ragged widths + horizontal scroll] and a smaller-font carry via a paint delegate [more code, hard to read]): the matrix `DisplayRole` now returns just `_fmt(value)` — the `carry` value is still computed but feeds only `BackgroundRole`+`ToolTipRole`. No column-width change was needed once the annotation was gone. **Scope = annual matrix only:** the budget *monthly progress view* (`budget_monthly_view.py`) shows the carry inline as `{actual} / {available} (+205.00)` in a flexible-width label that grows to fit (never truncated), so it is left unchanged — the two views now differ in carry presentation, matching their densities. View-layer only, no migration. Verified offscreen on `mfl_public.mfl` (190 carry cells) + `mfl_dev.mfl` (54): each carry cell renders the bare figure with the `_rollover_bg()` background + a non-empty tooltip intact. — **(ADR-123) Distribution reversal: Microsoft Store-ONLY for 1.0.1 — supersedes ADR-078 K0.** For a small/low-revenue app the direct-download path was all fixed cost (annual code-signing cert + self-managed sales tax/accountant + a legal entity with merchant policies); the Store turns that into one variable **15%** cut and is Merchant-of-Record (signs the app, tax, payments, refunds, policies). **Verified (web, Jun 2026):** apps 15% via MS commerce / 0% own-commerce; **individual registration free** (since Jun 2025); **MS signs Store packages (no cert)**; **promo codes** give the paid app free to early-access volunteers; `broadFileSystemAccess` is a restricted-review capability but **avoidable** via file pickers + %APPDATA% (keep Snapshots/Library in %APPDATA% under MSIX). **Plan — one codebase, two channels:** **1.0 = local Inno .exe** (ADR-122) for early-access/sideload volunteers, **unsigned OK** (no cert bought); **1.0.1 = MS Store (MSIX)**, MS-signed, paid GA. **Shelves ADR-079** (own license key + MoR) — Store owns purchase+entitlement; licensing code stays dormant. Store auto-updates close ADR-078's WinSparkle gap. **Next round (own ADR): MSIX packaging of the PyInstaller output + file-access validation + Partner Center listing + promo-code flow.** macOS out of immediate scope. — **(ADR-122) Windows installer + signing-ready release pipeline.** Completes ADR-104's Windows leg: new `packaging/installer.iss` (Inno Setup) — **per-machine** Program Files install (owner fork; admin/UAC), stable AppId for in-place upgrades, Start-menu + uninstaller + optional desktop icon, `MyFinancialLife-<ver>-setup.exe`; user data stays in %APPDATA% (ADR-050) so uninstall never touches .mfl files. `build_windows.ps1` reads version from version.py, passes /D defines to ISCC, and **signs both the app exe (pre-package) and the installer (post-compile)** when WINDOWS_SIGN_PFX/PASSWORD set; unset → working unsigned build. CI now `choco install innosetup` + uploads `dist/*-setup.exe`. **Owner forks:** sign-before-1.0 (buying a Windows code-signing cert — signing gates 1.0); per-machine install. **Only step left for a clean signed 1.0 = procure the cert + set 2 env vars, no code change.** Deferred: auto-update (WinSparkle/Sparkle, unwired); tag→GitHub Release job. Verified what's checkable in-sandbox (PS parses clean; version query=1.0.0; .iss follows Inno 6 conventions) — owner runs the real build on Windows; CI compiles it. — **(ADR-121) Historical net worth (net worth over time).** The point-in-time Net Worth screen gains a **Now | Over time** view toggle (owner fork) with a **stacked-area + net line** chart (owner fork): asset families stacked above zero, debts below, bold net-worth line on top. Computed-not-stored: new pure `net_worth_history.py` (`gather_net_worth_history` — per account, cash balance via a txn replay + holdings market value via `compute_value_history`, FX-converted at each sample date per ADR-055 exclude-no-rate, bucketed into asset/debt families; `cash_balance_at_dates`/`month_end_samples` pure helpers; FX rate memoised per (ccy,date)). New paintEvent chart `net_worth_history_chart.py` (modelled on returns_chart). Window: Now|Over time pills in the page header over a QStackedWidget, period combo (1y/3y/5y/All), history built lazily + on ccy/closed/period change; reuses display-ccy + show-closed. New repo `earliest_posted_date()` for the All range. Family→kind passed in from `_FAMILY_VIEW` so the pure module stays Qt-free. **Note:** property/vehicle valuation is transaction-driven — revaluation entries posted in the asset account (the owner's Banktivity workflow) are tracked over time by the running-balance valuation, so a house bought/revalued/sold rises and falls correctly (verified 0→300k→350k→400k→0); no market price feed for them, and an asset is only flat if never revalued (data choice, not an engine gap) — so a separate valuation-timeline UI was dropped as redundant. **v1 deferred:** no point drill-down; no saved-report variant. Verified: final sample reconciles exactly with point-in-time net worth (delta £0.00 on the demo file); `tests/test_net_worth_history.py` 4/4; offscreen window build + over-time render + period switch + dark mode. On master. — **(ADR-120) Net Worth: one big donut + Assets/Debts toggle.** The centre panel's cramped fixed-190px Debts donut (real debt totals elided to nothing in its ~70px hole) is replaced by one big donut plus an Assets|Debts segmented toggle (same pill pattern as the ADR-113 Income&Expense toggle). Both sides' segments+totals are computed once per refresh and cached, so toggling re-renders from cache (no recompute); the legend shows only the active side (old ASSETS/DEBTS headings dropped); an all-asset file falls back to Assets and disables the Debts button; drill-down (ADR-083) works for whichever side shows. View-layer only — ADR-067 donut + ADR-055 FX + _compute_type_totals untouched; the second DonutChart + _heading_row removed. Verified offscreen on mfl_public.mfl (toggle swaps data+legend, full-size donut paints with the centre total intact, Debts disables with no debts, dark-mode restyles the pills). On master. — **(ADR-119) MRL-style chrome redesign.** Owner compared MFL side-by-side with the sister app **My Retirement Life (MRL)** and asked to make MFL "as sleek and professional as MRL." Diagnosis: MFL's design *system* was already strong (ADR-076 semantic light/dark tokens + global QSS + brand teal/gold), so the gap was **structural polish**, not colour. Reframe recorded: the owner's first screenshot was the **register** (a dense data grid — correctly dense, like Quicken/Banktivity), MRL's was its **dashboard**; the like-for-like is MRL's dashboard vs MFL's **Home**, so the register stays a grid (refined, not turned into a dashboard). Two forks chosen with the owner: **(a)** a *full* MRL-style redesign (drop the OS menu bar; page-header bar; card dashboard; primary/secondary buttons) — not a targeted pass; **(b)** the ~40 menu commands keep the **same menus/actions/shortcuts**, just rendered as flat styled dropdown buttons in a custom header (rejected: reorganising into fewer destination surfaces; a "More ⋯" overflow). Done on a **branch** — an explicit one-off exception to the commit-on-master rule, owner's call, given the size. Delivered in arcs, each verified offscreen + screenshotted. **Arc 1 (design system + sidebar + buttons, `27f5258`):** `theme.py` gains an opt-in **`mflVariant`** button system ("primary" filled / "ghost") set via dynamic property (any QPushButton joins without a subclass), and a flush, airier "navigation panel" treatment scoped to **`QTreeWidget#sidebar`** (borderless, roomier rows). `sidebar.py`: the tree gets the `sidebar` objectName; section headers become muted-caps **labels** (no grey band) with letter-spacing. `register_window.py`: sidebar brand header gains a **subtitle** ("personal finance"); the **Garelochsoft publisher mark moves from the status bar to a sidebar footer** ("PUBLISHED BY", superseding ADR-117's status-bar placement); "New Transaction" becomes the filled **primary**; roomier filter bar + register row height 22→26. **Arc 2 (app header replaces menu bar + toolbar, `7e4a8fd`):** the native **`QMenuBar` and the ADR-116 quick-action `QToolBar` are gone**. New **`ui/app_header.py`** (`AppHeader`) hosts the eight menus as flat **`QToolButton`** dropdowns built from the *same* `QMenu`/`QAction` objects (handlers + shortcuts unchanged), with a left menu cluster and a right utility cluster — the ADR-116 Update Prices/Rates/All actions folded into one **`Update ▾`** dropdown, a **dark-mode toggle**, and a **person chip** (initials avatar). New **`ui/page_header.py`** (`PageHeader`) — MRL-style title + subtitle, driven from `_update_window_title` so it tracks the view (Home / All transactions / account name + currency). `_build_toolbar`→`_build_header_utilities`, `_build_menus`→`_build_header_menus`; the **View menu is removed** (Dark Mode is now the header toggle; the toolbar-visibility toggle is obsolete). Without a menu bar a QAction's accelerator only fires if it's on a live window widget, so every shortcut-bearing action is re-registered on the window. macOS loses its native top menu bar (Windows-focused; noted in the ADR). **Arc 3 (Home dashboard, `3e0c3e6`):** net worth becomes a **full-width hero card** above the two-column grid — 40px number, summary line, brand-**accent left edge** (`QFrame#homeHeroCard`); `_Card` gains an optional header **"action →" link** (accent, right-aligned — the MRL clickable-card affordance) wired on the budget/bills/top-payees/top-categories cards. The **report windows** inherit Arc 1's theming (buttons/tables/cards) but the page-header retrofit **shipped 2026-06-29** (see the ADR-119 retrofit addendum below). **Follow-ups same session:** **(i)** the header **person chip** derives initials from the file's `person.name`, reads the *current* repo, and is refreshed on every repo swap (`_adopt_repository` — covers File ▸ Open + Data Library loads — and after first-run) so it never shows a previously-open file's holder (`e13ff24`); the avatar-initials confusion was the demo `mfl_public.mfl` holder "Jordan Avery" persisting after opening another file. **(ii)** There was **no way to set the name** (first-run only names the *account*; the seed person is "Me"), so clicking the chip now opens a small **`ProfileDialog`** (live initials preview) that writes `person.name` via the new **`Repository.set_person_name`** and updates the chip live (`9930625`). **(iii)** Fixed a **use-after-free crash** clicking a Home card (`ddba317`): the clickable `_Card`/`_Row`/`_AccordionHeader` emitted `clicked` (whose slot rebuilds Home and deletes the widget) *before* `super().mousePressEvent` → "Internal C++ object already deleted"; reordered to run base handling first and emit last — a **latent pre-existing bug** made easy to hit now Home is reachable from both the sidebar and the new header Home button. Verified offscreen on `mfl_public.mfl` throughout (chrome renders with no menu bar/toolbar; chip "JA"→"MH" on rename + persists; empty name rejected). **Branch state:** `redesign/mrl-style-chrome` = 6 commits (Arc1/Arc2/Arc3 + chip-refresh + crash-fix + profile-editor), **not merged to master** — owner to review/merge. Note: the owner's real `mfl_dev.mfl` person is the placeholder **"Test User"** (chip "TU"); rename via the new avatar dialog. **Prior 2026-06-27 session — (ADR-118) Import-time category review + transfer-ref fix + import undo.** Diagnosed from the owner's live file: importing **Amex Gold.csv** (generic CSV) created categories it shouldn't. Two causes — (a) **bug:** Banktivity writes transfers in the category column as a bracketed last segment (`[Chase Checking]` / `Transfer:[Chase Checking]`); the QIF path handled this (ADR-111) but the generic-CSV path didn't, so `[Chase Checking]` became a category with 123 txns; (b) **default:** ADR-112's map only reroutes *previously-curated* categories — a brand-new name is still created (match-only off), against the owner's expectation. Fixes: **(1)** `import_service._transfer_ref_account` detects a bracketed **last** segment centrally → resolves to Uncategorised + a `Transfer to/from {acct}` memo note (for Reconcile Transfers, ADR-037), excluded from the auto-categorisation fill so it's never re-spent; **(2)** `plan_new_categories` + new **`ImportCategoryReviewDialog`** list would-be-new categories before commit so the user **maps / creates / sends to Needs Review** each (a *map* is recorded as a durable import mapping); `commit_import(category_decisions=...)` applies them; no new categories ⇒ no dialog; **(3)** `Repository.delete_import_batch`/`list_import_batches` + **File ▸ Undo Import…** (`ImportHistoryDialog`) reverse a past import (txns first — `import_batch_id` is ON DELETE SET NULL; splits cascade). **Undo also offers to delete the categories that batch created which are now empty** (`delete_import_batch` returns them; `delete_empty_import_categories` does a **direct** delete — *no* Needs-Review mapping, unlike ADR-112 `delete_category` — so a re-import re-offers them in the review dialog; emptiness guarded against txns/split-lines/children/`scheduled_txn`/`budget_line`). A category an *earlier already-undone* import orphaned isn't attributable to the batch → stays a manual delete. Service/UI/repo only, no migration. Verified end-to-end on a copy of the live file + the real CSV (2,667 imported, 123 `[Chase Checking]` rows Uncategorised+noted, 0 bracket categories, only `Household:General Items`+`Gift Received` flagged new; undo reports those two as now-empty and deleting them makes a re-import re-flag them); `tests/test_import_category_review.py` 8/8; ADR-112 map tests still 11/11. **Owner cleanup state:** the owner already re-imported once on the fixed code (transfer bug gone — `[Chase Checking]` empty, 123 transfers noted) but the review dialog didn't show because `General Items`/`Gift Received` still existed from the first bad import. To get the review (their choice): relaunch → File ▸ Undo Import → the Amex batch (now offers to delete the 2 empty categories) → manually delete the leftover empty `[Chase Checking]` in Manage ▸ Categories → re-import `Amex Gold.csv` → review dialog. **Prior 2026-06-27 (quick-action toolbar + brand chrome) — added the first main-window toolbar, brand logos in the persistent UI, and recorded the post-feature polish gaps. (0) Brand chrome — ADR-117.** Owner feedback "the UI is very text heavy." Added an **MFL brand header atop the sidebar** (`_build_sidebar_panel` wraps the tree under the hexagon mark + "My Financial Life" in `accent` teal, shared `surface` bg + hairline rule) and pinned the **Garelochsoft wordmark to the status bar** as a permanent widget (`_add_company_logo_to_status_bar`, survives transient `showMessage`, tooltip) — the owner's pick from four mocked placements. The supplied logo PNGs carried a **flat light-grey background** (ugly light box on dark surfaces); new `tools/make_transparent_logos.py` flood-fills it out from the edges → transparent `garelochsoft_logo.png` (overwritten) + new `mfl_mark.png`; new `resources.brand_mark(size)` loads the transparent hexagon for in-UI chrome (sidebar header, About, first-run, splash now use it), distinct from `app_pixmap`/`app_icon` which feed the **untouched** dock/taskbar icon set. Asset+view layer only, no migration; verified offscreen (header + status logo render; `brand_mark`/`company_logo` non-null with alpha; knockout composites clean on dark). **This session also added the first main-window toolbar and recorded the post-feature polish gaps. (1) Quick-action toolbar — ADR-116.** A persistent non-movable `QToolBar` at the top of `RegisterWindow` (`_build_toolbar`, called right after `_build_menus`; text-only, no per-action icons): **Home · Update Prices · Update Rates · Update All**. **Home** = `sidebar.select_home()` + `_show_home()` (closes the owner gripe that Home — a sidebar row since ADR-075 — is easily missed). **Update Prices / Update Rates fetch directly, no dialog** — the same synchronous `force=True` path the Securities/Currencies dialogs' Refresh-Now buttons use (`prices.refresh_latest_prices_into` / `fx.refresh_latest_into`), under a wait cursor, then `_refresh_sidebar_balances()`; outcome on the status bar (`"Updated N prices"`), a `QMessageBox.warning` only when errors are non-empty, and **route-to-dialog when the API key is unset** (info box → Manage ▸ Securities/Currencies). **Update All** runs both in one click (the F2 "Update all"): shared logic factored into `_refresh_prices`/`_refresh_rates` cores that each catch their own exceptions so one failing provider doesn't abort the other; a provider with no key is **skipped** in the status line (`"Updated 5 rates · skipped prices (no Tiingo key)"`), not a dialog — one click must not spawn two modal asks; **bank feeds excluded** (need interactive consent, keep their own dialog). **Refresh runs off the UI thread** (owner feedback — the first cut froze the window): a `QRunnable` on `QThreadPool` with its own `Repository` connection (WAL-safe, mirrors `__main__`'s launch refresh) hands results back via a `QObject` signal connected to a **bound method** (not a lambda — an unbound functor has no thread affinity, so the slot could run on the worker thread); the three Update actions disable while running, status shows "Updating…". **Refreshing results no longer moves the user** (owner feedback — it jumped to the first account): new `_refresh_after_market_update` reloads the sidebar *preserving selection* (not `_refresh_sidebar_balances`, which re-selects an account → on Home, the first account) and re-asserts Home + refreshes the dashboard when Home is showing. **Toolbar is restorable** (owner feedback — hiding it via Qt's context menu was a dead end): `toggleViewAction()` added to the **View** menu ("Quick Actions Toolbar"); `_build_toolbar` now runs before `_build_menus` so the toggle can be wired. No new refresh logic (reuses the dialogs' functions, so 24h-skip-bypass / rate-limit back-off / per-symbol error collection stay single-sourced). View layer only, no migration. Verified offscreen on `mfl_public.mfl`: toolbar builds `[Home, Update Prices, Update Rates, Update All]`; the View toggle hides+restores it; a threaded Update All from Home **stays on Home** (stack idx 0, sidebar `home`) with actions disabled→re-enabled and status `"Updated 4 prices, 6 rates"`; an Update Prices from an account view stays on that account; no-key branches route to the dialog / report "Nothing to update". **(2) Backlog refreshed (`docs/RELEASE_1.0_BACKLOG.md`).** Added **P7 — Post-feature polish** (toolbar [done]; per-feature in-app Help for bonds/options + loans + budget bills + Investment Income + reports-include-closed; the Investment Income filter dialog not yet on the shared `ReportFilterDialogBase`; a **UI-freeze checkpoint** before the manual), carried the open **post-ADR-112 category triage** owner action into the backlog, and expanded **W2** with the **full user manual (web + PDF, screenshots last)** as the explicitly-LAST task once the UI is steady. **Prior 2026-06-27 session (report polish + analysis fixes) — shipped one feature + four report fixes, all view-layer, no migrations, pushed to `master`.** **(1) Income & Expense breakdown donut — ADR-113.** A composition donut + **Income / Expense** toggle pinned to the bottom of the report's right-hand summary panel, showing the period's cash-flow split by **top-level category**. Reuses the Sankey category-kind totals (`sankey_category_totals`), rolls each leaf up to its top-level ancestor via new **pure `mfl_desktop.reports.income_expense.compose_top_level()`** (`CompositionSlice` rows, largest-first, tail folded into "Other"), and renders with the existing `DonutChart`. A **donut, not a pie**, is the ADR-067-blessed shape for a point-in-time composition of a positive whole; the time-series stays bars+line. Single **flat ring** (new `DonutChart(two_ring=False)` flag — Net Worth keeps its two-ring sunburst), **no centre total** (the headline already shows it), compact **legend** (swatch · name · amount) underneath; Income/Expense toggle styled like the Account-Summary period pills, checked-state synced. Verified `tests/test_income_expense_composition.py` 4/4 + offscreen render. **(2–5) Spending / Income Over Time drill-down — ADR-114 (+3 same-day amendments).** Owner gripes, fixed in sequence: **(2) a click never reached the transactions** — the rollup descent ladder maps `leaf→leaf`, so at the leaf level a click just re-narrowed to the same category forever; now a terminal click (already-leaf, or a category with no children) opens a `TransactionsListWindow` for the category-and-descendants. **(amend 1) Scope to the clicked bucket** — it opened the whole report range ("Rent for 2023" listed 2007–2025); now resolves the bucket key to a date span via `bucket_bounds` (the I&E pure module, both reports share `_BUCKET_EXPR`) against a remembered `self._last_granularity`, **clamped to the report range** so an edge bucket lists only the slice the bar summed. **(amend 2) Double-click opens transactions; single-click drills** — the real "wrong year" bug: a single click *drills* and re-lays-out the chart, so a double-click's **first press drilled before the second click was recognised**, landing on a different (drilled-into child) bar → subset/wrong data. `SpendingChart` now **defers the single-click drill by `QApplication.doubleClickInterval()`** and emits a new **`segment_double_clicked`** on a real double-click (timer cancelled, no drill); the window opens the clicked category+bucket directly on double-click, single-click still drills. **(amend 3) Reinvested-dividends series hidden while drilled** — the ADR-110 standalone DRIP series (`REINVESTED_GROUP_ID`) showed independent of the category filter, which is noise once drilled into e.g. Rental Income; `_refresh` stops requesting it from the aggregate while `self._drill_stack` is non-empty (`include_reinvested = toggle and not drilled`), returns on Back. **(amend 4) Category drill is split-aware** — `DrillDownFilterProxy` matched only `txn.category_id`, but a split parent's `category_id` is Uncategorised (real categories on `txn_split` lines), so a category that lives only on split lines drilled to an empty list even though the report counts those lines (via `txn_category_line`); now also accept a row when its `split_category_ids` intersect the drilled subtree, mirroring the main register's base proxy. **(6) Reports include closed accounts by default — ADR-115.** Closed (archived, ADR-069) accounts dropped out of `list_accounts()`, so **every analysis report silently excluded their transactions** — breaking long-term trend analysis (close last year's account → its history vanishes from the charts meant to show the long run). All **seven** report windows (Spending, Income, Income & Expense, Payee, Category·Payee, Sankey, Investment Income, Investment Returns) now seed accounts with **`include_closed=True`** (cash via `list_accounts`, investment via `list_investment_accounts`); the empty-`account_ids`="all" default and the filter checklist both pick them up. Shared `ReportFilterDialogBase._make_accounts_panel` tags a closed row **"(closed)"** for easy unchecking; saved-filter convention unchanged (empty == all; explicit subset respected). **Operational write-path pickers** (new txn, transfer dest, schedules, budget setup, loans, bank feeds) and **Net Worth's own point-in-time "Show closed" toggle** deliberately untouched. Verified offscreen: closed-account income now in the report by default + filter lists it tagged/checked. **Prior 2026-06-26 session — import robustness + investment-report fixes: shipped five fixes on top of the same-day ADR-109 (below), all pushed to `master`.** **(1) Imports stop recreating curated categories — ADR-112, migration 0032.** Importing **REI Master Card** recreated categories the owner had merged/reparented/deleted: the importer resolved each source path (e.g. `Bills:Utilities:Cable`) through `find_or_create_category_path`, which *creates* anything it can't find, so every curation was silently undone (`source='import'` is no filter — 179/186 of the owner's categories are import-sourced). Migration 0032 adds **`category_import_map(source_path TEXT PRIMARY KEY, target_category_id … ON DELETE CASCADE)`** keyed on a **normalised** (trimmed + lower-cased, ':'-joined) path, plus a seeded **"Needs Review"** holding category (id in `setting.needs_review_category_id`). The map is **auto-recorded by the curation verbs inside their existing transactions**: `merge_categories` records each source's full path → target (and repoints any mapping that targeted a source); `delete_category` records the deleted path → Needs Review (and repoints mappings that targeted it); `reparent_category` records the **old** path of the moved category *and every descendant* → their unchanged ids. New repo helpers: `normalize_category_path`/`_category_path_string`/`category_display_path`, `find_category_path` (the create-free twin), `get/set/delete/list_category_import_map`, `import_match_only_categories`/`set_…`, `needs_review_category_id`. `ImportService._resolve_category_id` now resolves: empty→Uncategorised → **explicit mapping wins** → existing path used as-is (no dup) → else create **unless** the per-file opt-in **match-only** setting (`import_match_only_categories`, default OFF so a fresh file still builds its tree) is on, then park in Needs Review. UI: Manage ▸ Categories gains a **"Match imports only"** checkbox + an **"Import Mappings…"** button → new read-mostly **`ImportMappingsDialog`** (lists redirects, Forget). **Triage for the damage already done:** redo the 6 forked merges by hand (`Bills:Utilities:Cable and Internet`→`Bills:Cable and Internet`; `:Mobile Phone`→`Bills:Phone`; `Personal:Education:Tuition`/`:Books`→`Personal:Education`; `Fees:Charges`→owner's choice; `Cash`→`Personal:Cash`; then delete the emptied `Bills:Utilities`+`Fees`) — and because merge now records mappings, doing them inoculates the next import. Verified Qt-free `tests/test_category_import_map.py` 11/11 + offscreen `tests/test_category_map_dialogs_smoke.py` 4/4; demo upgrades through 0032. **(2) QIF cash-ledger import — ADR-111.** The QIF parser dispatched only investment sections, so the REI `!Type:CCard` export imported **0 transactions**. `_section_for_header` now maps `!Type:Bank/CCard/Cash/Oth` → a `cash` section feeding the same cash dict CSV/OFX produce (`_normalise_cash_record`: D/T/U/C/P/M/N/L, sign from T, `L[Account]`→transfer note); splits import at total for now. No downstream change (no `action` key → routed as cash). Verified `tests/test_qif_cash.py` 6/6 + end-to-end (1,202 committed, re-import dedupes to 0). **(3) Crash on quit fixed.** On quit RegisterWindow closes the shared Repository, but a queued `WindowActivate` could still reach a secondary window (Account Summary, a drill-down list); its refresh then queried the closed connection and crashed. Both now guard their activate-refresh on `repo.is_open()` (matching BudgetWindow/HomeView). Verified `tests/test_shutdown_refresh_guard.py` 2/2. **(4) Reinvested dividends as their own income series — ADR-110.** The ADR-089 *Include reinvested dividends* toggle merged DRIP amounts into the matched income category (they vanished into *Interest Inc.*). Renamed **"Show Reinvested Dividends"** + surfaced as a distinct legend series (`REINVESTED_GROUP_ID = -100`). View layer, no migration. **(5) Investment drill-downs use investment columns** (no ADR; bugfix). Drilling from Investment Income/Returns or an investment Account Summary opened a register with cash columns (Payee/Category); `TransactionsListWindow._set_model` now derives `invest` from the account family (or security scope for a cross-account drill), and `register_model` gains `COLUMNS_INVEST_ALL` (security-aware + Account column, no running balance) for cross-account security drills. **Prior same-day session — ADR-109: cloud-safe data file — never-silent-swap launch, visible working file, configurable backups.** Owner relaunched and a day of edits was "missing": the app had opened a *different* file and Manage Data showed neither the current file nor any snapshots. Root cause (confirmed on disk): the real working file lives in a **cloud-synced folder** (iCloud Drive — `…/com~apple~CloudDocs/Banktivity Exports/mfl_dev_windows3.mfl`); the `session/last_db_path` pointer was correct, but the launch resolver gated it behind `Path.exists()`, and an **evicted** cloud file (bytes off-disk, placeholder only) reads False — so it **silently fell through** to the stale repo `mfl_dev.mfl` / hidden app-data file, whose beside-file Snapshots folders looked empty. General problem (iCloud backs macOS Documents/Desktop by default; OneDrive on Windows), so the fix is provider-agnostic. **Launch resolution moved to a unit-testable `mfl_desktop/launch.py` (`resolve_database`)** with precedence `--db → pointer(materialise-or-recover, NEVER silent swap) → legacy cwd dev file (pointer-absent only) → first-run default`; every resolved path is now an explicit choice so `__main__` records it unconditionally (the whole `fell_back_from_remembered` block deleted, superseding the ADR-092 amendment). New **`mfl_desktop/cloud.py`** (provider-agnostic; the change's *only* platform branch): `is_available` (a real 1-byte read, stronger than `exists()`), `icloud_placeholder` (`.{name}.icloud` marker), `request_download` (`brctl download` on macOS / read-to-hydrate elsewhere), `ensure_available` (bounded poll on a daemon thread, `pump`s `app.processEvents` so the splash keeps painting during a download). New **`FileRecoveryDialog`** (shown over the splash, no parent): **Retry / Open a different file… / Start a new file…** / close=quit. **First-run default now a *visible* `~/Documents/My Financial Life/`** (`QStandardPaths.DocumentsLocation`) instead of hidden app-data. **Snapshots folder is now configurable + renamed `Snapshots`→`MFL Snapshots`**: retention *policy* stays per-file (`setting` table, portable across a Mac↔PC synced `.mfl`); folder *location* is app-level (`app_session.snapshots_root`, key `locations/snapshots_root`, default `AppDataLocation` — snapshots are multi-MB, don't bloat the cloud); `existing_snapshots`/`prune` **union-read** the legacy beside-file `Snapshots/` so no upgrader history is orphaned (kept Qt-free via a lazily-imported `_snapshots_root` indirection so it still imports on the base interpreter). **Manage Data now shows the live working file** (`data_library.current_file` → pinned bold non-deletable `kind="current"` row) + a new **Locations…** button → `LocationsDialog` (main file: *Open existing…* / *Move to folder…*; snapshots: *Change…* parent / *Reveal*), wired to window handlers (`_on_open_existing_main`; `_relocate_main_file` = `checkpoint`+`save_copy`+verify-open+`_swap_repository`+best-effort-delete, never a cross-device `os.rename`; `_set_snapshots_root`). **Auto-save on exit hardened**: `closeEvent` factored into idempotent `_flush_and_close` (final snapshot → WAL `checkpoint(TRUNCATE)` → close) also wired to `app.aboutToQuit`, so Cmd/Ctrl-Q (which bypasses `closeEvent`) still self-contains the `.mfl`. Verified: compileall clean; Qt-free `tests/test_cloud.py` 7/7 + `test_snapshots_location.py` 4/4 + `test_data_library_current.py` 2/2 on base python; PySide6 `test_launch_resolution.py` 9/9 + offscreen `test_locations_dialogs_smoke.py` 4/4 under miniforge; IRI guard 6/6; read-only check confirms the resolver now targets the owner's real iCloud file. **Dev-workflow note:** with an authoritative pointer a checked-out repo no longer auto-opens `mfl_dev.mfl` once a pointer is set — use `--db mfl_dev.mfl`. **Out of scope:** true multi-writer safety for two machines on one cloud file (SQLite WAL isn't networked; "Open a different file" is the escape hatch). **Prior 2026-06-25 session — ADR-107: Save & New on both New-Transaction dialogs — right-aligned + the default action.** Owner reported the ADR-105 Save & New button had "moved to the left": `QDialogButtonBox`'s macOS layout policy banishes `ActionRole` buttons (Save & New, Split) to the *left*, away from where the eye lands. Replaced the auto-laid box in **both** the cash (`transaction_dialog.py`) and investment (`investment_transaction_dialog.py`) dialogs with a hand-laid `QHBoxLayout` row — deterministic + identical cross-platform — ordered secondary-action-left, then **Cancel / Save / Save && New on the right**, with **Save & New the default** (Enter commits + reopens for fast multi-entry). `autoDefault` pinned off on the non-default buttons so a focused button can't steal the default; Esc still rejects (QDialog built-in). The **investment dialog gains Save & New for the first time** (new `_save_and_new_requested` flag + `_on_save_and_new` reusing `_on_save` so the paths never diverge + `save_and_new_requested()` accessor), shown **create-mode only** — an edit keeps Cancel/Save with Save as default; `register_window._open_investment_txn_dialog` now returns the reopen flag and `_on_new_transaction` loops it exactly like the cash `_create_one_transaction` path. Deliberate UX call: Save & New default means Enter reopens (Esc to stop) — owner-requested for batch entry. No migration; view/dialog layer only. Verified offscreen on the public-demo DB (cash + investment-create order `Split…/Cancel/Save/Save && New` default=Save & New; investment-edit order `Cancel/Save` default=Save, Save & New absent) + compileall (all modules) + IRI guard 6/6. **Prior 2026-06-25 session — ADR-106: payee category pre-fill now infers from history + remembered categories surfaced in the Rules screen.** Diagnosed two owner reports ("Scot Rail" entered as Uncategorised; Rules screen empty despite auto-categorising payees) as a design gap: auto-categorisation runs off the per-payee `default_category_id` memory (19 payees), NOT the `rule` table the Rules screen shows (0 rules), and the typed payee was an un-merged variant with no memory. Fix: `most_common_category_for_payee` history fallback in the New Transaction dialog pre-fill (no data writes, transfer-kinds excluded), and a manageable "Remembered payee categories" section (Edit / Forget) folded into the Rules dialog. No migration; verified offscreen on a live-DB copy. Merging fragmented payee spellings deferred (owner). **Prior 2026-06-25 session: ADR-105 — four `docs/backlog_notes.txt` papercuts, view/dialog layer only.** (1) **Save & New** button on the manual New Transaction dialog (register's new-txn handler refactored into a loop over `_create_one_transaction`, reusing the full transfer/split commit path); (2) **payee autocomplete** `QCompleter` in that dialog (same config as the register cell + Bulk Edit); (3) **report windows no longer drop behind the register** after their modal filter dialog — `raise_()`+`activateWindow()` in Spending (→ Income subclass) + the four sibling report windows; (4) **bulk edit in the report drill-down** `TransactionsListWindow` — register-matching edit triggers, reconciled-edit guard, right-click + Ctrl+E **Bulk Edit N…** via `BulkEditDialog`/`bulk_update_transactions` for payee/category/status/memo, with **transfers + splits deliberately left to the register** (transfer-kind category refused with a redirect). No migration. Verified offscreen on the public-demo DB + compileall + IRI guard. **Prior 2026-06-21 session below: Phase-1 launch-polish: P3b de-overlap + M1 IRI guard shipped (ADR-096); P4/P5/P6 polish to follow.** **(P3b — last de-overlap candidate, ADR-096.)** The two transfer-matching surfaces (inline confirm/picker ADR-036; Reconcile Transfers ADR-037) each carried byte-identical `_CHIP_COLOURS`/`_fmt_amount`/strength-chip copies → extracted to shared **`mfl_desktop/ui/transfer_chips.py`** (`CHIP_COLOURS`/`fmt_amount`/`strength_chip`/`strength_chip_holder`); both dialogs import them aliased to old private names (call sites untouched), row layouts stay bespoke, dead locals + unused imports removed. Chip colours stay literal hex (white-on-colour pills read in both themes, ADR-076). **P3 now COMPLETE** (P3a ADR-084 + P3b + P3c-confirmed). **(M1 — IRI boundary guard, ADR-096.)** First file under **`tests/`**: Qt-free `test_iri_boundary.py` (runs on base `python3` or pytest; bare-script runner prints PASS/FAIL) pins the MFL↔MRL join key — accounts/person stay `mrl:<Class>_<n>` (class-scoped sequential, never `mfl:`), private entities stay `mfl:`, the `mrl:Person_1`+`mrl:CashAccount_1` seed is pinned at both seed sites (source-text check, no Qt), account round-trips via `get_account_by_iri`; negative-checked to fail on a simulated `mrl:`→`mfl:` regression. Also ticked stale DoD boxes (P1 was already complete). **(P4 — visual polish, ADR-097.)** Dark-mode completion sweep of the ADR-076-r3 item-brush bug class (frozen light hex on table/tree item foregrounds): closed four stragglers — `net_worth_window` "(no rate)" flag, `investment_returns_window` gain/loss table cells + Performers % labels (deleted `_GAIN`/`_LOSS` constants; `_colour` now returns `tokens.c("positive"/"negative")` live), `statements_window` status column + summary (`_status_text` returns a token *name*, resolved at call site; deleted `_MUTED`/`_GREEN`/`_AMBER`/`_RED`), `transfer_match_dialogs` picker sentinel (`tokens.c("accent")`). All resolve at populate time (reference pattern = `account_summary_window` holdings table); light values unchanged except statements "In progress" amber-600→amber-700 `warning` (no exact token; closest match, = net-worth's flag). Chart *series* colours intentionally stay literal (established `balance_flow_chart` pattern). **Button audit (ADR-097): already consistent** — ~40/47 dialogs use `QDialogButtonBox` (native order + Esc + default); hand-rolled remainder are action toolbars (no default = correct, Enter mustn't Delete). Open under P4: typography-scale round + app icon (assets). Empty-state folded into P5. **(P5 — first-run onboarding & help, ADR-098.)** New `FirstRunDialog` (`first_run_dialog.py`) shown once when `__main__` just seeded a brand-new file (`just_seeded` flag at the `_seed_starter_db` site): pick **base currency** (closes the GBP-hardcode gap) + name the first account, with **Get started** / **Import a statement…** exits (both apply first; import routes via new public `RegisterWindow.start_first_run_import(iri)`). New `Repository.set_base_currency` writes the `setting` key the app reads **and** `person.base_currency` together; applies chosen ccy+name to the starter account; `refresh_after_first_run()` re-renders Home+sidebar. Esc applies nothing (strictly additive). **Help menu** gains Getting Started + Visit Website → `version.DOCS_URL`/`WEBSITE_URL` (new product constants, placeholder launch-domain URLs until W1). About box already shipped (ADR-079). **Crash-safety review:** confirmed no change for 1.0 — K0 direct-notarised (non-sandboxed) keeps ADR-057 Snapshots/Library-beside-file working; sandbox is K3/1.1+. **(P6 — release hygiene, ADR-099.)** (1) **Deps pinned:** new `requirements-desktop.txt` pins the *actual* desktop runtime — **PySide6 / cryptography / ofxtools** (the root `requirements.txt` is the legacy v0.1 web-app's deps and didn't even list PySide6; FX/price use stdlib urllib). (2) **Build metadata:** `version.build_revision()`/`build_string()` read a CI-stamped `_build_info.py` (gitignored; "source" fallback, never shells git at runtime); About shows a "Build {rev}" line. (3) **Crash log + diagnostics (no telemetry):** `mfl_desktop/diagnostics.py` — rotating file log (`<AppData>/MFL/logs/mfl.log`) + a re-entrant-guarded excepthook (logs + best-effort "your data is safe / log is here" dialog), both wired early in `main()`; `collect_diagnostics(repo)` → PII-light blob (env/build/Python/Qt/OS, paths, account count, log tail); Help ▸ **Export Diagnostics…** writes it to a user-chosen file. (4) **Test gate:** `tests/` IRI guard green + import-all/`compileall` smoke (135 modules, 0 fail). Open under P6 (with the ADR-078 packaging round): per-OS PyInstaller specs + full lock + CI matrix. **— P1–P6 engineering items done.** **(P4 finish-out: brand identity, ADR-100/101/102.)** Owner supplied app-icon artwork (teal+gold hexagons; MFL = gold "M"+coin+arrow on petrol teal). **ADR-100 brand re-tone:** accent token blue-600 → **brand teal `#1f6e78`** (dark `#39a0aa`), which via the ADR-076 token system re-tones the whole UI (buttons/selection/links/focus/menu) in one place; new **`brand_gold`** token (`#c9a23a`, contrast-safe graphic accent only — first use = About divider rule; never text/money). Accent-semantic chart lines (balance/today/net/payee-bar/price/invested) now read new theme-aware `chart_helpers.chart_accent()`; categorical/data blues (GROUP_PALETTE, net-worth families, returns cost line) intentionally kept (collide with existing teal/cyan). Green/red unchanged. **ADR-101 app icon:** extracted MFL hexagon → `assets/icons/` (PNG set + macOS `.icns` via iconutil + Windows `.ico`); new `resources.py` (sys._MEIPASS-aware) wires it as the runtime window/dock icon; `.icns`/`.ico` feed the ADR-078 packaging step. **ADR-102 typography:** `ui/type_scale.py` = 9 named px steps reverse-engineered from existing use; folded the two off-scale one-offs (14→LEAD 15, 17→SUBTITLE 18) so every `font-size` is on-scale. **P4 now COMPLETE.** **(Brand polish, ADR-103.)** Surfaced the icon in-app + a launch splash (owner-requested): new `resources.app_pixmap(size)`; icon in the **About box** (64px beside name/version/build) + **first-run welcome** (48px beside heading); new **`mfl_desktop/ui/splash.py`** branded `QSplashScreen` (icon + teal name + "Loading…") shown in `__main__` before the DB/migrations/window work and `finish(win)`-closed when the window appears (uses brand literals, not tokens — composed pre-theme). Owner declined a Home empty-state getting-started card (first-run dialog already covers onboarding). **(Packaging scaffold, ADR-104 — implements ADR-078 K0.)** Turned the source tree into a packaged app. The one blocker was runtime data deps: audit found **migrations** (`mfl_desktop/migrations/*.sql`, loaded `__file__`-relative) as the critical one → made `db/schema.py._migrations_dir()` `sys._MEIPASS`-aware (license key is an embedded string; icons already `_MEIPASS`-safe via `resources.py`). New `packaging/`: `mfl.spec` (one spec, both OSes; bundles migrations + `assets/icons` + ofxtools data, per-OS `.icns`/`.ico`, windowed, excludes legacy web stack, macOS `BUNDLE`+Info.plist, entry `mfl_main.py`), `stamp_build_info.py` (writes gitignored `_build_info.py` git-SHA/date → About/diagnostics), `build_macos.sh` (.app→dmg, sign/notarise env-gated) + `build_windows.ps1` (folder+exe, sign/installer env-gated); `requirements-build.txt` (`pyinstaller==6.21.0`); `.github/workflows/build.yml` (macOS+Windows matrix: offscreen smokes → unsigned build → upload). **Verified a real macOS build:** `My Financial Life.app` (159MB) bundles all 31 migrations + 7 icons, starts headless with working logging/build-metadata, and **applies all 31 migrations from the bundle** against a fresh DB. Open (owner accounts, not code): Apple Developer ID + notary profile (K1), Windows code-signing cert (K2), Inno Setup `installer.iss`, auto-update feeds. **Remaining 1.0 gate is non-engineering: signing certs/accounts (K), business/legal (B), website (W), commerce (C), QA matrix (Q).** **Prior session below. New-features session: bonds/options, budget bills + stepped burn-down, loan accounts.** Three owner-requested features for the 1.0 ship, each its own ADR + migration, all verified headless + offscreen Qt and pushed to `master`. **(1) Bonds & options (ADR-093, migration 0029).** `security` gains `instrument_type` + `price_multiplier` (stock 1; bond face/100; option contract_size) + per-class metadata; `txn.accrued_interest` (cash, not basis). The holdings engine (`compute_holdings_view`/`value_history`/`returns`) takes optional `multipliers` + accrued (subtracted from basis on buy, proceeds on sell); 5 callers pass `security_multipliers()`. Investment dialog gains an Instrument combo + per-class fields + accrued + multiplier-aware tri-field solver; Stock Record shows the metadata. Basic options only (long buy→sell/expire; no exercise/assignment). **(2) Budget bills + stepped burn-down (ADR-094, migration 0030).** A bill = a `budget_line` backed by a real `scheduled_txn` (`budget_line.scheduled_txn_id`). `compute_burndown` is bill-aware + stepped: staircase ideal, projection = actual + **unpaid** (amount-matched) bills + discretionary run-rate → a **paid bill flattens**; weekly/twice-monthly bills step per occurrence. `burn_down_chart.py` rebuilt as a filled staircase. New pure `bill_occurrences_in_month`; repo `add_bill_line_from_schedule`/`set_budget_line_schedule`/`list_bill_schedules_for_budget`/`list_schedules_not_in_budget`; UI: setup "Add bills from schedules…" picker, matrix "Make this a bill…", reverse "Add to budget?" ask. Owner forks: pull existing schedules in (adjustable); new bill→full Schedule dialog; reverse asks each time; paid = amount-match. **(3) Loan accounts + amortization (ADR-095, migration 0031).** New `loan` family (`loan_std`, liability) + 1:1 `loan` terms table (CHECK widened via the ADR-032 recipe, preserving `credit_limit`). Pure `loan_calc.py` (`compute_schedule`/`required_payment` rounded-up/`monthly_rate`/`split_payment`/neg-amort). Repo `Loan` CRUD + `create_loan_account` + `loan_schedule` + **`post_loan_payment`** (3 split modes: whole / interest-in-loan / interest-from-payment) + `create_loan_paydown_goal` (reuses ADR-058 R4b for budget integration). UI: `loan_dialog.py` (live preview, budget-goal opt-in); the amortization schedule lives **on the Account Summary** (family dispatch → `LoanScheduleWidget` in `loan_schedule_view.py`: summary + balance chart + table + Record payment; no Top-Payees panel), via double-click / Account ▸ Summary; register **New Loan…** opens it; Net Worth/Home `loan` family maps. Deferred per owner: what-if scenarios + recurring auto-post split. **(4) Public demo regenerated** (`tools/make_public_demo.py` → `mfl_public.mfl`) to showcase the above: Home Mortgage + Car Finance loan accounts **with 12 months of payment history** (the mortgage interest books under Rent / Mortgage on the in-perimeter current account, so the budget housing line still shows the true cost), an Apple bond + an AAPL call option in the US brokerage, two budget bill lines + a mortgage pay-down goal. Net worth now **~£282k / 630 txns** (mortgage-adjusted; the older "~£490k / 590 txns" figure below is superseded). **Prior 2026-06-21 session below (schedules robustness / licensing / demo / macOS polish):** **(1) Auto-post failures surfaced + category-kind guard (ADR-091).** The launch sweep `Repository.auto_post_due` silently swallowed per-schedule post failures, so a transfer-kind schedule with no destination (its category flipped expense→transfer after creation) sat overdue forever. It now returns an `AutoPostResult` (`posted` + `failures`, each carrying a schedule label + reason); `RegisterWindow._run_auto_post_sweep` warns the user listing skipped schedules. `change_category_kind('transfer')` now **refuses** (ValueError, surfaced by the Categories dialog) when any active schedule on the category/descendants lacks a destination — blocking the orphaning at source. **(2) Reopen last-used file on launch (ADR-092).** New `mfl_desktop/app_session.py` persists the most-recently-opened path in **QSettings** (app-level, cross-file — the per-file `setting` table can't be read before a file is open); launch precedence `--db → last-opened → legacy cwd (mfl_dev.mfl/.db) → appdata`. Recorded at launch + on File ▸ Open (`_swap_repository`). **Amended** same day for transient-absence (iCloud/removable) robustness: `last_db_path()` no longer filters on `exists()`; the resolver checks existence itself and, on a remembered file it can't use this launch, opens the default **without clobbering the pointer** (next launch retries once the file is back) and never hands a missing path to `Repository` (which would recreate it). **(3) Offline licensing IMPLEMENTED (ADR-079).** Qt-free verify-only `licensing.py` (Ed25519 over the existing `cryptography` dep; key = `payload_b64.sig_b64`, edition entitles its major + older), `license_service.py` orchestration, `version.py` (`__version__`/`APP_EDITION`), `app_session` key + **30-day first-write-wins trial** in QSettings, About box + Enter-License dialog + Help menu + gentle launch nag/title cue (enforcement is friction not lockout — `LicenseStatus.unlocked` ready to harden), `tools/license_tool.py` offline signer (private key gitignored) + `cli license-check`. Ships a **DEV** public key to swap for production; Merchant-of-Record delivery + `BUY_URL`/website still open (workstream C2/W1). **(4) Public demo file.** `tools/make_public_demo.py` deterministically builds **mfl_public.mfl** (now **versioned** via a `.gitignore` exception; `-wal/-shm` sidecars stay ignored) — a fictional UK persona (Jordan Avery, GBP, 12 months, ~£490k net worth, 590 txns) across current/savings/credit/ISA/USD-brokerage/pension/home/car with realistic payees, multi-currency price history + dividends, transfers, and a populated budget — so screenshots never use real data. **(5) macOS font sizing (ADR-076 amendment).** Forced Fusion + point sizes rendered ~25% small on macOS (72 DPI logical vs Windows' 96): base `*` font is now **13px on macOS** (unchanged `10pt` on Windows/Linux), and `ui/ui_fonts.set_pt(font, pt)` pins the px-equivalent on macOS for the 51 chart/display `setPointSize` calls + inline `9pt/13pt` QSS accents (Windows byte-for-byte unchanged; relative hero-number bumps left as-is). **(6) Home follows File ▸ Open (ADR-075 amendment).** `HomeView` kept its own (old, then-closed) repo after a file swap → stale dashboard until restart; `HomeView.set_repo()` + refresh in `_adopt_repository` fixes it. All committed + pushed to `master` (no branches — owner preference).

---

## Status at a glance

- **v0.1 shipped** as a local web app (FastAPI + HTMX + Oxigraph). MVP complete, owner-only. Now in maintenance mode.
- **2026-06-05 pivot:** MFL is being rebuilt as a **native desktop application** (PySide6 + SQLite) for Windows-first distribution. See [ADR-008](docs/adr/ADR-008-desktop-ui-framework.md), [ADR-009](docs/adr/ADR-009-storage-engine-for-ledger-data.md), and [ADR-010](docs/adr/ADR-010-transactional-schema-design.md).
- **Desktop app under `mfl_desktop/` is the live target.** Multi-account register with an All-transactions cross-account view, OFX/QFX/CSV import working end-to-end through the Qt UI, layered architecture (UI → proxy → model → Repository → SQLite). Owner has loaded six months of real data into it (~1,300 transactions) and confirmed the feel.
- **Basic management round complete (2026-06-05):** new + delete + bulk-edit transaction (Ctrl+E modal with per-field checkboxes); account CRUD (create / edit / delete) with opening balance; payee + category management dialogs with rename / merge / delete (cross-merge/-kind-merge rejected explicitly); category `kind` (income/expense/transfer) with cascade on reparent and direct Change Kind verb; Banktivity-style account folders in the sidebar with balance roll-up; File → Save Copy As… / Open… for `.mfl` snapshots; register search now covers payee/memo/amount/date and is comma-insensitive; category combos in dialogs are searchable typeaheads.
- **Reports round 1 (2026-06-05):** Reports → Spending Over Time (stacked bar by top-level expense category group, granularity weekly/monthly/quarterly/annually, date range, account/category/Uncategorised filters, average line, strict-outflow semantics per ADR-018); Reports → Net Worth (Pocketsmith-style three-column layout, big total + horizontal proportional bar + colour-coded legend / Assets / Debts, grouped by account type with per-account drill-down, +Asset / +Debt buttons opening the existing AccountDialog).
- **Transfers (2026-06-05):** category-driven (ADR-020). No dedicated New Transfer verb — picking a `kind='transfer'` category on any flow (New Transaction, inline cell edit, Bulk Edit) prompts for the destination account and creates a partner row sharing one `transfer_id`. Direction inferred from source amount sign. Delete is partner-aware. Migration 0004.
- **Generic CSV mapping wizard (2026-06-05):** unknown-format CSVs (Pocketsmith, etc.) now open a `CsvMappingDialog` — file-preview at top, mapping form in the middle, live after-mapping preview at the bottom (ADR-021). Smart defaults pre-fill all five fields from the existing alias lists; user just confirms for conventional layouts. No schema change. Known formats still commit silently per the no-dialog-for-known-imports rule. Saved mapping profiles (auto-skip the dialog for repeat imports) are explicitly deferred to a future ADR. Shipped alongside a fix to `_classify_and_stage` so within-batch composite-hash collisions (two CSV rows with the same date/amount/empty-payee) get a deterministic `:N` suffix instead of blowing up the `UNIQUE(account_id, import_hash)` constraint at commit.
- **Register typeahead delegates (2026-06-05):** Payee and Category inline editors are now repository-backed typeaheads matching the dialog flows (ADR-022). `PayeeTypeaheadDelegate` wraps `QLineEdit` + `QCompleter` over `Repository.list_payee_names()`; `CategoryTypeaheadDelegate` reuses the same `make_category_picker` helper as the dialog combos and reads `list_categories_flat()` fresh per editor open (no delegate rebind needed). Typing an unknown category name in the Category cell pops a single Yes/No confirm; on Yes the category is created top-level with `kind='expense'`, `source='user'` (aligned with the import path's default) and committed into the cell. Lightweight `_reload_category_cache()` on the window refreshes the cached choice list + filter combo without resetting the model, so the inline-create path is safe to call from inside a delegate `setModelData`.
- **Scheduled transactions / bills (2026-06-05):** First half of the budget arc (ADR-023). New `scheduled_txn` table (migration 0005) stores templates: cadence (weekly/biweekly/monthly/quarterly/annual), anchor date, next-due date, optional end date, signed estimated amount, `variable` flag (prompt at post), `auto_post` flag. Posting materialises one `txn` (or a transfer pair via `create_transfer` when category kind = transfer) and advances next-due via anchor-based math (Jan 31 monthly → Feb 28 → Mar 31). Manual post by default; launch-time `auto_post_due(today)` sweep catches up auto-posters loop-style (a user gone two months gets every missed occurrence). New dialogs: `ScheduleDialog` (single-record edit, transfer destination revealed when category kind = transfer) and `SchedulesDialog` (Manage → Schedules… — table view + New/Edit/Post Now/Delete). No txn → schedule back-link in v1.
- **Budget core (2026-06-05):** Second half of the budget arc (ADR-024). New `budget` / `budget_account` (M:N perimeter) / `budget_category` (target + cadence + role) tables (migration 0006). One budget per file in v1; schema supports many. Perimeter rule: transfers between two in-perimeter accounts cancel out; transfers crossing the perimeter count. Per-category amount stored positive; direction inferred from `category.kind`. Role on `budget_category` (bills / saving / discretionary). Actuals bucket against the **nearest budgeted ancestor** of each txn's category; un-budgeted-chain txns fall into "Other". Pure-Python `mfl_desktop/budget_calc.py` does pro-rating (`365.25/12` etc., identity for matching cadence on its calendar period) and the four Simplifi tiles (Income after bills & saving / Planned spending / Other spending / Available). **Transfer-kind budget categories are skipped in the tile math** (post-ship amendment to ADR-024). Cash-on-hand badge in the header is the separate reality-check. New screens: `BudgetSetupDialog` (two-tab modal — Accounts + Categories) and `BudgetWindow` (non-modal `QMainWindow` under a new top-level **Budget** menu, Ctrl+B, refreshes on `WindowActivate`). No rollover.
- **Budget visualisations (2026-06-06):** Round C of the budget arc (ADR-025). Closes the four ADR-024 deferrals. **Full-cadence-period subtitle** on non-monthly cards (e.g. £1,800 annual Holidays card now shows "£147.84/mo" + a second line "£1,800 annually · this year: £450 of £1,800"). **Burn-down chart** under the tile strip — QtCharts line chart with Actual cumulative outflow vs. linear Ideal pacing + vertical today marker. **Proportional summary bar** (reusing `ProportionalBar` from ADR-019) showing Bills / Saving / Planned / Other / Available proportions with colour-keyed legend — owner's no-pies rule (ADR-018) preserved. **Scheduled-but-not-posted projection** per card: "+£X expected" badge surfacing un-posted scheduled outflows whose `next_due_date` falls inside the screen period. All computation in `mfl_desktop/budget_calc.py` as optional kwargs to `compute_budget_view`; pure-function contract preserved. New widget `mfl_desktop/ui/burn_down_chart.py`. Cadence-period containment uses the global anchor rule (Monday weeks, calendar quarters/years; bi-weekly = 14-day window ending today pending per-schedule anchors).
- **Budget arc state (2026-06-06):** All three rounds shipped end-to-end, but the screen is **rough** — owner flagged it as needing further polish iterations after real use. Likely targets for the next pass: card-layout density (current vertical stack of header + cash badge + 4 tiles + bar + chart + cards eats ~480px before any card is visible), ~~visual tone of the QtCharts default styling on the burn-down~~ (closed 2026-06-06 by ADR-026 — burn-down is now a hand-rolled paintEvent widget matching the spending chart), labelling clarity on the tile colours, and the "Other" bucket behaviour when the user hasn't set up budget categories yet. Don't treat any specific bit of the screen as final — every part is fair game when polish work resumes.
- **Visual baseline + paintEvent charts (2026-06-06):** ADR-026 settled the "1990's Microsoft" complaint. Fusion style + a custom Tailwind v3 slate / `blue-600` QPalette + a minimal QSS layer are applied once from `__main__` via `mfl_desktop/ui/theme.py::apply_theme(app)`. Both charts (Spending Over Time, budget burn-down) hand-rolled as `paintEvent` `QWidget`s after a side-by-side comparison vs. QtCharts and pyqtgraph — owner picked paintEvent for the modern flat look (rounded top corners, soft gridlines, pill-shaped dashed average label, hover tooltip) plus full typography control plus zero new dependency. Shared chart primitives in `mfl_desktop/ui/chart_helpers.py` (palette, `nice_ticks`, `fmt_currency`, `legend_chip`); spending and burn-down both consume them. pyqtgraph removed from requirements; QtCharts no longer imported by either chart widget. Memory: future chart shapes default to paintEvent ([[feedback-chart-engine-preference]]).
- **Period presets revised + Custom range (2026-06-06):** ADR-033 amendment after the polish round shipped. New `PERIOD_KEYS` are `("quarter", "6m", "ytd", "1y", "3y", "custom")` with labels Last Quarter / Last 6 months / Year to date / Last 12 months / Last 3 years / Custom — finance-native vocabulary matching the Banktivity inspiration. "Last Quarter" is rolling 90 days (consistent with the other rolling presets). The old "all" preset is retired in favour of explicit Custom. New `mfl_desktop/ui/custom_period_dialog.py` is a small modal with From/To `QDateEdit` calendar pickers, From ≤ To validation, both clamped to today; defaults seeded from the previously-displayed range so Custom is "edit where you are". Shared helpers `fmt_date_range` and `period_display_label` in `mfl_desktop/account_summary.py` render "Custom: 1 Jun → 6 Jun" (same year) or "30 Dec 2025 → 6 Jun 2026" (otherwise). `TxnListFilter` carries `custom_start` / `custom_end` so a drill-down opened from a Custom-mode summary inherits the same range; the filter's `signature()` includes the bounds so distinct custom ranges open as distinct windows. Drill-down period chip is now non-removable (period is always set; the user changes it via the button row, not by removing it). Default period stays a rolling 90 days under the new "quarter" key, so the chart looks identical on first open.
- **Per-account summary polish — dual-axis chart, section cards, Top-N drill-down (2026-06-06):** ADR-034 closes three pieces of real-use feedback after ADR-033 shipped, in one polish round. **Dual y-axis** on `BalanceFlowChart` — independent left (bars) and right (balance line) scales, each with its own `nice_ticks`; right-axis labels coloured blue-600 to match the line. Bars own the zero baseline; the line doesn't. Closes the credit-card-paydown case where the balance line was dwarfing the bars. **Section cards** around the four panels (chart, info, top-payees, top-categories): `QFrame` with rounded 10px border + 1px slate-200 outline + white background on a slate-50 window canvas. Splitter handles widened to 12px so the cards have a visible gutter. **Top-N drill-down** — clicking "Eating out" or "Tesco" in a Top-N panel opens a new `TransactionsListWindow` (`mfl_desktop/ui/transactions_list_window.py`) showing the underlying transactions as an editable register view with a breadcrumb chip strip (Account / Period / Category / Payee, each removable via ×) and the same six-preset period selector as the summary. New `DrillDownFilterProxy` subclasses `TransactionFilterProxy` with date-range + payee-id + category-descendants filters. New `TxnListFilter` frozen dataclass + `for_payee` / `for_category` factory constructors carry the spec. Category filter is "this and descendants" (uses `Repository.category_descendants`). Same inline delegates as the main register so edits propagate through the Repository. **`TopNRow` gains an `entity_id`** so the drill-down can filter on ids rather than labels — `_top_n_by_id` aggregates by id with a synthetic `None` bucket for `(No payee)` (non-clickable in v1). **`_TopNList` becomes interactive**: hover tints the row slate-100, cursor switches to PointingHand on clickable rows, press emits `row_clicked(TopNRow)`. **Window policy**: one drill-down per distinct `(account_id, period_key, category_id, payee_id)` signature — same row clicked twice raises the existing window, different row spawns a new one (owner picked this over replace-in-place via `AskUserQuestion`). Drill-downs parented to the summary so closing the summary closes them all. Closes the dual-axis, section-borders, and drill-down follow-ups originally flagged on ADR-033.
- **Per-account summary screen (2026-06-06):** ADR-033 lands the per-account focus view — the missing other-axis next to the cross-account register, the budget window, and Net Worth. New non-modal `QMainWindow` (`mfl_desktop/ui/account_summary_window.py`) opened from sidebar context menu (Account Summary… first item), sidebar double-click on an account row, and **Account → Summary…** (Ctrl+I). Single-instance per account — `RegisterWindow._account_summary_wins: dict[int, AccountSummaryWindow]` keyed on `account.id`; repeat opens raise the existing window. Top of screen: a combo chart — income bars above the zero baseline, spending bars below, balance polyline overlaid in blue-600 — driven by the owner's framing ("would be good for current accounts and also quite a nice view for a credit card to see the balance coming down if you're trying to pay off a debt"). Period selector below: Last 30 days / Last 90 days / YTD / Last 12 months / Last 5 years / All time, default 90d, granularity auto-picked (daily ≤45d, weekly ≤120d, monthly ≤800d, quarterly ≤2200d, yearly beyond). Right column: Summary (recorded balance + scheduled-count line), Additional Info (Uncleared `(N)` count + amount in red, Cleared Balance), Upcoming (next 5 schedules touching the focus account within 30 days — picks up either `account_id` match or transfer-destination match, flipping sign for transfer-in), plus a placeholder "NO STATEMENTS · RECONCILE ›" row that opens an info dialog (handler swap when the reconciliation arc lands; layout untouched). Bottom row: TOP PAYEES + TOP CATEGORIES — strict-outflow lists with proportional bar fills, period-scoped. Non-cash families (investment / property / vehicle) render an amber banner above the chart: "Balance reflects recorded transactions; valuations not yet wired." until the valuation pipeline arrives. Chart, info panel, top-payees, and top-categories sit in nested `QSplitter`s so any of the four panes can be resized. New paintEvent widget `mfl_desktop/ui/balance_flow_chart.py` reusing `chart_helpers`; new pure-Python `mfl_desktop/account_summary.py` mirroring `budget_calc.py` (compute_balance_flow_series / compute_period_summary / compute_status_breakdown / top_payees / top_categories / upcoming_scheduled). No schema change. Refreshes on `WindowActivate`. Smoke-tested with synthetic data — period math balances (opening + inflows − outflows = closing), cleared + uncleared = recorded, strict-outflow proportions sum to 100%.
- **Create Schedule From Transaction (2026-06-06):** ADR-027 adds a right-click verb on the register that seeds the `ScheduleDialog` from a transaction (account / payee / category / amount / memo / transfer-destination if applicable). The dialog field renamed to **"Next occurrence:"** in both seeded and from-scratch flows. The handler steps `txn.posted_date` forward by the default cadence (`monthly`) until the result is strictly after today, passes that as `seed.anchor_date`; on save anchor = next_due = that future date. New frozen `ScheduleSeed` dataclass + `seed=` kwarg on `ScheduleDialog`; new `Repository.get_transfer_partner_account_id`. No schema change.
- **Payee aliases round 1 (2026-06-06):** First round of the three-round payee arc planned in ADR-028. Migration 0007 adds `payee.canonical_id` (self-ref, `ON DELETE SET NULL`) + partial index. `Repository` gains `set_alias_of` / `promote_to_canonical` / `list_canonical_payees` / `list_aliases_of`; `list_payee_names` filtered to canonicals only (typeahead + bulk-edit completer now suggest preferred labels only); `list_payees_with_usage` rewritten with rolled-up counts on canonicals; `merge_payees` patched to re-point sources' aliases onto the target before deletion. `PayeeRow` extended (`canonical_id`, `canonical_name`, `direct_usage_count`). Payees dialog gets 3 columns (Name / Alias of / Used in), indented aliases, and two new verbs (**Make &Alias of…**, **&Promote to Canonical**). `ADR-012` amended with the canonical/alias model and the three-shape verb table (Merge / Alias / Delete). **Round 2** (import-time alias lookup) and **round 3** (rules engine) deferred per ADR-028 plan. Round 1 deliberately doesn't roll up the register's per-row display to the canonical — round 2 sets `txn.payee_id` to the canonical at import time, which avoids a silent rewrite-on-no-change pitfall in the inline typeahead.
- **Multi-currency foundation (2026-06-06):** ADR-035 lays the schema + data layer for true multi-currency support. Migration 0009 adds three tables — `setting` (flat key/value store for the openexchangerates API key, last-refresh timestamp, and matcher tunables), `fx_rate` (date/base/quote/rate/source with daily granularity), and `transfer` (parent row per pair, keyed on the existing `txn.transfer_id` IRI, carrying the exchange rate that was used at posting time + its provenance: `derived` / `manual` / `fx_rate`). Existing same-currency transfers are back-filled at rate=1.0. `Repository.get_fx_rate_nearest` walks a six-step lookup chain: exact bilateral → exact inverse → exact USD-pivot → nearest-prior bilateral → nearest-prior inverse → nearest-prior USD-pivot, so any `(date, base, quote)` ask resolves cleanly off a USD-base provider feed plus arbitrary bilateral manual entries. `Repository.convert_amount` is the single conversion path; same-currency early-exits cheaply. **Transfer plumbing is now currency-aware end-to-end** — `create_transfer`, `convert_to_transfer`, and `post_scheduled_txn` each take optional `to_amount` / `rate` / `rate_source` kwargs (two-of-three rule: any one resolves the others, missing both triggers an FX-table lookup with nearest-prior fallback). The destination row's stored amount is whatever really hit that account's statement; the parent `transfer.rate` row is the truth-of-intent. **txn does NOT gain a currency column** — a txn's currency is its account's currency, single source of truth. `account.currency` and `person.base_currency` already existed in 0001 so no ALTER was needed on existing tables.
- **openexchangerates.org integration + Currencies dialog (2026-06-06):** new `mfl_desktop/fx.py` is a pure-Python urllib client (no Qt imports — CLI-compatible). Endpoints used: `/latest.json` and `/historical/<date>.json`, both with the free-tier USD-base constraint. `refresh_latest_into(repo, force=False)` skips when last refresh < 24h ago (configurable via `LAUNCH_REFRESH_INTERVAL_HOURS`) and when no non-USD accounts exist — single-currency users never burn API budget. **Background launch refresh** wired in `__main__.py` via `QThreadPool.globalInstance().start(_FxRefreshRunnable(args.db))` with its own Repository connection (sqlite3 cross-thread safety). New **Manage → Currencies…** dialog (`mfl_desktop/ui/currencies_dialog.py`) holds the API key field (with "stored inside this file" disclaimer per ADR-035), Refresh Now button (synchronous, wait-cursor), latest-rates table (one row per `(base, quote)` pair we've stored, showing the most recent date+rate), "Add manual:" row at the bottom for ad-hoc rate entry (covers the OXR-not-set case and weekend gaps), and the two matcher tunables from ADR-036 (`transfer_match_window_days` default 3, `transfer_fx_tolerance_pct` default 1.0). API key persists to `setting.oxr_api_key` on Save; tunables persist to their own setting keys. `backfill_historical()` exists in `fx.py` for explicit date-range backfills but isn't wired into the dialog yet.
- **Transfer matching (2026-06-06):** ADR-036 replaces ADR-020's always-create-partner behaviour with a matcher that finds an existing other-side row first. `Repository.find_transfer_candidates` filters by opposite sign, `transfer_id IS NULL`, posted_date within `±transfer_match_window_days`, and (for cross-currency) amount within `transfer_fx_tolerance_pct × 5` of `source_magnitude × spot_rate`. Returns scored candidates. `Repository.link_transfer` writes a fresh `transfer_id` IRI on both rows, **rewrites both rows' `category_id`** to the source's transfer category (the rewrite is the whole point per ADR-036 §what-about-the-source-row), and inserts a `transfer` parent row with rate back-derived from the two amounts. `mfl_desktop/transfer_reconcile.py` holds the pure-Python `score_candidate` (weights: 100 base, −5×|days_apart|, −50×amount_mismatch_pct/100, −20 if currencies differ, +10 on payee-token overlap; stopwords filter out "transfer/to/from/payment" etc.) and the `greedy_pair` bipartite helper. Strength bins: ≥80 Strong (blue), ≥60 Good (amber), else Possible (slate). **UI:** new `transfer_match_dialogs.py` holds `TransferMatchConfirmDialog` (one candidate; [Match] / [Create new partner] / [Cancel] verbs) and `TransferMatchPickerDialog` (many candidates; ranked table with strength chips + trailing "Create new" sentinel row). Both wired into `register_window._on_model_data_changed` via the new `_offer_transfer_match` helper — the inline-edit flow now routes through them before falling back to `convert_to_transfer`. Matcher errors (e.g. missing FX rate) fail soft: status-bar message + fallback to create-new. **Bulk-edit dispatcher** in `_on_bulk_edit` was rewritten in two phases: phase 1 (`bulk_update_transactions`) applies category + payee/status/memo; phase 2 runs the matcher per row, opens a `BulkTransferReviewDialog` (summary line + per-row decision cell — fixed label for unambiguous rows, inline combo box for the rows with multiple candidates, top-score pre-selected), and writes via `bulk_match_or_create_transfers` (single SQL transaction). New `BulkRowAnalysis` dataclass + `LinkExisting` / `CreateNew` decision types are the Repository's `BulkTransferDecision` union.
- **Cross-currency entry collects partner amount (2026-06-07):** ADR-035 amendment closes the gap between ADR-035 §UI and the as-built behaviour. New `mfl_desktop/ui/transfer_destination_dialog.py` (`TransferDestinationDialog`) surfaces a single combined picker: account combo at the top, cross-currency block (revealed only when the chosen other-account differs in currency) with editable amount field + live "Implied rate: 1 USD = 0.7900 GBP" line + hint about pre-fill source. Pre-fill comes from `Repository.get_fx_rate_nearest`; when no rate is on file the field is blank and the user types the magnitude that actually hit the destination. Returns `TransferDestinationChoice(account_id, other_amount)` — sign-neutral, framed as "this account / other account." Wired into the **New Transaction** transfer flow (translates `other_amount` to `amount` / `to_amount` per direction), the **inline category-edit** flow (passes `to_amount` to `convert_to_transfer`; the matcher still runs first per ADR-036), the **bulk-edit transfer review** (new "Dest amount" column with per-row FX pre-fill — `BulkRowAnalysis` gains `dest_currency` + `fx_prefill_amount`, the dialog only renders the extra column when any row crosses currencies), and **Post Now on cross-currency transfer schedules** (via a new `locked_account_id` kwarg that pins the combo to `transfer_to_account_id`). `Repository.post_scheduled_txn` gains a `to_amount` kwarg + a `derived`-rate branch that bypasses the FX-table lookup when supplied; `auto_post_due` is unchanged (no user prompt path). All four user-facing entry surfaces now stamp `rate_source='derived'` when the user types a partner amount, preserving ADR-035's truth-of-intent contract (`fx_rate` source reserved for stored-rate lookups, `manual` reserved for user-typed rates which the txn-entry UI still doesn't expose).
- **Saved reports round 1 (2026-06-07):** ADR-039 (Accepted, round 1 shipped). Migration 0010 adds `report` (id / iri / name / type enum / folder_id / filters_json / archived_at) and `report_folder` (id / iri / name / sort_order / archived_at) tables. Type enum is `spending_over_time` / `net_worth` / `income_expense` / `sankey`; round 1 only persists Spending Over Time. `mfl_desktop/reports/` is now a **package** (`__init__.py` re-exports the old category helpers; `filters.py` holds `SpendingOverTimeFilters` frozen dataclass + `to_json` / `from_json` / `default` + `filters_from_json` dispatch + `migrate_filters` helper for in-place JSON-shape upgrades). Repository gains `ReportRow` / `ReportFolderRow` dataclasses + `list_reports` / `get_report` / `create_report` / `update_report` (uses the class-level `self._UNSET` sentinel — see pitfalls) / `delete_report` plus the four report-folder methods mirroring account-folder; `set_report_folder` for moves; `expand_canonical_payee_ids` so the payee filter expands a canonical selection to include its alias txns (bridges the ADR-029 round-1 gap); `spending_aggregates` gained a `payee_ids` kwarg. **Sidebar restructured** — `AccountSidebar` renamed to `Sidebar` (alias kept for back-compat), grows two top-level group rows (ACCOUNTS / REPORTS) with the lower header carrying a small leading gap as the visual separator. New `selection_changed(kind, payload)` signal carries `"all_transactions" | "account" | "report"`. Section header items + report-folder + report rows column-span both columns so names without a balance use the full width; the 540px sidebar max-width cap removed (splitter handle is now the only constraint). Right-click context menu split per section: Reports section gets New Report… / New Folder…; report folders get rename / move up-down / delete / new-folder-and-assign; reports get Open / Move to Folder / Delete. New `Sidebar.section_at_y(y)` helper so right-clicking in REPORTS empty space hits the Reports verbs rather than defaulting to the Accounts menu. **Spending Over Time window rewritten** around a top bar (Back / name / Filter… / Save / Save As…), the chart on the left, and a right-side summary panel (period / granularity / rollup / filter chips / total / average / scrollable vertical category legend rebuilt per-render using `legend_chip` + `colour_for` so chips line up with bar segment colours). Bare windows hide the standalone Save As… button (primary button reads "Save As…" already). Filter dialog (`spending_filter_dialog.py`) houses every dimension — period preset + Custom range picker, granularity auto/weekly/monthly/quarterly/annually, rollup top/group/leaf, accounts + categories + payees as `CheckListPanel` instances (search box + Select all / Deselect all + checkable list). **Click-to-drill-down**: `SpendingChart.segment_clicked(group_id, bucket)` emitted on left-click of a stack segment; window pushes current filters onto a stack, narrows `category_ids` to the clicked group's descendants, descends rollup one notch (top→group→leaf). Back button pops the stack and restores the prior snapshot. Drill state is view-only — `Save` persists `self._drill_stack[0]` if the stack is non-empty, otherwise `_current_filters`. New dialogs: `NewReportDialog` (type picker; Net Worth / Income & Expense / Sankey rows render as "coming later" and aren't selectable until their per-type rounds land) and `SaveReportAsDialog` (name + folder combo with inline "New folder…" verb). Two-singleton policy on report windows: one bare-window per *type* (Reports menu repeat clicks raise existing); one saved-window per *report id* (sidebar repeat clicks raise existing). **Status enforced via type CHECK** at the SQL level + filter dataclass dispatch so adding the Net Worth enum value without a filter dataclass crashes the sidebar build (intentional — forces the next round to land both).
- **Register polish bundled into the saved-reports commit (2026-06-07):** **Category-filter combo scoped to in-view categories** — new `Repository.distinct_category_ids_for_account(account_id)` (None = all accounts); `_populate_category_combo` is called from `_show_account` / `_show_all_transactions` so the combo lists only categories that have at least one txn in the current view. When the previously-selected filter id isn't visible in the new view, the combo silently reverts to All *and* the proxy filter is cleared in lockstep (blockSignals(True) eats the currentIndexChanged so we have to push the None through by hand). **Status-bar shows net of visible rows** — alongside the count, single-currency views show `Net: ±£X.XX` derived by iterating proxy visible rows and summing the signed amount; All-transactions view skips the net (mixed currency would mislead). **Editor-swap guard** — `_set_model` now closes any open inline editor first (via `QApplication.focusWidget()` + `self._table.closeEditor(focused, NoHint)`) so it doesn't try to commit against the about-to-be-replaced view/model. Fixes the Qt warning `commitData called with an editor that does not belong to this view` that the owner hit after deleting a large account while a cell was being edited.
- **Bulk transfer reconcile (2026-06-06):** ADR-037 ships **Manage → Reconcile Transfers… (Ctrl+Shift+R)** for housekeeping after importing both sides of many transfer pairs separately. `Repository.find_transfer_pairs(account_a_id, account_b_id, window_days, fx_tolerance_pct)` builds the cross-product of A's unmatched rows × B's unmatched rows, scores via the shared `score_candidate`, then runs `greedy_pair` (highest-score-first; each source/target row claimed at most once; ties broken by score desc then id asc for stable ordering). Returns ranked `TransferPair` rows (Strong → Good → Possible). New `mfl_desktop/ui/transfer_reconcile_dialog.py` has account A + B combos at top, a read-only tunable summary line ("Window: ±3 days · FX tolerance: ±1.0% (change in Manage ▸ Currencies)"), and a checkbox table of proposed pairs with strength chip + side-by-side view (A date/amount/payee | B date/amount/payee). Cross-currency rows surface a tooltip on the payee cells: "Implied 1.2739 · Spot 1.2700 · Δ +0.31%". Three shortcut buttons — Check all Strong / Check all Strong + Good / Uncheck all. Transfer-only category picker via the existing `make_category_picker`, defaulting to the seeded Transfer root. Apply writes through `bulk_match_or_create_transfers` and re-runs the search so matched rows drop out — the user can do a second pass with different settings without re-opening. Two-accounts-at-a-time is deliberate per ADR-037 — a future N-account wildcard mode is on the backlog.
- **Budget redesign underway (2026-06-12):** ADR-058 (umbrella) **supersedes ADR-024 + ADR-025**. After real use of the always-"rough" original, the owner re-stated 12 principles → a genuine hybrid of **envelope + zero-sum + long-term planning**. The load-bearing change: the single amortized `budget_category.amount + cadence` is replaced by **explicit per-month `budget_allocation` rows** (matrix is the native editable object; copy-forward affordance on edit — just this month / this + later / all 12). Also: **multiple budgets + duplicate-as-scenario**, a **budget period** (default Jan–Dec), **historical-average suggestions**, **roll-up budgeting that still tracks children** (nearest-ancestor bucketing carries forward), **soft Unallocated indicator** vs the perimeter-account pool (reddens, never blocks), **auto-rollover** per line (default on for expenses, `carry_in` computed not stored, reallocate by editing cells), a **12-month editable matrix** (Budget/Actual/Diff by income/expense/transfer + per-section Unbudgeted rows), a later **monthly view**, and a **projected burn-down** (extends the actual spend rate forward, unlike Pocketsmith's stop-at-today). Migration 0019 adds `budget_line` + `budget_allocation`, migrates + drops `budget_category`. `budget_calc` rewritten around `compute_matrix` but stays pure. **Phased: R1 foundation + editable matrix ✅ shipped → R2 per-line rollover/role controls ✅ shipped → R3 monthly view + projected burn-down ✅ shipped → R4a credit cards as a pool source ✅ shipped → R4b pay-down goals (next).** The old `budget_window.py` / `budget_setup_dialog.py` / `burn_down_chart.py` / `budget_calc.py` are the rebuild targets; ADR-024/025 status bullets above describe the superseded design.
- **Budget annual-matrix polish — totals, Net line, collapsible sections (2026-06-12):** Cosmetic round on the Annual view (`BudgetMatrixModel` in `budget_window.py`) from first real use. **Far-right Total column** = each row's year sum (Budget/Actual/Diff), non-editable, slate-50 tint. **Bottom Net line** — `Net (Income − Expenses)` header + Budget/Actual/Diff rows where each month = Σincome − Σexpense − Σtransfer (a raw column sum across opposite-intent sections would be nonsense, so the bottom line is the *net*; Diff = actual-net − budget-net, positive = better). **Collapsible sections** — single-click an Income/Expenses/Transfers header to expand/collapse (▾/▸ chevron); collapse state persists across the activate-refresh. **Section header row shows the section's Budget total per month + Total** (was blank — the headline plan, and the only totals still visible once the section is collapsed; mirrors the bottom Budget subtotal. Net header stays blank since its own Budget row is adjacent). View-layer only (model computes from the existing `BudgetMatrix`); guards added so the Total column and Net row don't drill or edit. Verified offscreen.
- **Budget R4a — credit cards as a pool source (2026-06-12):** ADR-058 R4a. **Migration 0020** adds `account.credit_limit` (pence, nullable) + `budget_account.contribution` (`balance`/`available_credit`/`excluded`, default `balance` → existing budgets unchanged). Membership in `budget_account` still = the perimeter for *actuals*; the new column governs only the *pool*. `compute_perimeter_pool` rewritten to sum each account's contribution — balance (prior behaviour), available_credit = `credit_limit + balance` (signed; £5k card owing £1.8k → +£3.2k), or excluded — and returns reason-tagged exclusions (no FX rate / no limit set) for the banner. Repo: `AccountSummary.credit_limit`; `create/update_account` credit-limit kwargs (`update_account` via the class-level `_UNSET` sentinel **moved to the top of the class** so it's in scope for default args — omit = leave limit untouched, None = clear); `list_budget_account_contributions`; `set_budget_accounts(accounts=[(id,mode)])`; `duplicate_budget` copies contribution. UI: **AccountDialog** Credit-limit row shown only for credit family (wired through register + net-worth create/edit); **Budget Setup ▸ Accounts** now a table with a per-account contribution combo (available-credit offered only for cards); budget-header exclusion banner reworded. **View-layer pool only — no `compute_matrix`/`budget_calc` change.** Verified offscreen against a replayed-migration dev-DB copy. **R4b shipped (see next bullet); R4c (savings goals) is the remaining round.**
- **Budget R4c — multi-account, proportional savings goals (2026-06-13):** ADR-058 R4c — the budget redesign arc's final round. Scoped as a "UI pass" (R4b's `goal_calc`/dialog/card were already direction-agnostic) until owner feedback **broke the R4b one-account-per-goal model**: a goal can span **many accounts** (Retirement = savings + investment + pension), **one account can be split across goals** (some % of a savings account → Camper van, rest → Retirement), goals get **their own name** + **independent horizons**, and contributing accounts may be **multi-currency**. Owner forks (`AskUserQuestion`): **percentage-per-link** (not earmarked amounts); **pension = Investment account** (no new type). **Migration 0022** recreates `budget_goal` (drops `account_id`/`start_amount`/`start_date`/the `UNIQUE`; adds `name` + reporting `currency`; `target_amount` signed pence in that ccy) + adds M:N **`budget_goal_account`** (`share_bp` basis points 0..10000 = % of the account's balance; `baseline_balance` = the account's **full native** balance at link creation, share applied at compute time so a share change never re-baselines; `start_date`; `UNIQUE(goal_id,account_id)`). Existing R4b goals migrate losslessly → a named single-link (100%) goal in their account's ccy (verified on a live-DB snapshot). **FX + roll-up live in the Repository** (`compute_goal_aggregates`: per goal, `start`/`current` = Σ(balance×share) converted to goal ccy via `convert_amount`, ADR-055 *exclude-no-rate* — never par-add), mirroring `compute_perimeter_pool`→matrix; **`goal_calc` stays pure** (`compute_goal_progress(goal,*,start_amount,current_amount,today,rate_missing)` → goal-level `GoalProgress` with `name`/`account_count`/`rate_missing`; dropped `compute_goals`). Repo CRUD reworked M:N: `add_budget_goal(accounts=[(id,share_bp)])` captures each link baseline; `update_budget_goal` reconciles links (kept = reweight only, added = baseline now, removed = delete); `duplicate_budget` copies goals+links. `GoalDialog` gains a name, a **Savings/Pay-down kind toggle** (refilters candidates: savings→cash+investment families, paydown→liabilities; goal accounts are **not perimeter-restricted** — savings/investment/pension vehicles you fund sit outside the spending perimeter), a currency selector, and an **accounts table** (per-row account+share%, add/remove, soft >100%-across-all-goals warning from a per-account share-usage map); `_GoalCard` shows goal name + "N accounts · CCY" subtitle (+ `*`/tooltip when an account couldn't convert). Matrix Goals section (R4b) unchanged — window feeds it the goal name + share-weighted, FX-converted contributions as Actual. **Two fixes from first real use (2026-06-14):** (1) a **savings** goal measures progress from **zero** (existing balance counts; was reading 0% because R4b measured from the creation baseline); **pay-down** still measures from the captured debt baseline (clearing runs 0→100%, new charges reduce it); only %/`is_met` depend on it. (2) goal aggregation now uses **market value** (`compute_account_values` — cash + holdings, ADR-044) instead of cash balance, so a 401(k)/brokerage with £0 cash but six-figure holdings counts toward a savings goal (a 401(k) worth $1M+ was contributing nothing). `add/update_budget_goal` also capture the baseline at market value. Verified headless (proportional split reconciles, multi-currency GBP+USD@manual-rate, no-rate exclusion, metrics vs closed-form, matrix planned/actual + Unallocated drop, single-liability paydown regression) + offscreen Qt (dialog kind-toggle refilter/add-remove/accessors/share-usage + card).
- **Budget R4b — pay-down goals (2026-06-13):** ADR-058 R4b. A dedicated goal object: pick a card/debt account + target balance + target date → the app derives the **required monthly payment** + **balance-based progress**. **Migration 0021** adds `budget_goal` (`budget_id`+`account_id`, `UNIQUE(budget_id, account_id)`; `kind` CHECK `paydown`/`savings`; signed-pence `target_amount`+`start_amount`; `target_date`+`start_date`). Owner forks (`AskUserQuestion`): goals belong to a **budget** (`duplicate_budget` branches them, fresh IRIs); a **Goals strip visible in both Annual+Monthly views**; **balance-based progress** measured from the baseline captured at creation (`start_amount`) so new charges reduce it. Pure **`mfl_desktop/goal_calc.py`** (`compute_goal_progress`/`compute_goals` → `GoalProgress`) on signed balances — same formula serves R4c (`progress=(current−start)/(target−start)` clamped; `required=work-left/max(1,months-left)`; `is_overdue`); imports `BudgetGoal` from repo, repo never imports it (mirrors `budget_calc`). Repo: `BudgetGoal` + `list/add/update/delete_budget_goal` (`add` captures live balance as baseline; `update` = target+date only); `duplicate_budget` copies goals. UI: `_GoalCard` (paintEvent bar) per goal + **＋ Goal…** in a goals strip on `budget_window.py` above the view stack; `GoalDialog` (`goal_dialog.py`, `parent=None`) types target as a positive "still owing" magnitude → stored signed, picker offers only perimeter liabilities without a goal, edit adds Delete. Verified offscreen: baseline capture, 0/33/100% progress + required/mo, new-charges clamp to 0, overdue whole-remainder, edit/delete, duplicate carries goal, savings-direction, window strip + eligibility + dialog framing. **Amendment (2026-06-13, real-use feedback): goals now also appear in the matrix as a zero-sum allocation.** `compute_matrix` gains a **Goals section** (`kind='goals'`, new `bc.GoalPlan` input): Budget = required/mo spread current→target month (whole remainder if overdue), Actual = payments made (`Repository.account_inflows_by_month`, positive inflows; purchases excluded). Owner forks (`AskUserQuestion`): planned **counts toward `assigned`** so **Unallocated drops by it** (debt pay-down = money given a job); shows real Budget/Actual/Diff (favourable like income). Goals are **excluded from the Income−Expenses−Transfers Net line** (transfer-like) and **non-editable** (computed; edit via the goal dialog); monthly view skips the Goals section (strip already shows it). Verified: £283.33 planned vs £300 paid on a £1,700/6mo goal, Unallocated drop = planned, non-editable, Net unchanged.
- **Budget R3 — monthly view + projected burn-down (2026-06-12):** ADR-058 R3. Two deliverables, both reading R1's `BudgetMatrix` (single source of budget truth) — **no migration, no schema change**. **(1) Projected burn-down:** new pure `budget_calc.compute_burndown() -> BurnDownData` for one month + one scope — `actual` (cumulative outflow to today), `ideal` (linear pacing to planned), and the **forward `proj`** that extends the observed daily rate from today to month-end so an overspending scope crosses its budget early (principle 12, "better than Pocketsmith"). Whole-budget scope = all expense-kind outflows; single-category = nearest-budgeted-ancestor (reconciles with the matrix Actual). `mfl_desktop/ui/burn_down_chart.py` rebuilt as a paintEvent widget (R1 had removed the ADR-025 one): Actual (solid red) / Ideal (grey dashed) / Projected (amber dashed) + horizontal Budget reference + Today marker; y-axis includes the projection peak. **(2) Monthly view** (`mfl_desktop/ui/budget_monthly_view.py`, principle 11): one focused month as **per-envelope progress bars** (owner's pick) — ◀/month/▶ selector (default today, clamped), soft Unallocated for that month, burn-down block with a **Whole-budget + per-expense-line scope combo**, then a scrollable Income/Expenses/Transfers list of `spent / available` bars (green→amber≥85%→red over), ↻ glyph on rolling lines, muted Unbudgeted row, bold subtotals. Editable (click amount → `getDouble` → copy-forward `edit_cb`) and double-click bar → R1 drill. `budget_window.py` gains a **View: Annual/Monthly** toggle over a `QStackedWidget`; `_render_inner` feeds the one matrix to both pages so they never diverge. Owner deferred a per-envelope mini-chart; the planned inline child-row expansion remains on the R1 drill-down (R2 decision). Verified offscreen against the live dev DB.
- **Budget R2 — per-line rollover + role controls (2026-06-12):** ADR-058 R2. Surfaces the per-line `rollover` policy on the matrix itself (was Setup-dialog-only). A **↻ glyph** in a line's label marks `rollover='accumulate'` (tooltip states the policy); **right-click a budget line** → context menu: *Rolls over unspent* toggle, *Role* submenu (bills/saving/discretionary, hidden for income), *Remove from budget* (confirm → `delete_budget_line`). All route through the existing `Repository.update_budget_line` / `delete_budget_line` + re-render — **view-layer only, no migration, no `compute_matrix` change** (carry math + child roll-up bucketing already shipped in R1). **Scope decision:** the planned inline expandable child-tracking sub-rows were *dropped* — owner kept the per-child breakdown on the existing double-click-Actual drill-down rather than add a second tree-style view. Changes confined to `budget_window.py`.
- **Automatic snapshots + clean-close checkpoint (2026-06-12):** ADR-057. The live `.mfl` already auto-commits (ADR-016) and is the live file (ADR-050 Tier-2), so durability was solved — ADR-057 adds the two missing safety pieces. `RegisterWindow.closeEvent` now `Repository.checkpoint()`s (`PRAGMA wal_checkpoint(TRUNCATE)`) + `close()`s so the single `.mfl` is self-contained on quit (was leaving committed writes in the `-wal` sidecar — a bare-file copy lost them). Plus **rotating timestamped backups** via new pure `mfl_desktop/snapshots.py` (`save_copy` into a `Snapshots/` folder beside the live DB), taken on launch + a 30-min in-session `QTimer` + clean close, keeping the newest `SNAPSHOT_KEEP=10`. `maybe_snapshot` is best-effort and **change-detects via `max(mtime(.mfl), mtime(-wal))` vs the newest snapshot** (the WAL mtime is the real signal — the main file's only advances on checkpoint), so the three triggers never churn byte-identical copies. Restore = File ▸ Open a snapshot (they're ordinary schema-upgradeable DBs). `_swap_repository` gives the file being *left* the same checkpoint+snapshot. **Retention is now the configurable GFS policy of [[ADR-060]] — the original flat `SNAPSHOT_KEEP=10` is gone.**
- **Data Library — visual save/load of whole datasets (2026-06-13):** ADR-059. A test-data management screen so the two blind file-pickers (`Save Copy As…` / `Open…`) aren't the only way to juggle fixtures. New `Library/` folder beside the live db (parallel to ADR-057's `Snapshots/`). **File ▸ Manage Data… (`Ctrl+Shift+D`)** opens a modal listing named saves + the auto-snapshots in two tabs, each with Load / Save current as… / Rename / Delete. Two owner forks (`AskUserQuestion`): list **named saves + snapshots**; **Load = fresh working copy** — clones the dataset *onto* the live working file (read-only source via `file:…?mode=ro`, temp-file + atomic `os.replace`) so the **saved original stays pristine** (load a baseline → mess it up → reload clean; the library copy never changes). Decisive contrast with `File ▸ Open`, which makes the picked file itself live. Qt-free `mfl_desktop/data_library.py` (list_saved / list_snapshots / library_path / sanitize_name / clone_database / rename_saved / delete_file); new `mfl_desktop/ui/data_library_dialog.py` (emits `load_requested`, closes — only the window can replace the live file). `register_window._swap_repository` split into `_adopt_repository` (bring up) + `_teardown_repository` (snapshot+checkpoint+close); new `_load_dataset` tears down the bench first, clones onto it, reopens; loaded dataset name shown in the title in place of the bench filename. No schema change, no new dependency. Fulfils ADR-057's deferred "Restore from snapshot…". Verified offscreen on a real Repository (save→library, load discards edits to baseline, editing the loaded copy leaves the saved original pristine, failed clone leaves bench byte-identical, dialog build/signal smoke).
- **Configurable GFS snapshot retention (2026-06-13):** ADR-060 (amends ADR-057). Replaces the flat `SNAPSHOT_KEEP=10` (which a busy day exhausts in hours — losing every restore point older than this morning) with a **grandfather-father-son** policy: keep *all* snapshots within `subdaily_hours`, then *one per day* to `daily_days`, then *one per month* to `monthly_months`, dropping older (defaults 24h/7d/12mo). Owner forks (`AskUserQuestion`): **editable tier spin boxes** (not presets) + **capped** oldest tier (not forever). Four knobs persist per file in the `setting` table via `snapshots.RetentionPolicy` + `load_policy`/`save_policy` (floor-clamped). `prune` rewritten to `prune(db_path, policy, now)` — newest→oldest keep-all/newest-per-day/newest-per-month/drop; new `_stamp_of` parses the filename timestamp (tolerates the `-N` suffix), returns None for unrelated files so they're never deleted; `maybe_snapshot` prunes on both write + no-change paths so aging sets self-thin. New `mfl_desktop/ui/snapshot_settings_dialog.py` (four `QSpinBox` + live plain-English summary), reached via a **Settings…** button on the Data Library Snapshots tab; Save persists + prunes immediately + emits, register_window re-arms the capture timer (`_apply_snapshot_interval`, also called per-file in `_adopt_repository`). No migration, no new dependency. Verified offscreen: 1680 fakes → 25 kept (5/7/13), 12-month cap respected, idempotent, policy round-trip + clamp, dialog wiring.
- **Dark-mode budget-screen brush fix (2026-06-15):** ADR-076 round 3. Real use showed the budget **Annual matrix** washed out in dark mode (pale cells, light text). Cause: `BudgetMatrixModel.data()` returned hardcoded light `QColor` singletons as `Background`/`ForegroundRole` brushes — model brushes are reached by neither the palette/QSS re-apply nor the `themed()` registry, so they stayed light under the dark palette. Same shape in the goal-card paintEvent (`_GoalBar`/`_GoalCard`) and the Monthly view's bar-fill constants. Fix: all became **token accessors resolved at data()/paint time** (`tokens.c(...)`); added tokens `positive_strong` / `caution` / `today_col` / `rollover_bg` (each light value == the replaced hex, so light mode is pixel-identical). No new signal wiring — `apply_theme`'s force-repaint already re-queries `data()`; `_GoalBar` stores a token *name* and resolves in `paintEvent` so its fill is live-correct on a toggle. Verified offscreen on the live DB: dark emits only dark tints (no light leak), light emits the exact original hexes, Monthly + goal cards build in both themes.
- **Original PySide6 prototype kept at `prototype_register/`** as a reference for the data-grid pattern; it's not the main app.
- **Sister app MRL stays RDF-based.** MFL ↔ MRL integration is now at the data-exchange boundary, not shared storage.
- **Post-launch-prep session (2026-06-19):** **P3a de-overlap** (ADR-084 — shared `ReportFilterDialogBase` toolkit across the six report filter dialogs; P3b/P3c still open). **Investment Returns polish** — the right summary panel scrolls instead of squashing the Performers list, and the chart's last x-axis label no longer collides with the end-date. **`ofxtools` installed** (the OFX-import dependency was missing from the env). **Cross-source, count-aware import dedup** (ADR-085 — closes the OFX-vs-Banktivity duplicate hole that re-imported ~46 June rows; multiplicity-preserving so legitimate same-amount repeats survive; new `ImportReviewDialog`). **Register edit UX** — Tab in the payee editor advances to Category, and setting a known payee auto-fills its remembered category inline (ADR-072 amend). **Editable investment-transaction categories** for the cash income/expense actions only (ADR-086). **Reconciled edit-guard is now statement-aware** (ADR-040 amend) — a Banktivity orphan-reconciled (`status='Reconciled'`, no `statement_id`) **split** now opens on double-click instead of being blocked by a false "may unbalance the statement" confirm (27,977 such rows in the live file). All verified headless. _Recovery done by the owner (2026-06-29) using the built-in undo/dedup — the duplicate June import was removed and re-imported; ADR-085 dedup validated in real use._
- **Reports + dashboard fixes (2026-06-20):** **Income Over Time** report (ADR-088) — the income mirror of Spending Over Time, built by parameterising `SpendingReportWindow` over a `_Direction` descriptor (`IncomeReportWindow` is a one-attribute subclass; new `Repository.income_aggregates`, `IncomeOverTimeFilters`, migration 0028 widens the report-type CHECK). **Income & Expense gained a category filter** (ADR-088 amends ADR-064 — Categories checklist + `category_ids` on `income_expense_series`, parent selections expand to descendants). **Home budget card over/under fix** (ADR-087) — the card now measures spend against envelope **available** (allocation + rollover), not bare allocation, so it agrees with the Budget window (ADR-058). All verified headless; on `master`.
- **Investment income reporting + trade de-noising (2026-06-20):** Reinvested dividends (DRIP) now count as income (ADR-089) — reinvests are user-categorisable (amends ADR-086), the Income Over Time report has an *Include reinvested dividends* toggle valuing them at qty × price, and new reinvests auto-tag on import via a self-seeding default category. Investment **portfolio-move trades** (buy/sell/share-move/reinvest/split) are excluded from the Spending / Payee / Category-&-Payee / Income-&-Expense / Sankey reports (ADR-090) — a Buy was being miscounted as £1.85M of spending and as "(No payee)" noise. Also fixed the Spending/Income filter's rollup combo not re-rendering the category list until reopen. All headless-verified; on `master`.
- **Backlog usability round (2026-06-25):** ADR-105 — four `docs/backlog_notes.txt` papercuts in one round, view/dialog layer only (no migration). **(1) Save & New** on the manual New Transaction dialog: a third button (`save_and_new_requested()` flag) commits and reopens a fresh dialog on the same account. `register_window._on_new_transaction` refactored into a loop over new `_create_one_transaction(default_id) -> Optional[int]` (returns the account id to reuse, or None to stop) so the full commit path — transfer / split / payee-default-category — is reused unchanged; Split / cancel / error stop the loop. **(2) Payee autocomplete** in that dialog: optional `payee_names` → a `QCompleter` (PopupCompletion / MatchContains / case-insensitive), the same config as `PayeeTypeaheadDelegate` + `BulkEditDialog`; register passes `list_payee_names()`. **(3) Report no longer drops behind the register after the filter dialog** — `SpendingReportWindow._on_open_filter` (and the four sibling report windows: Income & Expense, Payee, Sankey, Category & Payee, Investment Returns) now `raise_()`+`activateWindow()` unconditionally after the modal filter closes; `IncomeReportWindow` inherits it. **(4) Bulk edit in the report drill-down** (`TransactionsListWindow`, ADR-034): register-matching edit triggers (`DoubleClicked|SelectedClicked|EditKeyPressed`), a reconciled-edit guard wired on the model, and a right-click menu + Ctrl+E **Bulk Edit N…** reusing `BulkEditDialog` + `bulk_update_transactions` for payee/category/status/memo. **Scope line (matches ADR-034): transfers + splits stay in the register** — a transfer-kind category is refused in the drill-down with a redirect. Future direction noted in the ADR: extract a shared transaction-table-behaviours mixin if a third surface needs these verbs. Verified offscreen on the public-demo DB (Save&New flag+values, completer 51 names, drill-down triggers+guard+2-row selection, all report windows build) + compileall + IRI guard green; on `master`.
- **Payee→category inference + Rules visibility (2026-06-25):** ADR-106 — two real-use reports diagnosed as a design gap, not a bug. Root cause: **two independent** auto-categorisation systems — the **pattern rules** (`rule` table) the Manage ▸ Rules… screen shows (owner has **0**), and the per-payee **`default_category_id`** memory (ADR-072, owner has **19**/3,135) that actually does the categorising — so the Rules screen looked empty/broken. Plus the memory is sparse (only written via the inline "remember this?" prompt / Payees dialog) and payees are fragmented (`ScotRail` carried the memory; the owner typed `Scot Rail`, a separate un-merged record with none). Owner forks (`AskUserQuestion`): infer from history; fold memories into the Rules screen; defer merging dupes. **(1)** New read-only `Repository.most_common_category_for_payee` (most-frequent non-Uncategorised non-transfer category across the payee + aliases, ties→most recent); `RegisterWindow._payee_default_category_for_name` now falls back to it when no explicit memory exists — **New Transaction dialog pre-fill only** (no write, doesn't touch inline edit or the importer, transfer-kinds excluded). Typing "Scot Rail" now pre-fills Commuting. **(2)** New `Repository.list_payee_default_categories`; the Rules dialog gains a **"Remembered payee categories"** section (Payee → Auto-category table + **Edit category…** reusing the Payees picker + **Forget** clearing via `set_payee_default_category(pid, None)`), so the automation picture is genuinely in one place. No migration. Verified offscreen on a live-DB copy (ScotRail variants all → Commuting, 19 memories listed, dialog builds + selection enables buttons) + compileall + IRI guard. **Done (2026-06-29):** the owner merged the fragmented payee spellings (ScotRail ×6, etc.) via the built-in alias/merge tools. **Follow-up data-safety fix (same day):** the Payees dialog filtered via `setRowHidden` (all 3,135 payees are rows), so a shift-click range over visible rows also selected the hidden rows between them — a merge confirm offered to fold **1,458** payees into 'ScotRail'. `_selected_ids()` now skips hidden rows, so Merge / Delete / Alias only ever act on visible (filtered-in) payees. Repro'd + verified headless on a live-DB copy (1,458→4).
- **Save & New polish + investment parity (2026-06-25):** ADR-107 — two owner asks on the ADR-105 Save & New button: it had drifted to the **left** of the dialog, and it should exist on **investment** transactions too. Root cause of the drift: `QDialogButtonBox` applies macOS's button policy, which left-aligns `ActionRole` buttons. **(1)** Replaced the auto-laid box in **both** New-Transaction dialogs (`transaction_dialog.py` cash, `investment_transaction_dialog.py`) with a hand-laid `QHBoxLayout` — secondary action left, then **Cancel / Save / Save && New on the right** — so the primary action sits where the eye lands, consistently on macOS + Windows. **(2) Save & New is now the default** (Enter) in both; `autoDefault` pinned off on the rest so focus can't steal it; Esc still rejects via QDialog's built-in. **(3)** The investment dialog gains Save & New for the first time: new `_save_and_new_requested` flag + `_on_save_and_new` (reuses `_on_save`; clears the flag if validation bails) + `save_and_new_requested()`, shown **create-mode only** (edit keeps `Cancel/Save`, Save default). `register_window._open_investment_txn_dialog` now returns the reopen flag and `_on_new_transaction` loops the investment path like the cash `_create_one_transaction` loop. No migration, view/dialog layer only. Verified offscreen on the public-demo DB (button order + default for cash, investment-create, investment-edit; Save & New absent on edit) + compileall (all modules) + IRI guard 6/6; on `master`.

---

## Owner profile

Mark Hall (`mark.a.hall@gmail.com`). Sole developer, not a professional developer, comfortable reading and editing Python. Runs the app locally; copies code into files manually. Moving personally from macOS to Windows — hence Windows-first distribution. Will share the packaged app with non-technical friends/family.

**Durable working preferences:**
- Deliver **complete files**, not diffs or snippets. After full-file replacements, verify imports were preserved.
- **Every significant architectural decision is recorded as an ADR** under `docs/adr/`, with rationale for the alternatives rejected — even decisions the owner has explicitly delegated.

---

## Target architecture (post-pivot, in progress)

**Stack:** Python 3.13 + **PySide6 (Qt for Python)** + **SQLite**, with DuckDB attachment as a future option for analytical queries.

**Distribution:** **macOS and Windows are now equal, first-class targets (ADR-050 — retires the ADR-008 "Windows-first" framing; development is back on macOS).** The app is already platform-clean (PySide6 + SQLite + `pathlib` + Fusion) and runs on macOS with no code change; the binding portability rule set lives in *Known pitfalls* §8. Packaging is per-OS (PyInstaller can't cross-compile): `.app`/`.dmg` built on macOS, `.exe` on Windows — ideally a GitHub Actions matrix. macOS distribution to non-technical users needs code-signing + notarization (Apple Developer Program). Linux is best-effort. Neither platform is packaged yet (no spec/build scripts exist — own round, future ADR).

**Layering** — mirrored in the prototype, will carry forward into the real app:

```
QMainWindow                ← top-level windows + navigation
    │
    ▼
QSortFilterProxyModel      ← filter / sort
    │
    ▼
TransactionTableModel      ← QAbstractTableModel contract
    │
    ▼
Repository                 ← only layer that touches SQL
    │
    ▼
SQLite
```

The Repository contract isolates the rest of the codebase from the storage decision. Future DuckDB analytics attachment slots in at the Repository level without disturbing UI or service code.

---

## Legacy architecture (v0.1, maintenance only)

**Stack:** FastAPI + Jinja2 + HTMX + Tailwind/DaisyUI + Chart.js + pyoxigraph 0.5.8.

**Run:** `python main.py` from the project root → `http://127.0.0.1:8000`.

**Location:** `main.py` + everything under `app/`. Jinja templates under `app/templates/`. TTL ontology under `docs/ontology/`.

**Status:** No new features. Bug fixes only if the owner needs them while the rewrite is in flight. The import engine (`app/core/import_engine/`) will be **lifted** onto the new repository — preserved, not rewritten.

---

## What survives the rewrite

| Element | Status in rewrite |
|---|---|
| CSV / OFX / QFX parsers (`app/core/import_engine/`) | Preserved; retargeted at the SQLite repository |
| Duplicate detection (OFX FITID hash, CSV composite MD5 hash) | Preserved |
| Account types and families (cash / credit / investment / property) | Preserved |
| Transaction status enum (Pending / Uncleared / Cleared / Reconciled) | Preserved |
| Category taxonomy | **Changed** — now hierarchical and dual-sourced (see below) |
| MRL-compatible IRIs as identifiers | Preserved — stored as opaque text columns in SQLite |
| MFL ontology TTL (`mfl-ontology.ttl`) | Becomes a reference document; no longer a runtime artefact |
| HTMX templates / Jinja layouts | Discarded — replaced by Qt widgets |
| Pagination logic | Discarded — `QTableView` virtualises natively |
| SPARQL data-access code | Discarded for MFL (retained inside MRL only) |

---

## Domain reference (still authoritative)

### Account types and families

| Key | Label | Family | Liability | Balance from |
|---|---|---|---|---|
| `cash_std` | Current account | cash | No | Transactions SUM |
| `savings_std` | Savings account | cash | No | Transactions SUM |
| `credit_std` | Credit card | credit | Yes | Transactions SUM |
| `investment_std` | Investment account | investment | No | Latest valuation |
| `property_std` | Property | property | No | Latest valuation |
| `vehicle_std` | Vehicle | vehicle | No | Latest valuation (planned) |
| `loan_std` | Loan | loan | Yes | Transactions SUM (amortizing; terms in `loan` table, ADR-095) |

### Transaction statuses

| Status | Usage |
|---|---|
| Pending | Manual entry, not yet on statement |
| Uncleared | Imported, needs review |
| Cleared | Verified correct |
| Reconciled | Matched to closing balance |

Import default behaviour: first import on an empty account → `Cleared` (treated as historical load); subsequent imports → `Uncleared` (review). Banktivity CSV imports always honour the per-transaction status from the file.

### Categories (revised model)

Categories form a **tree** with three sources:

1. **System defaults** — seeded from the v0.1 taxonomy (Income / Expense top-level, with the v0.1 subcategories). Not deletable; archivable/hidden TBD.
2. **User-created** — the user can add categories anywhere in the tree at any time.
3. **Import-created** — when an import file carries a category MFL doesn't recognise, the category is auto-created. For source formats with hierarchical paths (Banktivity uses `Parent:Child`), the path is parsed and intermediate nodes are created as needed.

Import lookup is by **full path**, not just leaf name — `Auto:Gas` and a top-level `Gas` are distinct categories. Reports/filters use SQLite `WITH RECURSIVE` to walk the tree when "this category and descendants" is requested. Open design questions tracked in memory (`project-categories-design`) and will be settled in the schema ADR.

### Identifier conventions

Carried forward from ADR-006:

- **Accounts and Person:** `mrl:ClassName_N` (integer suffix), MRL-compatible — e.g. `mrl:CashAccount_1`, `mrl:Person_1`.
- **Transactions, ImportBatches, ValuationEvents:** `mfl:ClassName_<uuid8>` — e.g. `mfl:Transaction_a3f7c901`.

In SQLite, these are stored as `TEXT` columns on the relevant rows (e.g. `account.iri`, `txn.iri`). They remain meaningful for any future RDF export and for MRL integration; internally the database uses INTEGER PKs for joins.

### Import workflow (preserved)

```
Upload (OFX / QFX / CSV)
    ↓
parse_and_stage() → returns (token, "preview" | "map")
    ↓ if "map"
Column mapping UI            ← for unknown CSV layouts
    ↓
apply_mapping_and_stage()
    ↓
Preview                      ← new / duplicate / potential-match classification
    ↓
commit_import()
    ↓
Result summary
```

**Duplicate detection:**
- OFX: bank FITID stored as `import_hash`.
- CSV: `MD5(account_iri + "|" + date + "|" + amount + "|" + payee_raw)[:12]`.

**Potential match:** manual entry on same account, same amount, same direction, date ±2 days. Default action is merge; merge preserves user data and adds the import hash to the manual entry.

### CSV format detection (preserved)

`_detect_format(lines)` returns `"banktivity"`, `"creditcard"`, or `"generic"`.

- **Banktivity:** Row 1 = account name (≤2 commas), row 2 has Type/Status/Date/Payee headers. Per-transaction status honoured. Amounts have a currency symbol (`£` / `$` / `€`) and commas. Date format M/D/YY. Split transactions collapsed to parent total. Direction is taken from the amount sign for all three Types (Deposit / Withdrawal / Transfer) per **ADR-038** — Banktivity exports signed amounts across the board, so the sign is the single source of truth. The Type column is informational only; mismatches (a `Withdrawal` exported with a positive amount because the user mis-tagged it) are logged at INFO and the sign wins. **Categories use `:` as the hierarchy separator** and will be created if unknown.
- **Credit card:** headers contain `debitCreditCode` or `merchant.name`. Date is ISO 8601 with time. Amount is always positive; direction comes from `debitCreditCode`.
- **Generic:** falls through to the column-mapping UI.

---

## Sister app — My Retirement Life (MRL)

MRL remains RDF/Oxigraph-based — its workload (tax law across jurisdictions, heterogeneous evolving relationships) is what RDF was designed for. Integration with MFL now happens at the **data-exchange boundary**, not shared storage:

- **Identifier compatibility** — MFL stores MRL-style IRIs as opaque text on rows that need to refer to MRL entities.
- **Reference reads** — MFL can read MRL's Oxigraph store directly for reference data (tax rates, jurisdictions).
- **Data exchange** — MFL can emit and consume RDF when full data exchange is needed.

The original "shared database with MRL" goal from ADR-001 is no longer the strategy at the storage layer. See ADR-009.

---

## Repository structure

```
C:\Users\hallm\Documents\GitHub\my-financial-life\
├── main.py                          # v0.1 entrypoint (legacy)
├── requirements.txt                 # v0.1 deps (legacy)
├── CLAUDE_CONTEXT.md                # this file
├── docs/
│   ├── adr/                         # ADR-001 through ADR-010 + README
│   ├── schema.sql                   # ADR-010 reference schema (copy lives in mfl_desktop/migrations/)
│   └── ontology/
│       ├── mrl-ontology.ttl         # shared with MRL — do not edit from MFL
│       └── mfl-ontology.ttl         # MFL-specific, now a reference document
├── app/                             # v0.1 web app (legacy — maintenance only)
│   ├── api/                         # FastAPI routes
│   ├── core/
│   │   ├── accounts/
│   │   ├── dashboard/
│   │   ├── import_engine/           # SUPERSEDED — port lives at mfl_desktop/import_engine/
│   │   ├── ontology/
│   │   ├── transactions/
│   │   └── templates.py
│   ├── data/                        # Oxigraph singleton + loader
│   └── templates/                   # Jinja/HTMX (discarded in rewrite)
├── mfl_desktop/                     # Native desktop app — the live target
│   ├── __main__.py                  # `python -m mfl_desktop` launches the GUI
│   ├── cli.py                       # init / import / list / categories / add-account
│   ├── requirements.txt             # PySide6 + ofxtools
│   ├── README.md
│   ├── db/
│   │   ├── money.py                 # Decimal ↔ INTEGER pence
│   │   ├── repository.py            # Repository — the ONLY layer that touches SQL
│   │   └── schema.py                # Migration runner (manages schema_version)
│   ├── migrations/
│   │   ├── 0001_initial.sql         # ADR-010 schema + seeded categories
│   │   ├── 0002_category_kind.sql   # ADR-014: kind column + Transfer seed
│   │   ├── 0003_account_folders.sql # ADR-015: account_folder + account.folder_id
│   │   ├── 0004_transfers.sql       # ADR-020: txn.transfer_id + partial index
│   │   ├── 0005_scheduled_txn.sql   # ADR-023: scheduled_txn template + due-date indexes
│   │   ├── 0006_budgets.sql         # ADR-024: budget + budget_account + budget_category
│   │   ├── 0007_payee_canonical.sql # ADR-029: payee.canonical_id + partial index
│   │   ├── 0008_vehicle_account_type.sql # ADR-032: widen account.type CHECK to include vehicle_std
│   │   ├── 0009_multi_currency.sql  # ADR-035: setting + fx_rate + transfer parent tables + backfill
│   │   ├── 0010_reports.sql         # ADR-039: report + report_folder tables
│   │   ├── 0011_reconciliation.sql  # ADR-040: statement + statement_txn + txn.statement_id
│   │   ├── 0012_investments.sql      # ADR-043: security table + txn investment columns (action/security_id/quantity/price/commission)
│   │   ├── 0013_security_prices.sql  # ADR-044: security_price table (per-security date-stamped prices)
│   │   ├── 0014_investment_returns_report.sql # ADR-046: widen report.type CHECK to add 'investment_returns'
│   │   ├── 0015_security_price_transaction_source.sql # ADR-047: widen security_price.source CHECK to add 'transaction'
│   │   ├── 0016_security_price_fetch_status.sql # ADR-049: security.price_fetch_failed_at (give-up memory for uncovered tickers)
│   │   ├── 0017–0028 …             # splits (ADR-051), budget redesign (0019, ADR-058) + credit pool/goals (0020–0022), payee/cat-payee reports (0023/0024), rule matchers (0025), feed_account (0026), csv mappings (0027), income-over-time (0028)
│   │   ├── 0029_bonds_and_options.sql # ADR-093: security instrument_type + price_multiplier + bond/option metadata; txn.accrued_interest
│   │   ├── 0030_budget_bill_lines.sql # ADR-094: budget_line.scheduled_txn_id (a bill = scheduled-backed line)
│   │   └── 0031_loan_accounts.sql   # ADR-095: widen account.type CHECK for loan_std + the loan terms table
│   ├── import_engine/               # Lifted from app/core/import_engine/
│   │   ├── ofx_parser.py            # OFX/QFX — verbatim from v0.1
│   │   ├── csv_parser.py            # Banktivity / credit-card / generic CSV (syntax bug fixed)
│   │   ├── qif_parser.py            # ADR-043: QIF investment import (!Account/Cat/Security/Invst); action→cash-sign mapping
│   │   ├── qif_actions.py           # ADR-044: shared investment action sets (share-in/out/zero-cash) used by parser + holdings
│   │   └── import_service.py        # Stage + classify + commit, rewritten against Repository (investment-aware per ADR-043)
│   ├── account_summary.py           # ADR-033: pure-Python per-account aggregations (mirror of budget_calc.py)
│   ├── budget_calc.py               # ADR-058/094: compute_matrix + compute_burndown (bill-aware staircase) + bill_occurrences_in_month (pure)
│   ├── goal_calc.py                 # ADR-058 R4b/c: pure pay-down/savings goal progress + required-monthly
│   ├── loan_calc.py                 # ADR-095: pure amortization — compute_schedule / required_payment / monthly_rate / split_payment
│   ├── holdings.py                  # ADR-044/045/046/053/054/093: FIFO holdings + value_history + returns; bond/option price multipliers + accrued-interest basis (ADR-093); ShrsIn/ShrsOut transfers; StkSplit scales lots
│   ├── home_dashboard.py            # ADR-075: Qt-free HomeData + gather_home_data (cards reuse shipped compute)
│   ├── prices.py                    # ADR-044/045/047/048/049: Tiingo client + refresh + backfill (full/missing-only/single-security) + fetch_metadata/lookup_symbol_name; ADR-049 adds 429 back-off (RateLimitedError + tiingo_rate_limited_until) + give-up memory for uncovered tickers + orphan-skip (securities_to_price) (no Qt deps; mirrors fx.py)
│   ├── fx.py                        # ADR-035: openexchangerates.org client + refresh helpers (no Qt deps)
│   ├── transfer_reconcile.py        # ADR-036/037: pure-Python score_candidate + greedy_pair helpers
│   ├── reports/                     # Package (was reports.py); category rollup helpers + filter dataclasses
│   │   ├── __init__.py              # category_group_map / category_root_map / category_path (re-exports)
│   │   └── filters.py               # ADR-039: SpendingOverTimeFilters + to_json/from_json/migrate + type dispatch
│   └── ui/
│       ├── register_window.py       # QMainWindow with sidebar + register
│       ├── register_model.py        # QAbstractTableModel — single-account + all-transactions modes
│       ├── filter_proxy.py          # Sort / filter on underlying values (search + status + ADR-062 date/amount range)
│       ├── register_filters_popover.py # ADR-062: "Filters" popover — date From/To + signed amount Min/Max
│       ├── delegates.py             # Payee + Category typeahead delegates + Status combo (ADR-022)
│       ├── category_picker.py       # Shared editable-combo helper for category fields
│       ├── csv_mapping_dialog.py    # ADR-021: column-mapping wizard for unknown CSV formats
│       ├── schedule_dialog.py       # ADR-023: single-record schedule create/edit
│       ├── schedules_dialog.py     # ADR-023: schedule list + CRUD + Post Now
│       ├── budget_setup_dialog.py   # ADR-024: perimeter + per-category setup
│       ├── budget_window.py         # ADR-024/025: budget screen (tiles + bar + chart + cards)
│       ├── burn_down_chart.py       # ADR-094: filled staircase — stepped actual/ideal/projection (bills step at due days)
│       ├── balance_flow_chart.py    # ADR-033/034: combo chart (income/spending bars + balance line; dual y-axis)
│       ├── value_chart.py           # ADR-045: two-tone cost-basis-vs-market-value bars (Positions tab)
│       ├── value_history_chart.py   # ADR-045 amend: portfolio value-over-time (Invested + Market value lines)
│       ├── treemap_chart.py         # ADR-045: squarified allocation treemap (default Portfolio-tab view)
│       ├── returns_chart.py         # ADR-046: stacked-composition total-return chart (cost/gain-loss/realized/dividends)
│       ├── investment_returns_window.py # ADR-046: Investment Returns report (chart + per-security table + totals)
│       ├── investment_returns_filter_dialog.py # ADR-046: period + account + security filters
│       ├── account_summary_window.py # ADR-033/034: per-account focus screen; family dispatch — cash (chart+Top-N) / investment (holdings tabs) / loan (LoanScheduleWidget, ADR-095)
│       ├── loan_dialog.py           # ADR-095: create/edit a loan — terms, calc-or-typed payment, live amortization preview, budget-goal opt-in
│       ├── loan_schedule_view.py    # ADR-095: reusable LoanScheduleWidget (summary + balance chart + schedule table + Record payment) embedded in the Account Summary
│       ├── transactions_list_window.py # ADR-034: drill-down register view with breadcrumb chips
│       ├── custom_period_dialog.py  # ADR-033 amendment: From/To date picker for "Custom" period
│       ├── currencies_dialog.py     # ADR-035: Manage → Currencies… (API key + refresh + manual rates + tunables)
│       ├── securities_dialog.py     # ADR-044/047: Manage → Securities… (Tiingo key + refresh + latest-prices table + manual prices; double-click → Stock Record)
│       ├── stock_record_dialog.py   # ADR-047/052: per-security Stock Record (editable ticker + Fetch, price history add/edit/delete, position, transactions, Merge… button)
│       ├── merge_securities_dialog.py # ADR-052: combine duplicate securities (same-ticker matches surfaced; best-data default survivor; precedence-aware price merge)
│       ├── price_history_chart.py   # ADR-047: single-line paintEvent price-over-time mini chart
│       ├── investment_transaction_dialog.py # ADR-048/093: action-driven New/Edit investment txn; ADR-093 adds the Instrument combo (Stock/Bond/Option) + per-class fields + accrued + multiplier-aware solver
│       ├── transfer_match_dialogs.py # ADR-036: confirm + picker + bulk review dialogs
│       ├── transfer_reconcile_dialog.py # ADR-037: Manage → Reconcile Transfers… (Ctrl+Shift+R)
│       ├── transfer_destination_dialog.py # ADR-035 amendment: account + cross-currency partner-amount picker
│       ├── spending_report_window.py # ADR-039: Spending Over Time window (Filter modal + drill-down + Save/Save As)
│       ├── spending_chart.py        # ADR-026 + ADR-039: paintEvent stacked bars (segment_clicked + set_show_legend)
│       ├── sankey_chart.py          # ADR-056: paintEvent tree-partition Sankey (SankeyNode; cubic-bezier ribbons; spine=max(in,out))
│       ├── sankey_report_window.py  # ADR-056: Sankey report (inline controls: timeframe/levels/threshold/amounts-%; income+exp+saved summary)
│       ├── spending_filter_dialog.py # ADR-039: filter modal — period / granularity / rollup + 3 checklists
│       ├── check_list_panel.py      # ADR-039: reusable search + Select-all / Deselect-all checklist widget
│       ├── new_report_dialog.py     # ADR-039: type picker (Spending shipped; others "coming later")
│       ├── save_report_as_dialog.py # ADR-039: name + folder picker with inline New folder… verb
│       ├── reconcile_wizard.py      # ADR-040: two-page reconcile (balances → check-off + live Missing)
│       ├── statements_window.py     # ADR-040: per-account statement history (New/Open/Delete + status)
│       └── sidebar.py               # ADR-015/039: ACCOUNTS + REPORTS top-level group sections
└── prototype_register/              # Original PySide6 prototype — kept for reference
    ├── README.md
    ├── requirements.txt
    ├── seed.py                      # builds prototype.db (10k synthetic txns)
    └── register_proto.py            # single-window register demo
```

---

## Backlog

Captured during the rewrite as features are deferred for later turns.

### New-feature follow-ups (2026-06-21 — ADR-093/094/095)

Deferred from the bonds/options, budget-bills, and loan features (all documented in their ADRs):

- **Loans (ADR-095):** the **"what-if" extra-payment scenario** (owner-flagged future — the engine already supports it; a scenario just re-runs `compute_schedule` with an altered extra payment); **auto-posting** the recurring loan payment with its dynamic split (Round 1 records payments on demand via the Account Summary's *Record payment* — a scheduled auto-split post extends ADR-023); exact per-day-count daily interest (Round 1 uses an effective-monthly rate); a loan-payment *bill* envelope (currently the loan is in the budget via a pay-down goal + the interest as an expense).
- **Bonds/options (ADR-093):** **coupon-income auto-scheduling** from `coupon_rate` + `maturity_date` (slots onto the schedule↔line link from ADR-094; would also net the accrued interest against the first coupon); **option exercise/assignment** (Round 1 is long buy→sell/expire only; written/short options record cash but flag `basis_incomplete`); **bond auto-pricing** is still impossible (no price feed — manual / transaction-derived only; see [[bond-pricing-cusip-not-supported]]); a bond yield-to-maturity column.
- **Budget bills (ADR-094):** **multiple bills per line** (a category with several schedules links the representative one; the others still post) — a multi-bill-per-line model is the natural extension.

### Register UX

Items (1)–(3) of the original cluster shipped under ADR-022 (typeahead delegates + inline category create). Item (4) bulk-edit shipped in the basic-management round under ADR-017. Follow-ups noted in ADR-022's consequences section:

- **Kind-aware inline create.** Today's confirm dialog fixes `kind='expense'` for the new category. If real use shows the owner regularly wanting income or transfer-kind categories created inline, the right v2 is a tiny kind radio inside the confirm dialog — not a separate full-create dialog path.
- **Memo history typeahead.** The memo cell is still a bare `QLineEdit`. A repository-backed completer over distinct memo strings would slot in symmetrically with the payee delegate.
- ~~**Category filter combo lists categories not in view.**~~ Closed 2026-06-07 — combo is now scoped to `Repository.distinct_category_ids_for_account(account_id)`; falls back to All when the prior pick disappears, clearing the proxy filter in lockstep.
- ~~**No visible count/net of currently-shown rows.**~~ Closed 2026-06-07 — status bar shows `Showing X of Y transactions · Net: ±£Z.ZZ` for single-currency views (All-transactions view skips the net since mixed currency would mislead).
- ~~**Register row-count / date-window filter (2026-06-08).**~~ **Shipped 2026-06-08 as ADR-041.** Profiling the real DB (11,917 txns; busiest account 6,403 rows) located the cost as the **proxy sort** — `QSortFilterProxyModel` calls the Python `lessThan` ~80k times per account switch / column-header click (199 ms by date, 513 ms by amount); load+reset add ~100 ms; painting is virtualised so the 2.3 s full-`data()` sweep is *not* what's felt. Fix: a "Show: Last 30 days / Rolling quarter / Year to date / All" combo on the filter bar, backed by **repository-level windowing** (chosen over a proxy filter — pushing `WHERE posted_date >= ?` into SQL cuts load + reset + sort together, vs. a proxy filter that still pays the full fetch). Default = **Rolling quarter (90 days)** (owner's pick). Running balance stays correct in a window via a seed query (`opening_balance + SUM(amount) WHERE posted_date < since`). Lower-bound-only so future-dated rows stay visible. `list_transactions_for_account` / `list_all_transactions` gained a `since` kwarg; `TransactionTableModel` gained `since` + `set_since`. Measured: windowed load 1.4 ms vs 69 ms full. Window is session state, not persisted (per-account saved preference remains a separate backlog item).
- **Inline add-transaction — faster manual entry (2026-06-08; first attempt parked).** Enter transactions directly in the ledger instead of always through the New Transaction modal. **First attempt (ADR-042, a docked fixed-bottom `QuickEntryBar`) was built, trialled, and reverted the same day** — owner: *"looks out of place, it's there permanently, and it doesn't feel quicker to enter on it."* Lessons for the next attempt (see ADR-042's parked-outcome note): entry should happen **in the grid** and aligned to the columns (a true in-grid append/edit row — the model/proxy-integration cost that got it rejected in ADR-042 may be worth paying), and the affordance should be **on-demand** (appears on a keypress / "＋", commits, gets out of the way) rather than permanent chrome; also re-check whether the modal is actually the bottleneck or just needs to open faster / pre-focused. The decoupled-widget approach (isolated from the model/proxy) is what *forced* the out-of-place detached strip — that tradeoff is the thing to revisit. Needs a fresh ADR.
- **New-entry save verbs: Cancel / Save / Save and add another (default) (2026-06-08).** On committing a new register entry the default verb is "Save and add another" (fresh line, ready to type); "Save" commits and closes; "Cancel" discards. The verbs themselves tested fine in the ADR-042 trial; they were parked alongside the bar and should ride on whichever entry surface the next attempt lands.

### Polish backlog from 2026-06-05 basic-management round

- **Visible "New Transaction" button.** Today the only entry points are the Transaction menu and Ctrl+N. A toolbar / register-pane button would be more discoverable. Owner asked for this during step 1; deferred to a later UI polish pass.
- **Unlock kind combo on New sub-category.** In the New Category dialog, when a parent is chosen the kind combo is locked to the parent's kind. For mixed-kind structures (e.g. Paycheck with Gross Pay = income and Taxes = expense beneath it), creating a different-kind child is currently two steps (create as inherited kind, then Change Kind). Editing this to leave the kind combo *unlocked* (just defaulting to parent's kind) is the obvious fix when real-world use confirms the need.
- ~~**Spending Over Time chart visuals.**~~ Closed by ADR-026 (paintEvent rewrite — modern flat look, rounded top corners, soft gridlines, hover tooltips, pill-shaped average label). Surviving sub-items (cleaner axis labels like `Jan 2026`, optional numbers-on-bars toggle, Save Chart As Image) carried into the new "Reports — round 2 follow-ups" section below.

### Budget arc — follow-ups after round C (2026-06-06)

The three-round arc (ADR-023 / 024 / 025) is complete. Items deliberately left for later polish or to feed real-use feedback:

- **Per-card sparkline** of historical actuals over the last N periods — a thin chart inside each card showing the trend, scoped to the card's own cadence period. Deferred from round C to keep card-layout simple while owner lives with the screen.
- **Reports → Budget vs Actual** time-series window — a separate report showing planned vs actual per category over a date range. Bigger window of its own; defer until use surfaces a clear need beyond the current per-month view.
- **Overdue-schedules surface on the budget window** — round C deliberately omits overdue schedules (next_due_date before period start) from the per-card "expected" badge. A small "X overdue schedules — review in Manage ▸ Schedules" indicator on the budget header would close the visibility gap.
- **Per-schedule (and per-budget_category) cadence anchor** — the global Monday-weeks / calendar-quarters rule from ADR-023 holds for both schedules and budget cards. A user whose paycheck is a Friday-paid biweekly schedule would benefit from a per-row anchor override; needs UI in the schedule and budget setup dialogs and a small schema migration to add the anchor column on budget_category (it's already on scheduled_txn).
- **Rollover** of unspent surplus / deficit between periods — a per-`budget_category` rollover flag. Additive when needed.
- **Multi-budget per file** — schema already supports it; UI surfaces a single default budget today.
- **Stepped burn-down ideal** that drops at known scheduled-txn dates rather than a single straight line. Adds nuance without new data.
- **Budget palette extraction** — round C inlines the segment / chip colours in `budget_window.py` and the chart series colours in `burn_down_chart.py`; consolidate if a wider palette unification ever lands.

### Payee arc — round 1 shipped (2026-06-06)

Three-round arc planned in **ADR-028** (Proposed). **Round 1 shipped 2026-06-06** as **ADR-029** (Accepted) + an amendment to **ADR-012**.

- **Round 1 (shipped)**: migration 0007 adds `payee.canonical_id` + partial index. `Repository` gains `set_alias_of`, `promote_to_canonical`, `list_canonical_payees`, `list_aliases_of`; `list_payees_with_usage` rewritten with canonical rollup; `list_payee_names` filtered to canonicals only (typeahead + bulk-edit completer); `merge_payees` re-points sources' aliases onto the target before deletion. `PayeeRow` extended with `canonical_id` / `canonical_name` / `direct_usage_count`. PayeesDialog gets 3 columns (Name / Alias of / Used in), indented aliases, and two new verbs (Make Alias of…, Promote to Canonical). Sort-on-column-0 disabled so canonical→aliases grouping survives. **Deferred deliberately**: register inline display doesn't roll up to canonical (round 2's import-engine work is the natural place — silent rewrite-on-no-change pitfall via the inline typeahead if done at display layer).
- **Round 2 — SHIPPED 2026-06-15 as part of ADR-072 (Arc G round 1).** Import-time alias lookup landed: `resolve_import_payee(raw)` exact-matches the raw string and normalises an alias hit to its **canonical**, so new ledger rows point at the canonical (the deferred round-1 display rollup is now done at import, dodging the inline-typeahead pitfall). Bundled with the per-payee category memory (the canonical's `default_category_id`, applied at import to otherwise-Uncategorised rows). Pattern / fuzzy alias matching folded into round 3 (G2) below.
- **Round 3 (= Arc G round 2 / G2) — SHIPPED 2026-06-15 as ADR-073.** The `rule` table is wired up (migration 0025 swaps `pattern_kind` to contains / starts_with / ends_with / is_exactly), pure `rules_engine.py` matches payee/memo text and sets payee and/or category by priority (field-independent first-win), applied at import after alias resolution. New **Manage ▸ Rules…** screen (rules CRUD + read-only aliases), retroactive apply (ask-each-time, unset/uncategorised-only), and the deferred manual-entry category pre-fill. **Arc G complete.** Possible future polish: regex matchers (deliberately omitted), match-against-resolved-payee (overlaps the per-payee default), an "N auto-categorised" count in the import summary.

**Decisions still ahead** (per-round ADRs to settle): round-2 match strategies; round-3 rule priority semantics + retroactive application; (lower priority) per-row register display rollup; (lower priority) a "Show aliases" toggle in the Payees dialog.

**Round 3 scope sharpening (added 2026-06-06)** — owner sketched the round-3 UI: a dedicated screen that shows **both** aliases (the round-1 manual mappings) and rules side-by-side, with create/delete affordances. Rule creation: matcher type (`contains` / `starts-with` / `ends-with` / `is-exactly`) + string + target canonical payee. Aliases are essentially the `is-exactly` case already, so round 3 should consider unifying the two — either by treating the round-1 `payee.canonical_id` rows as an implicit `is-exactly` rule in the unified view, or by migrating them into a single `payee_rule` (or similar) table with a `match_kind` column. Decision deferred to round-3 planning ADR; this note exists so the round-3 author starts with the unified-screen intent in hand rather than discovering it mid-build. Owner's wording: *"a dedicated screen to be able to see all of the aliases, and be able to create or delete rules. You'd be allowed to say 'if contains xxxx string, then xxxx payee' or 'starts with, ends with, is exactly' etc."*

### Reports — round 2 follow-ups (2026-06-06)

Owner saw the potential of the paintEvent approach (ADR-026) and flagged that reports has a lot more work to do — what shipped under ADR-018 is the floor, not the ceiling. Quoted reactions on the paintEvent landing: "Resizing is cleaner, the default hover over feels a bit more natural." Concrete items raised:

- ~~**Default to top-level category rollup on Spending Over Time.**~~ Shipped 2026-06-06 as **ADR-030**: new `Rollup:` combo on the report panel with three positions (Top level / Group / Leaf), default flipped from Group → Top level. New `mfl_desktop/reports.py::category_root_map` helper mirrors `category_group_map`; Leaf mode is the identity case. Categories checklist rebuilds on rollup change, all checked, Uncategorised's separate toggle preserved across all three modes.
- ~~**Hierarchical category pickers (charts, budget, transactions).**~~ Shipped 2026-06-06 as **ADR-031** — flat-with-breadcrumbs approach. `CategoryChoice` gained a `path` field; `Repository.list_categories_flat` builds it via a two-pass walk and sorts the result by path so siblings cluster under their parent. `make_category_picker` uses `c.path` as the display label (5 combo surfaces inherit the fix: transaction dialog, bulk edit, schedule, budget setup, register inline typeahead). New shared `mfl_desktop/reports.py::category_path(nodes_by_id, cid)` helper feeds the spending report's checklist so its labels show full paths too (matters most at Leaf rollup per ADR-030). Tree-popup variant rejected for v1 — flat list matches the payee typeahead pattern and stays consistent with the recently-shipped merge picker.
- **Editable register while a report is open — truly non-modal reports (2026-06-08).** Report windows are `QMainWindow`s but in practice behave almost modally: the owner can't edit the register or keep several reports open at once while one is up. Make report windows fully independent so the main register stays live and multiple reports can sit side-by-side. Likely a window-parenting / modality / focus-policy fix (check `setWindowModality`, parent assignment, and any app-modal exec loops) rather than new features — verify across every report window (Spending Over Time today; future Net Worth / Income & Expense / Sankey). Worth confirming the same for the other non-modal screens (account summary, budget, drill-downs) while in there.
- **Broader reports arc.** v0.1 had a dashboard and several views that haven't been ported yet. Owner wants to come back to reports as a thread now that the rendering ceiling is clear — likely a multi-round arc like the budget one. Open questions for the arc planning: which reports to ship, in what order, against which user task (cash flow, income vs spending, category trends, account balance history, net-worth trajectory). Treat as an open thread, not a single ADR.
- ~~**Saved reports arc planned 2026-06-07 as ADR-039.**~~ **Round 1 shipped 2026-06-07** as **ADR-039 (Accepted)**. Migration 0010, sidebar restructure (ACCOUNTS + REPORTS sections), Spending Over Time as the first persisted type, Save / Save As / Open / Edit / Delete vocabulary, Reports menu narrowed to bare-open verbs. Bonus that landed in the same commit: a Filter… modal (replacing the always-visible left panel — was too dense; round-1 polish landed before shipping), a right-side summary panel with vertical category legend (chart legend moved off the bottom band where long lists were truncated), and click-to-drill-down on bar segments with a Back button (drill state is view-only). **Open follow-ups for rounds 2-4 + polish**: Net Worth as the second persisted type (own ADR — its filter schema chosen at the time); Income & Expense; Sankey (needs its own rendering decision — `paintEvent` shape TBD); polish arc (drag-to-folder in sidebar, export to CSV / image, per-report colour override). The reusable `CheckListPanel` widget (search + Select all / Deselect all) is the natural fit for whatever filter checklists the later types need.

### Account workflows (2026-06-06)

Owner-raised additions on the same turn as ADR-032 (Vehicle account type). The per-account summary shipped 2026-06-06 (ADR-033); statement reconciliation remains an open arc.

- ~~**Statement reconciliation per account.**~~ **Shipped 2026-06-07** as **ADR-040 (Accepted)** + its Banktivity-aligned amendment — see the dedicated paragraph in *Status at a glance* for the as-built shape. Net deltas from the original plan: the `status` enum collapsed to `open` / `reconciled` (no `reconciled_with_variance`); the `adjustment_txn_id` column + auto-Adjustment-category seed were dropped (replaced by Add-Transaction-while-reconciling + live out-of-balance detection); the UI became a two-page wizard (`reconcile_wizard.py`) launched from a per-account history window (`statements_window.py`) rather than a single modal; entry points grew a register **Reconcile…** button alongside the menu (Ctrl+Alt+R) and summary row. **Follow-ups still open**: extend the reconciled-row edit confirm to the **bulk-edit (Ctrl+E)** path (today only inline register edits route through the `reconciled_edit_guard`); export a reconciled statement to PDF/CSV; statement-boundary auto-pre-tick from an OFX FITID set; an N-account / household reconcile. Carried-over unchanged: transfer pairs reconcile independently per side; cross-currency accounts reconcile in native currency; one open statement per account; reopen reverts linked rows to `Cleared`.
- **Account metadata fields (2026-06-08).** Per-account notes, account number, sort code, ~~credit limit (cards)~~, statement date. **`credit_limit` shipped 2026-06-12** as part of ADR-058 R4a (migration 0020 column on `account` + AccountDialog row shown for credit cards) — the budget's available-credit pool source; the per-account summary utilisation indicator it also unlocks is still open. The remaining fields (notes / account number / sort code / statement date) still want this item's dedicated metadata ADR. Several unlock downstream features: statement date feeds reconciliation pre-fill and the credit-card-payment scheduling item below. Needs a small migration (columns on `account`) + AccountDialog fields + an ADR (which fields, validation, where they surface). Sensitive fields (account number / sort code) live inside the `.mfl` file — carry the same "stored inside this file" disclaimer the Currencies dialog uses for the OXR key.
- **Schedule a credit-card payment after reconciling a card statement (2026-06-08).** When a credit-card statement is reconciled, offer to create / seed a scheduled transfer for the payment — amount = statement closing balance (or minimum), due = payment date derived from the statement-date metadata. Ties the reconciliation arc (ADR-040) to the schedules arc (ADR-023) and the account-metadata item above. Reuses the existing `ScheduleSeed` / `ScheduleDialog` seeding path (ADR-027). Needs an ADR for the trigger point + amount/date defaults.
- ~~**Per-account summary screen.**~~ Shipped 2026-06-06 as **ADR-033** + **ADR-034** (polish + drill-down). Combo chart (income/spending bars + balance line) over a six-preset period selector, right-column Summary / Additional Info / Upcoming / Reconcile placeholder, bottom-row Top Payees + Top Categories — now in card containers, with dual y-axis on the chart and clickable Top-N rows that open a `TransactionsListWindow` drill-down filtered to the clicked entity. **Follow-ups still open**: reuse `TransactionsListWindow` from other screens (Spending Report bar clicks, Net Worth account rows, payee/category dialogs' "show transactions" verbs — same dataclass, just wire each entry point); inflow Top-10 (symmetric to Top Payees but for incoming flow); cleared-only balance line variant (original ADR-033 backlog mentioned "cleared-vs-running variants"); per-account sparkline in the sidebar; saved period preference per account; custom date range; richer drill-down footer (inflow / outflow / net rather than just signed sum); drill-down for the `(No payee)` / `(Uncategorised)` synthetic buckets; reconciliation arc closes the placeholder.

### Multi-currency + transfer arc — follow-ups after 2026-06-06

The three-ADR arc (ADR-035 / 036 / 037) is shipped end-to-end. Open threads:

- **Report currency selector + conversion.** Spending Over Time, Budget tiles, and any cross-account aggregation in `TransactionsListWindow` still need a display-currency combo with `Repository.convert_amount` plumbed in. ADR-035 §UI specifies the surface; implementation deferred until after the user has lived with the matcher + currencies dialog. ~~**Net Worth was a confirmed par-add correctness bug.**~~ **Closed:** the headline report shipped as **ADR-055** (commit `adfffd3`, 2026-06-10) — display-currency selector (default base ccy, else GBP, else first in use), per-account/family/type/headline totals all sum **converted** values, unconvertable non-zero accounts **excluded** (never 1:1-folded) and surfaced in a "Set exchange rate…" banner, native amount in tooltips. The **sidebar folder roll-up** (the deferred follow-up in ADR-055 §Consequences) shipped as commit `262ca5c` — mixed-currency folders convert via the same FX layer, single-currency folders unchanged, repo-less callers fall back to the old no-symbol bare sum. Re-verified headless on the live DB 2026-06-14: 11 GBP + 15 USD accounts, base unset → GBP, 5 `fx_rate` rows present, all USD accounts now convert cleanly (0 excluded). The *other* surfaces above (Spending Over Time, Budget tiles, `TransactionsListWindow`) remain the open part of this item.
- **Import-time transfer suggestion.** After commit, surface candidates the matcher found between the just-imported account and every other account ("Looks like a transfer to Joint Savings; review these N rows?"). One-click bulk accept lands in the existing `TransferReconcileDialog` flow. Deferred until after the report selector since the user's stated workflow is to import → reconcile manually.
- **`backfill_historical` wired in the Currencies dialog.** The function exists in `mfl_desktop/fx.py`; the dialog has the Add manual rate row but not the Backfill historical button. A small "Backfill USD→GBP from 2024-01-01 to 2026-06-06" verb with the API-call-cost confirmation per ADR-035 §guard-rails would close the historical-rate gap for users importing years of foreign data.
- **Edit Transfer dialog with Unlink verb.** ADR-020 backlog item; now also the natural place to surface `update_transfer_rate` for the cross-currency case (e.g. user wants to override the FX-table rate that was used at link time). Inline category edits on a transfer half should redirect to it.
- **Per-budget currency override.** ADR-035 §budget left this deferred. A budget whose perimeter spans accounts of different currencies currently denominates tile totals in `person.base_currency`; users with two home currencies (rare but real) would benefit from picking the budget's own display currency.
- **OS-keychain API key storage.** Today the openexchangerates API key lives in the `setting` table — i.e. inside the `.mfl` file. The Currencies dialog warns about this. If real key-sharing (or multi-file portability) becomes a concern, a follow-up ADR can move it to the Windows Credential Manager / macOS Keychain.
- **Per-txn currency for manual foreign-currency entries.** Today a txn's currency is its account's currency. A user who wants to record a USD-denominated charge on a GBP credit card before the card converts has no first-class shape — they enter it at the converted GBP amount with a memo note. If this comes up, ADR-035 §rejected has the design space documented.
- **N-account reconcile wildcard.** ADR-037 deliberately picked two-accounts-at-a-time. A future "(Any other account)" mode in the B combo would let users do one pass for the discovery case ("which outflows on Current look like inflows anywhere?"). Additive to the existing dialog.
- **Per-pair category override in reconcile.** ADR-037 ships one category for the whole batch. A small per-pair category combo column is a one-evening addition if real use surfaces the need (e.g. mortgage transfers vs. between-own-accounts on the same A↔B pair).
- **Live re-pair on tolerance change.** Today the reconcile dialog only re-computes pairs on Apply or after the user re-opens. A reactive update when the user tweaks `transfer_match_window_days` or `transfer_fx_tolerance_pct` would feel snappier.
- **Stepped burn-down ideal.** Existing budget arc backlog item — drops at scheduled-txn dates instead of straight-line. Independent of multi-currency.

### App shell & navigation (2026-06-08)

Owner-raised cluster about how the app presents itself on launch and surfaces what needs attention.

- ~~**Landing / home screen on first launch.**~~ **Shipped 2026-06-15 as ADR-075 (Arc F).** Home is the default landing view (a `QStackedWidget` page beside the register; a Home row tops the sidebar). Eight cards: net worth, budget-this-month, accounts-by-family, upcoming bills, recent activity, top-5 payees, top-5 categories, investment gains/losses — all reusing shipped compute. **Follow-ups still open**: per-card period selector (spending cards are fixed to the current calendar month); card reorder/hide customisation; quick-action buttons (New Transaction / Import) — omitted from round 1 since menus/shortcuts cover them; a "first run / empty file" welcome variant (today the cards just self-hide).
- **Upcoming-bills notification in the sidebar.** A count / indicator of upcoming scheduled transactions (bills due soon) surfaced in the sidebar so due bills are visible without opening Manage ▸ Schedules or the budget window. Data already exists — `scheduled_txn.next_due_date` + the `upcoming_scheduled` helper in `account_summary.py`. Open question: a badge on an existing row vs. a dedicated "Upcoming" sidebar row vs. a header indicator. Sits naturally alongside the ACCOUNTS / REPORTS sidebar sections (ADR-015/039) — could be a third section.

### Save & persistence (2026-06-08)

- ~~**Save on close + auto-save.**~~ **Shipped 2026-06-12 as ADR-057.** The framing dissolved once ADR-050 Tier-2 made the default file a live, auto-committing `.mfl` — "write the open file on close" was already true (auto-commit, no pending state). The two *real* gaps were closed instead: (1) we open in WAL mode but never checkpointed/closed on exit, so a bare-`.mfl` copy dropped uncheckpointed writes → `closeEvent` now `Repository.checkpoint()`s (`wal_checkpoint(TRUNCATE)`) + `close()`s; (2) no guard against *logical* mistakes → **automatic rotating snapshots** (`mfl_desktop/snapshots.py`, `save_copy` into a `Snapshots/` folder beside the live DB on launch / 30-min in-session timer / clean close, keep 10, best-effort, change-detected via `max(mtime(.mfl), mtime(-wal))` so the three triggers don't churn dupes). Restore = File ▸ Open the snapshot. `_swap_repository` gives the file being left the same checkpoint+snapshot. Retention/interval are module constants pending a Preferences dialog. **Still genuinely open:** a periodic snapshot covers a long session, but there's no "Restore from snapshot…" picker yet (File ▸ Open already does it) and no Preferences UI for the tunables.

### Investment arc — follow-ups after round 1 (2026-06-08)

Round 1 (ADR-043) landed the QIF on-ramp + `security` master + `txn` investment columns + interleaved investment register. **Round 2 (ADR-044) landed holdings (FIFO) + prices (Tiingo + manual) + market-value net worth.** The data captured (`security_id`, `quantity`, `price`, `commission`, signed cash `amount`, linked-account memo) is enough for the later rounds to compute from without re-import. Open rounds (each its own ADR):

- ~~**Round 2 — holdings & cost basis.**~~ **Shipped 2026-06-09 (ADR-044).** FIFO computed on the fly in `holdings.py` (NOT persisted to `lot` — that stays reserved for manual-basis/specific-ID). Holdings table on the per-account summary; realized gain as a byproduct. `StkSplit` ratio application is still **deferred** (flagged `basis_incomplete`); shares/price stay REAL.
- ~~**Round 3 — prices & market value.**~~ **Shipped** across ADR-044 (current prices + market-value net worth) and ADR-045-amend (**historical prices**: `TiingoClient.fetch_historical` + `backfill_historical_into` + `Backfill history` button → `security_price`; `price_series`/`get_security_price_nearest`; `compute_value_history` powering the Overview value-over-time chart). Remaining nuance: **Net Worth still uses the *latest* price for every date** (it's a today snapshot — `compute_account_values`), so *historical* net worth (net worth as-of a past date, using that date's prices) isn't a thing yet; the per-account value-over-time chart does use historical prices correctly. The dormant `valuation` table remains unused — security prices live in `security_price`.
- **Round 4 — transfer-linking & income reporting.** Link the `L[Account]` cash rows (imported as plain cash with a memo note in round 1) to the real other-side account by reusing the ADR-036 matcher — this also fixes the holdings edge where whole-account `XIn`/`XOut` transfers don't move lots (ADR-044). Dividend/income reporting; per-currency display for cross-currency brokerage accounts.
- **Investment dashboard (ADR-045, amended).** Shipped: tabbed summary (**Overview** = portfolio value-over-time line chart; **Holdings** = searchable wide table; **Portfolio** = allocation **treemap** by default + a cost-vs-value bars view) + historical-price backfill (Tiingo) feeding the value line. ~~Phase 2: Returns + Dividends tabs~~ **superseded by ADR-046** — those became a cross-account *report* instead (see next item). Smaller follow-ups still open: a **period-zoom selector** on the value-over-time chart (currently full history; PERIOD presets lack an "all/max" so a small 1Y/3Y/5Y/Max toggle is the fit). ~~a launch-time auto-backfill for newly-seen securities~~ **shipped 2026-06-09 as ADR-047** (`securities_missing_history` + `backfill_missing_history_into`, wired into the launch runnable).
- ~~**Returns + Dividends (ADR-045 Phase 2).**~~ **Shipped 2026-06-09 as ADR-046** — built as the **Investment Returns report** (sidebar Reports section / Reports menu) rather than dashboard tabs, so it spans one account *or* the whole portfolio. `holdings.compute_returns()` (FIFO replay; per-security breakdown + chart series + totals), new `is_income`/`is_reinvest` in `qif_actions`, `report.type='investment_returns'` (migration 0014 widens the CHECK), `InvestmentReturnsFilters` (period incl. **max**/lifetime + account + security filters), a stacked-composition `returns_chart.py` (cost / gain-loss / realized / dividends, drawn relative to the cost line), `investment_returns_window.py` + `investment_returns_filter_dialog.py`, `Repository.list_investment_accounts()` / `list_securities_for_accounts()`. **Total return = unrealized (lifetime, as-of) + realized + dividends**, with **realized & dividends period-scoped** to the window (a TSLA sale years ago shows $0 YTD). Verified on live data: lifetime realized $2,672.14 (== holdings engine), TSLA +$354 in max / absent YTD, dividends $35,239.16 with no Div/ReinvDiv double-count. **Still open**: the cross-account view converts mixed currencies per-sample (not a time-weighted model). **ROI (return on cost) shipped 2026-06-09** (ADR-046 amendment — `cost_basis_sold` + Cost-deployed + Return %). **IRR (money-weighted return) is the next increment** — needs dated per-security cash flows + terminal MV + opening MV at window start for non-`max` periods + a root-finder; a true time-weighted period-return % also remains out of scope.
- ~~**Inline editing of investment rows + manual entry.**~~ **Shipped 2026-06-09 as ADR-048** — `InvestmentTransactionDialog` handles both New (on an investment account) and Edit (double-click a row); chose a single action-driven dialog over inline qty/price delegates (the cash amount + FIFO derivation live in one place). **Still open:** a manual cost-basis override on transferred-in shares (the `basis_incomplete` flag from ADR-044) is the natural point to finally **persist** lots in the `lot` table; transfer actions (XIn/XOut) + stock-split entry remain out of scope (deferred to the transfer-linking / split rounds); no delete-from-dialog or category picker in the dialog v1.
- **General QIF (`!Type:Bank` / `!Type:CCard`).** `qif_parser._section_for_header` / `_dispatch_record` are structured to add bank/credit-card QIF; do it when a cash-QIF file actually needs importing.
- **`!Type:Cat` ingestion.** Round 1 parses but doesn't bulk-create the source category tree. If a user wants their Banktivity categories pre-seeded rather than auto-created on demand, wire `QifFile.categories` → `find_or_create_category_path`.
- **Bond / fixed-income pricing by CUSIP (2026-06-18, investigated).** The app **cannot auto-price bonds** — `prices.py`/Tiingo covers only US stocks/ETFs/mutual funds **by ticker**, and the `security` master has no CUSIP/ISIN field (just `name` + nullable `symbol`). A bond like `037833ET3` (an Apple Inc. corporate bond) is an *untickered* security → it's valued by transaction-derived prices (ADR-047) + manual Stock-Record price points, just never auto-updated. Adding it is a real arc: a **CUSIP/ISIN identity field** on `security` + a **new price provider** (Tiingo can't; **FINRA TRACE** is the only free source but serves noisy trade prints, not clean EOD marks; ICE/Bloomberg/FMP are paid). Deferred unless bonds become a meaningful part of the portfolio.

### Other deferred items

- ~~**Saved CSV mapping profiles.**~~ **SHIPPED 2026-06-15** (ADR-077 Track 1 slice 2). Migration 0027 `csv_import_mapping` keyed by `csv_parser.header_signature`; first generic-CSV import maps + saves, same layout auto-applies + skips the wizard, renamed columns re-map, stale mapping → wizard fallback; account-agnostic. **Still open**: a profile-management UI (edit/rename/delete) — none yet (profiles are created/overwritten silently on map).
- ~~**QIF parser.**~~ **Shipped 2026-06-08 as ADR-043 (round 1)** — `mfl_desktop/import_engine/qif_parser.py` handles investment QIF (`!Account`/`!Type:Cat`/`!Type:Security`/`!Type:Invst`); `!Type:Bank`/`!Type:CCard` dispatch is stubbed for a later round when a cash-QIF file actually needs it.
- ~~**Categorisation rules engine.**~~ Absorbed into round 3 of the payee-aliases-and-rules arc — see ADR-028 (planning) and the "Payee arc" section below.
- **Per-lot IRR / ROI.** Schema is in place (`lot`, `valuation`); no computation yet.
- **Category management UI.** Re-parent, rename, archive categories — needed to manage what import-created and to undo path-conflict separations after the fact.
- **Dashboard.** v0.1 had it; needs porting to Qt — charts per ADR-026 (paintEvent), reusing `mfl_desktop/ui/chart_helpers.py`.
- **Account / settings management UI.** Add / edit / archive accounts, set base currency.
- **Valuation pipeline for non-cash accounts.** `valuation` table exists since ADR-010 but isn't wired. Vehicles (ADR-032), property, and investment accounts would all benefit. Mark-to-market source per family type (KBB/Autotrader for vehicles, land-registry/Zillow for property, broker feed for investments).
- **Packaging.** Single-file `.exe` via PyInstaller per ADR-008.

---

## Architecture Decision Records

| ADR | Topic | Status |
|---|---|---|
| ADR-001 | Backend language and triple store | Partially superseded by ADR-009 |
| ADR-002 | Frontend stack (HTMX) | **Superseded by ADR-008** |
| ADR-003 | Packaging (PyInstaller / AppImage) | Applies (Windows-first prioritised) |
| ADR-004 | Cross-platform portability | Applies |
| ADR-005 | Ontology strategy (MRL dependency) | Applies (reference-only for MFL) |
| ADR-006 | Instance IRI naming | Applies (IRIs become text keys in SQLite) |
| ADR-007 | Data access patterns (quad vs SPARQL) | Legacy-code only |
| ADR-008 | Desktop UI framework — PySide6 | **Accepted 2026-06-05** |
| ADR-009 | Storage engine — SQLite | **Accepted 2026-06-05** |
| ADR-010 | Transactional schema design | **Accepted 2026-06-05** |
| ADR-011 | Account delete policy — hard delete now, archive reserved | **Accepted 2026-06-05** |
| ADR-012 | Payee name-management policy | **Accepted 2026-06-05** |
| ADR-013 | Category management policy | **Accepted 2026-06-05** |
| ADR-014 | Category kind (income/expense/transfer) | **Accepted 2026-06-05** |
| ADR-015 | Account folders in the sidebar | **Accepted 2026-06-05** |
| ADR-016 | File save / open model — auto-commit + Save Copy As snapshots | **Accepted 2026-06-05** |
| ADR-017 | Bulk edit shape — modal dialog with per-field checkboxes | **Accepted 2026-06-05** |
| ADR-018 | Reports framework + first chart — Spending Over Time | **Accepted 2026-06-05** |
| ADR-019 | Net Worth report — three-column Assets / Net Worth / Debts | **Accepted 2026-06-05** |
| ADR-020 | Account transfers — category-driven, two linked txns sharing one transfer_id | **Accepted 2026-06-05** |
| ADR-021 | Generic CSV column-mapping wizard | **Accepted 2026-06-05** |
| ADR-022 | Register typeahead delegates + inline category create | **Accepted 2026-06-05** |
| ADR-023 | Scheduled transactions (bills, recurring income, recurring transfers) | **Accepted 2026-06-05** |
| ADR-024 | Budget core — perimeter + per-category targets + screen | **Superseded by ADR-058** |
| ADR-025 | Budget visualisations — burn-down + summary bar + cadence subtitles + scheduled projection | **Superseded by ADR-058** |
| ADR-026 | Visual style baseline (Fusion + custom palette) and chart-engine paintEvent | **Accepted 2026-06-06** |
| ADR-027 | Create Schedule From Transaction — right-click verb on the register | **Accepted 2026-06-06** |
| ADR-028 | Payee aliases, canonical labels, and the auto-categorisation arc (planning) | **Proposed 2026-06-06** |
| ADR-029 | Payee aliases round 1 — data model + manual alias UI | **Accepted 2026-06-06** |
| ADR-012 (amend) | Canonical / alias model + Merge/Alias/Delete verb table | **Accepted 2026-06-06** |
| ADR-030 | Spending Over Time rollup levels — Top / Group / Leaf | **Accepted 2026-06-06** |
| ADR-031 | Hierarchical category picker via full-path labels | **Accepted 2026-06-06** |
| ADR-032 | Vehicle account type and the `vehicle` family | **Accepted 2026-06-06** |
| ADR-033 | Per-account summary screen — combo chart, period selector, top-N breakdowns | **Accepted 2026-06-06** |
| ADR-034 | Per-account summary polish — dual-axis chart, section cards, Top-N drill-down | **Accepted 2026-06-06** |
| ADR-033 (amend) | Period preset set updated; Custom range added | **Accepted 2026-06-06** |
| ADR-035 | Multi-currency foundation — fx_rate, transfer parent, settings, openexchangerates integration | **Accepted 2026-06-06** |
| ADR-036 | Transfer matching — link existing other-side row instead of always creating a partner | **Accepted 2026-06-06** |
| ADR-037 | Bulk transfer reconcile — Manage → Reconcile Transfers… dialog | **Accepted 2026-06-06** |
| ADR-035 (amend) | Cross-currency entry collects partner amount at the point of creation | **Accepted 2026-06-07** |
| ADR-038 | Banktivity CSV import trusts the amount sign over the Type column | **Accepted 2026-06-07** |
| ADR-039 | Saved reports — schema, sidebar section, per-type filter persistence (round 1) | **Accepted 2026-06-07** |
| ADR-040 | Statement reconciliation — per-account flow, ending-balance match, statement persistence | **Accepted 2026-06-07** |
| ADR-040 (amend) | Banktivity-aligned UI: date-range + start/end balances, "Missing" counter, auto-select cleared, single-Save close-with-flag, computed out-of-balance, Statements history window, register Reconcile button; drops adjustment-txn + `reconciled_with_variance` | **Accepted 2026-06-07** |
| ADR-040 (amend) | Reconcile ending-balance default now tracks the statement **end date** (new `Repository.balance_as_of`, inclusive `posted_date <= end`) instead of the latest balance; re-derives on `dateChanged`; `textEdited` guard so a typed figure isn't clobbered | **Accepted 2026-06-10** |
| ADR-041 | Register date-window filter — repository-level windowing, default rolling quarter | **Accepted 2026-06-08** |
| ADR-042 | Inline register entry — docked quick-entry bar (built, trialled, reverted on feedback) | **Parked 2026-06-08** |
| ADR-043 | Investment accounts & QIF import — security master, txn investment columns, action→cash-sign mapping (arc plan + round 1) | **Accepted 2026-06-08** |
| ADR-044 | Investment holdings (FIFO) + prices (Tiingo + manual) + market-value net worth (round 2) | **Accepted 2026-06-09** |
| ADR-045 | Investment dashboard — tabbed summary + cost-vs-value chart (Phase 1); Returns/Dividends/Allocation tabs (Phase 2) | **Accepted 2026-06-09** |
| ADR-046 | Investment Returns report — cross-account total return (cost / unrealized / realized / dividends), period-scoped flows, stacked-composition chart; supersedes ADR-045's Returns/Dividends tabs; amended with cost-of-shares-sold + return-on-cost (ROI) | **Accepted (amended) 2026-06-09** |
| ADR-047 | Investment pricing — transaction-derived prices for untickered holdings, launch auto-backfill of missing history, editable ticker, source precedence (manual>tiingo>transaction), per-security Stock Record screen | **Accepted 2026-06-09** |
| ADR-048 | Investment transaction create + edit — action-driven dialog (New on investment account + double-click a row), signed-cash-impact derivation, investment rows now dialog-edited | **Accepted 2026-06-09** |
| ADR-049 | Tiingo request-waste reduction — orphan-skip, give-up memory, 429 back-off; amended with the history-backfill `startDate` fix (no-startDate returned a single row, never history) | **Accepted (amended) 2026-06-10** |
| ADR-050 | Cross-platform as a first-class goal (macOS + Windows) — retires ADR-008's "Windows-first"; binding portability rule set (see Known pitfalls §8); per-platform build + macOS signing/notarization; amends ADR-003/004/008 | **Accepted 2026-06-10** |
| ADR-051 | Split transactions — `txn_split` child table, parent keeps the signed total, `txn_category_line` unrolling view, "—Split—" register row + dialog, Banktivity split import (amended to allow transfer split lines) | **Accepted 2026-06-10** |
| ADR-052 | Merge securities — combine duplicate records for the same instrument (re-point txns + prices to a survivor, delete source); Stock Record "Merge…" verb; best-data default survivor; same-date price collisions resolved by ADR-047 source precedence; no migration | **Accepted 2026-06-10** |
| ADR-053 | In-kind share transfers (ShrsIn/ShrsOut) are not sales — ShrsOut books zero realized gain + carries cost basis to the matching ShrsIn via a per-security transfer pen (all 3 holdings engines); returns report pools same-currency accounts into one replay; cross-record transfers need an ADR-052 merge first | **Accepted 2026-06-10** |
| ADR-054 | Stock splits adjust the FIFO lots — `StkSplit` action (ratio = new-per-old in the quantity field) scales open lots (shares ×ratio, cost ÷ratio, basis unchanged) across all 3 holdings engines; "Stock split" entry in the investment dialog; reverse splits via a fractional ratio; no price rewrite | **Accepted 2026-06-10** |
| ADR-055 | Net Worth converts to a display currency — `convert_amount` per account before summing (was a bare-pence par-add of USD into GBP); display-currency selector; missing-rate accounts excluded + "Set exchange rate…" banner (no silent 1:1 fallback); sidebar folder roll-up same bug = follow-up | **Accepted 2026-06-10** |
| ADR-057 | Automatic rotating snapshots + clean-close checkpoint — closeEvent `wal_checkpoint(TRUNCATE)`s so the single `.mfl` is self-contained on quit; rotating timestamped copies in `Snapshots/` beside the live DB (launch + 30-min timer + clean close), keep 10, change-detection skips dupes; restore = File ▸ Open; extends ADR-016 | **Accepted 2026-06-12** |
| ADR-058 | **Budget redesign (umbrella)** — hybrid envelope/zero-sum/long-term-planning; per-month `budget_allocation` rows (copy-forward on edit) replace amortized amount+cadence; multiple budgets + scenarios; period (default Jan–Dec); 12-month editable matrix (Budget/Actual/Diff by income/expense/transfer) + per-section Unbudgeted rows; soft Unallocated vs perimeter-account pool; auto-rollover (reallocate by editing); historical suggestions; projected burn-down. Migration 0019 (`budget_line`+`budget_allocation`, drop `budget_category`). Supersedes ADR-024/025; keeps perimeter cancellation + nearest-ancestor bucketing + pure `budget_calc`. Phased R1–R4 (R2 = per-line rollover/role context menu + ↻ glyph; R3 = monthly progress-bar view + projected burn-down via pure `compute_burndown`/rebuilt `burn_down_chart.py`, Annual/Monthly toggle; R4a = credit cards as a pool source — migration 0020 `account.credit_limit` + `budget_account.contribution` (balance/available_credit/excluded), contribution-aware `compute_perimeter_pool`, AccountDialog credit-limit field, Setup per-account contribution combo; R4b = pay-down goals — migration 0021 `budget_goal`, pure `goal_calc` on signed balances, balance-based progress from a captured baseline, Goals strip (`_GoalCard`/`GoalDialog`) in both views; R4c = multi-account proportional savings goals — migration 0022 recreates `budget_goal` (+ `name`/`currency`) + adds M:N `budget_goal_account` (`share_bp` basis points + full-balance `baseline_balance`), `compute_goal_aggregates` rolls accounts up + FX-converts (ADR-055), pension=Investment, supersedes the R4b 1:1 model) | **Accepted 2026-06-13 (R1–R4c shipped — arc complete)** |
| ADR-056 | Sankey report (income → Total → expenses) — nested-by-hierarchy paintEvent tree-partition chart (ribbons never cross; spine = max(income,expense) + Savings/Deficit balance); income/expense by `category.kind` (transfers excluded); inline controls (depth, threshold→Other as % of total, amounts/%, YTD/MTD/Last-month/Custom); summary (income/exp/saved/saving%); saved-report type; income side needs owner to set category kinds | **Accepted 2026-06-10** |
| ADR-061 | Register search performance — searching the **All** date window on a big account (13.5k rows) froze per keystroke; three view/model-layer fixes: **(a)** debounce the Search box (200 ms single-shot `QTimer`); **(b)** precompute each row's lowercased search haystack once (`TransactionTableModel._search_blobs`, refreshed on load + `setData`; `search_blob_at`) so `filterAcceptsRow` is a single substring test (~15 ms → ~2 ms/pass); **(c)** coalesce the status-bar walk via a zero-delay single-shot timer (one walk per filter pass, was 2–3×). Follow-on to ADR-041's flagged "All is still slow", scoped to search not sort. No migration/schema/Repository change. **Amended 2026-06-14:** `category_name` added to the haystack + register Category combo removed (A2) | **Accepted 2026-06-14** |
| ADR-062 | Register date-range + amount-range filters (Arc A / A5) — a **"Filters ▾" popover** (`register_filters_popover.py`) with checkbox-gated date From/To + **signed** amount Min/Max + Clear, applied live; active-filter dot on the button. Filtering is **proxy-level** (in-memory, like Search — `TransactionFilterProxy.set_date_range`/`set_amount_range`), so no schema/Repository change. One coupling: a date-From earlier than the loaded Show window auto-widens the load via `_effective_since() = min(preset_since, filter_from)`. Owner forks: separate From/To fields, signed amounts, popover | **Accepted 2026-06-14** |
| ADR-063 | Schedules access from the register + overdue/due-soon cue (Arc A / A6 — **closes Arc A**) — a **"Schedules" button** on the filter bar (between ＋New Transaction and Reconcile) opens the existing cross-account `SchedulesDialog` (reuses `_on_manage_schedules`); its label carries a cue via new pure `account_summary.bills_due_summary(schedules, today, horizon_days=3)` (+ `BillsDueSummary`): plain when nothing's pending, amber `Schedules ● N` when due ≤3 days (inclusive), red `⚠ Schedules (N)` (N = overdue+due-soon) when anything's overdue; tooltip splits the count. Cross-account, counts **all** schedules incl. auto-post. `_refresh_schedules_cue()` after the launch auto-post sweep + on schedule changes + on `changeEvent`/`ActivationChange` (date-relative split survives an app left open across midnight). View-layer only — no schema/Repository/model change; best-effort. Owner forks (`AskUserQuestion`): "Schedules" label + count badge (not "Bills"/dot); count all incl. auto-post (matches the dialog summary) | **Accepted 2026-06-14** |

| ADR-064 | Income & Expense report (Arc E / E1) — fills ADR-039's reserved `income_expense` type (no migration; already in the 0014 CHECK). Income/expense **by category kind** (income=inflows on income cats, expense=outflows on expense cats; same as Sankey). **Transfers excluded by default + "Include transfers" toggle** (off by default): kind='transfer' always excluded by the kind rule, toggle additionally drops linked transfer-pair legs (`transfer_id IS NOT NULL`) filed under income/expense categories. Chart = income bars up / expense bars down + **net line** + dashed avg lines (paintEvent, single shared y-axis; borrows `BalanceFlowChart`). **Display-currency selector** converts before summing (ADR-055 exclude-no-rate, never par-add; not persisted — view pref). New pure `reports/income_expense.py` (`enumerate_buckets` keyed byte-identical to SQL `strftime` incl. `%W` New-Year straddle / `build_buckets` / `compute_summary`), `Repository.income_expense_series`, `income_expense_chart.py` / `income_expense_window.py` / `income_expense_filter_dialog.py`; wired into New Report dialog + Reports menu + bare/saved dispatch. Summary: total income/expense, net saved, savings rate %, per-bucket averages. No drill-down in E1. Owner forks: kind-based, up/down+net-line, display-currency, all 4 metrics. Verified pure + repo (multi-ccy exclude/convert) + offscreen Qt | **Accepted 2026-06-14** |

| ADR-065 | Historical FX rate backfill (UI + sampling) — wires the dormant `fx.backfill_historical` (no caller since ADR-035) to a **"Backfill historical…"** button in the Currencies dialog. New `FxBackfillDialog` (date From/To + Monthly/Weekly/Daily sampling + live "≈ N API calls" estimate + ≥60 confirm; synchronous w/ QProgressDialog, cancel keeps fetched rates). Backend: `backfill_historical` gains `granularity`; new pure `sample_backfill_dates` (daily/weekly/monthly; `date_to` appended); one OXR `/historical` call per sampled date, skip-if-present, USD-base (non-USD-in-use quotes; USD-only disables). Monthly default OK because nearest-prior fills gaps + linked-transfer rate is back-derived from real amounts. Unblocks historic multi-ccy conversion (Net Worth/Sankey/Income & Expense) + cross-currency transfer matching for past dates (matcher needs a rate ≤ the txn date; owner's rates only started 2026-06 so reconcile found 0 — adding a 2025 rate surfaced all 11 USD→GBP house-funding pairs as Strong). Owner fork: monthly default + selectable. Amends ADR-035 | **Accepted 2026-06-14** |

| ADR-066 | Payee report (Arc E / E2) — new `payee` report type, spending ranked by payee ("who do I pay the most?"). **Migration 0023** widens the `report.type` CHECK (0014 table-recreate recipe; `payee` was never reserved, unlike `income_expense`). **Strict outflow** (`kind='expense'` AND `amount<0`, the ADR-018 rule) over `txn_category_line`, grouped per **canonical payee** (`COALESCE(p.canonical_id,p.id)`, ADR-028/029 — aliases roll up; no-payee lines = one group). **Transfers excluded by default + "Include transfers" toggle** (drops `transfer_id` legs too), like ADR-064. **Display-currency selector** converts per-(payee,currency) at period-end; no-rate slices excluded+noted, never par-added (ADR-055; view pref, not persisted). **Ranked horizontal bars + sortable table** (payee/spend/%/txns); **top-N kept, tail omitted — NO "Other" bucket** (owner review; `hidden_count` shown in summary; `top_n=0`=all; pct/total over all payees so visible bars can sum <100%). **Bars + table rows click-through** to that payee's transactions — opens `TransactionsListWindow` (ADR-034) over the date range, **expanding canonical→canonical+aliases** so the drill matches the bar (No-payee group → NULL payee); widened the shared drill-down with `TxnListFilter.for_payees` (`payee_ids` set + `payee_is_null`) + `DrillDownFilterProxy.set_payee_ids`/`set_payee_null`. New `Repository.payee_spending_aggregates`, pure `reports/payee_report.py` (`build_report` — rank/truncate/pct/summary, no Other), `payee_chart.py` (horizontal-bar paintEvent, single hue, hover, `payee_clicked`), `payee_report_window.py`, `payee_filter_dialog.py`; wired into New Report dialog + Reports menu (`Reports ▸ Payee…`) + bare/saved dispatch. Owner forks: spending-only, bars+table, canonical roll-up, exclude-transfers+toggle; review changes: drop Other, clickable rows. Verified offscreen (alias roll-up, exclusions, no-payee group, transfers toggle, USD exclude→convert, rank/truncate/hidden-count/top_n=0, alias+null drill counts, click spawns drill, window build + saved round-trip) | **Accepted 2026-06-14** |

| ADR-067 | Net Worth two-ring donut (Arc E / E4) — **scoped exception to ADR-018 "no pies"**. Replaces the Net Worth summary-panel proportional bar with **two donuts** (larger **Assets** + smaller **Debts**, hidden when none): **inner ring = account type, outer ring = individual accounts** (outer = lighter tints of the type's family colour), hollow centre = side total, hover = amount + %. Two donuts because a donut can't hold a negative slice (debts flipped to positive). **Narrows ADR-018**: pies/donuts still banned for time-series/comparison; allowed for a **point-in-time composition of one positive whole**. New generic `donut_chart.py` (`DonutChart`/`DonutSegment`/`DonutChild`; paintEvent annulus via drawPie-overpaint + punched hole; angles clockwise-from-12, converted to Qt only at draw). `net_worth_window._donut_segments` builds from **already-FX-converted ADR-055 values** → multi-ccy + exclude-no-rate + missing-rate banner unchanged (no-rate account excluded from donut, still bannered). Existing standalone Net Worth window — no new saved type, no migration. `proportional_bar.py` now unused (left in place). Owner forks: two donuts, replace-the-bar, inner=type. Verified offscreen (inner ring = types, accounts nest + sum to type total, shaded children, debts shown/hidden, USD-no-rate excluded+bannered, empty/single paint) | **Accepted 2026-06-14** |

| ADR-068 | Category & Payee report (Arc E / E3) — the category×payee **intersection** as a **two-level drill**: level 1 ranks a **primary dimension** (Category/Payee top-bar toggle, default Category) → click a row → **level-2 breakdown by the other dimension** → click → **transactions** (`TransactionsListWindow` filtered by BOTH category-group descendants + payee, alias-expanded / NULL-payee). ← Back + breadcrumb. **Migration 0024** widens the report.type CHECK. One aggregate `Repository.category_payee_matrix` → spending per **(leaf category, canonical payee, currency)** cell (strict outflow, FX-converted, exclude-no-rate, transfers toggle); window **pivots/rolls/drills in-memory** (no re-query on toggle/drill). Category dimension = **budget-line group** (`category_group_map` — Groceries/Transport, NOT the Income/Expense roots); drill uses `category_descendants`. Reuses Payee `PayeeChart` + `build_report` (top-N + hidden-count, no Other) + sortable table + ADR-034 drill-down. New `category_payee_window.py` + `category_payee_filter_dialog.py`; wired into New Report dialog + Reports menu + dispatch. `PayeeSpendRow.payee_id` carries the row's item id (cat-group or payee). Owner forks: two-level drill (over matrix/folding), Category-first. Verified offscreen (cell merge Tesco canon+alias £60/2, level-1 budget-line groups, both drill dirs, leaf→2 txns, back clears, saved round-trip) | **Accepted 2026-06-14** |

| ADR-069 | Account lifecycle — close / reopen (Arc C) — **soft-close via the ADR-011 reserved `account.archived_at`** (no migration; read queries already filtered `archived_at IS NULL`). New `Repository.close_account`/`reopen_account` (set/clear `archived_at`, idempotent); `AccountSummary` gains `archived_at`+`is_closed`; `list_accounts`/`list_investment_accounts`/`compute_account_balances`/`compute_account_values` gain `include_closed=False` (default unchanged → every existing caller untouched; only the sidebar + Net Worth pass True). **Sidebar** partitions open vs closed → closed go into a **collapsible "Closed accounts (N)" group** (collapsed, muted, still selectable; `CLOSED_ROLE` marker; `select_account_by_iri` descends + expands). **Register window**: Account menu **Close Account…** (open only); context menu offers Reopen/Delete on a closed account, Edit/Move/Close/Delete on an open one. **Net Worth** gains a **"Show closed accounts"** checkbox threading `include_closed` into totals/donuts/columns (leaves suffixed "(closed)"). **Owner decisions** (AskUserQuestion): close-only (not close+hide); excluded-with-toggle (not always-excluded). **Flow reports keep closed history by construction** (they aggregate `txn`/`txn_category_line`, never gate on `account.archived_at`); only account-*pickers* drop the closed account. Verified offscreen on a live snapshot (28 checks: close/reopen list + value membership, net-worth £2,589,980↔£2,656,421, idempotent re-close, sidebar Closed group collapsed + CLOSED_ROLE, NW toggle, dialog) | **Accepted 2026-06-14** |
| ADR-070 | Category lifecycle — archive / restore (Arc D) — **soft-archive via the ADR-011 reserved `category.archived_at`** (no migration; tree/picker/budget reads already filtered it). Mirrors ADR-069 (archive-only; excluded-with-toggle; history retained — reports roll up `category_id` regardless of `archived_at`). New `Repository.archive_category` (**cascades down the subtree**; rejects Uncategorised id=1; idempotent) / `unarchive_category` (**cascades down + up to the root** so a restored node is always reachable) + private `_category_ancestors` (recursive mirror of `category_descendants`); `CategoryNode` gains `archived`; `list_category_tree(include_archived=False)`. **Invariant**: every non-archived category has a non-archived parent (or is root) — held by cascade rules + parent-pickers skipping archived nodes + **`find_or_create_category_path` resurrecting** (un-archiving) a matched archived path on import (UNIQUE(parent_id,name) blocks a fresh sibling anyway). **Categories dialog**: "Show archived" toggle (archived rows greyed + "(archived)" suffix) + one **Archive/Restore** button that flips by selection. Verified offscreen (archive 13-node branch cascade, hide/show, restore-descendant-restores-ancestors, full restore, Uncategorised reject, import-resurrect, dialog reload) | **Accepted 2026-06-14** |

| ADR-071 | Investment QIF `P` field is a description, not a payee — closes the root cause behind the 2026-06-14 junk-payee cleanup. Brokerage QIF (Banktivity export) puts a free-text description on `P` ("DIV - SABRA HEALTH CARE REIT INC REC 08/29/25 …"), but ADR-043 copied it into `payee_raw`, so every investment import minted one payee per description string and **re-imports regenerated them**. **Parser-only fix** (`qif_parser._normalise_invst_record`): fold `P` into the memo (de-duped against `M`) + set `payee_raw=''` so `get_or_create_payee('')` returns `None` — investment-action rows never mint a payee (identity already carried by `Y`→`security_id` + the `N` action). Re-import dedup unaffected (investment hash = account+date+action+security+quantity+amount, never the payee). No schema/migration/Repository/UI change; cash CSV/OFX/QIF-bank paths untouched. Owner forks (`AskUserQuestion`): scope = **all investment-action rows** (not just Div/income); `P` text = **folded into memo** (not dropped). Amends ADR-043. Verified offscreen (Div/Buy/Cash → `payee_raw=''`, `P`+`M` in memo) | **Accepted 2026-06-15** |

| ADR-072 | Payee→category memory (Arc G round 1) — implements ADR-028 round 2 (import-time alias lookup) + the per-payee "memorised payee→category" goal. **No migration** (uses `payee.default_category_id` + `canonical_id`, both reserved since 0001). Memory lives on the **canonical** (aliases route through it; Uncategorised clears). New `Repository.resolve_import_payee(raw)→(payee_id, default_cat)` (empty→none per ADR-071; alias name→**canonical** id, normalising new ledger rows; unknown→new canonical), `set/get_payee_default_category`, `count_/apply_default_category_to_uncategorised` (canonical+aliases, Uncategorised + non-transfer + non-split only, never overwrites). `commit_import` fills category from the memory **only when the source left it Uncategorised** (file category always wins; splits skipped); re-import dedup unaffected (cash hash uses `payee_raw`). Capture: `MemoriseCategoryDialog` on an inline non-transfer category edit **when the payee has no memory yet** ("Always categorise Tesco as Groceries?" + opt-in back-fill). Manage: Payees dialog **Auto-category** column + **Auto-category…** button (picker + Clear + back-fill prompt). Owner forks (`AskUserQuestion`): import-memory first, confirm-prompt capture, retroactive ask-each-time (uncategorised-only). Pattern-rules engine + unified Payees & Rules screen + manual-entry pre-fill = **round 2 (G2)**. Verified (repo helpers, end-to-end Banktivity import, offscreen Qt dialogs) | **Accepted 2026-06-15** |

| ADR-073 | Auto-categorisation rules engine (Arc G round 2) — wires the `rule` table (reserved since 0001; ADR-028 round 3) + the owner's unified "see aliases, create/delete rules" screen. **Migration 0025** swaps `rule.pattern_kind` `(substring,regex)` → `(contains,starts_with,ends_with,is_exactly)` (table-recreate recipe; table empty). Pure `rules_engine.py` (`rule_matches` case-insensitive 4-kind + empty guard; `apply_rules` priority-ascending, **field-independent first-win** so payee-only + category-only compose; duck-typed → no Repository import). Applied at import **after** ADR-072 alias resolution: rule payee overrides resolved payee (re-points default lookup); category precedence **rule → per-payee default** (file category still wins via Uncategorised guard); rule payee hits split parents, rule category doesn't. Retroactive (owner: **ask each time**) reuses `rule_matches` vs the **stored payee name** (raw string not retained), fills only Uncategorised/NULL, never overwrites/transfers/splits. Repo: `RuleRow`, `list/create/update/delete_rule`, `count_txns_matching_rule`/`apply_rule_to_existing`, `new_rule_iri()`. UI: `rule_edit_dialog.py` + `rules_dialog.py` (**Manage ▸ Rules…** — editable rules + read-only aliases) + `NewTransactionDialog` category pre-fill (deferred G1 nicety, optional callback). Owner forks (`AskUserQuestion`): rules screen+aliases, retroactive ask-each-time, rules set payee and/or category. Rejected: merge aliases into `rule` (ADR-028 Option C), regex, retroactive overwrite. Verified (pure engine, migration, repo CRUD+retroactive, end-to-end import incl. precedence + split guard, offscreen Qt dialogs) | **Accepted 2026-06-15** |

| ADR-074 | Capture a scheduled transfer's destination at post time — fixes an owner-reported bug: posting a transfer-kind schedule with no `transfer_to_account_id` raised "Transfer schedule is missing a destination account." with no way to fix it (cause: a category switched to transfer-kind after the schedule was created, or seeded without one; setup/edit already require one). `SchedulesDialog._on_post_now` now opens the existing `TransferDestinationDialog` (unlocked) when the destination is missing, persists it via new `Repository.set_scheduled_transfer_destination` (rejects dest==source), re-reads, and continues (confirm shows it; cross-currency block still collects the partner amount). Persisting repairs the template so future posts + the auto-post sweep work. No schema change. Verified offscreen (repo raise→fix→post both halves; dest==source rejected) + Qt (Post Now stubbed dialog persists+posts+emits) | **Accepted 2026-06-15** |

| ADR-075 | Home / dashboard screen (Arc F) — a launch landing view composing already-shipped compute (no new aggregation, no migration). Register window's right side → `QStackedWidget` (Home page + existing register/report panel); a **Home** row atop the sidebar (`selection_changed("home", None)`); `_show_*` flip to the register page, `_show_home` back. **Home is the default landing view on launch** (owner fork). Self-hiding cards: net worth (FX-converted, ADR-055, excluded-count noted), budget-this-month (default budget Expenses planned vs spent via `compute_matrix` + bar), accounts-by-family (click→register), upcoming bills (`list_scheduled_txns`, overdue cue), recent activity (new bounded `list_recent_transactions(limit)`, click→account), top-5 payees (`payee_spending_aggregates`+`build_report`, FX), top-5 categories (`category_payee_matrix` rolled up, FX — not native-sum, owner data is 11 GBP+15 USD), investment performance (top-5 unrealised gains/losses by security via `compute_holdings_view` across accounts, **each converted to display ccy before aggregating** per ADR-055). New Qt-free `home_dashboard.py` (`HomeData`+`gather_home_data`, per-card isolation) + presentation-only `ui/home_view.py` (scroll of cards, nav signals→existing handlers, refresh on WindowActivate) + sidebar Home row + register stacked-pane wiring. Spending cards scoped to current calendar month; investment = unrealised snapshot. Owner forks (`AskUserQuestion`): Home default; the 8 cards. Deferred: per-card period selector, card customise, quick actions. Verified offscreen on a seeded file + live-DB copy (net worth £2.66M/0 excluded, budget £1,900 vs £2,320, FX top payees/categories, per-security gains in GBP) + offscreen Qt (HomeView build+signals; sidebar Home row; RegisterWindow lands on Home, flips to register on account select) | **Accepted 2026-06-15** |

| ADR-076 | Design tokens + light/dark theming (Arc B round 1) — amends ADR-026. New `ui/tokens.py` semantic token layer (light+dark per token; `c(name)` resolves active), replacing ~140 scattered hardcoded hex (two drifted ramps: gray vs slate + casing). **Discipline: each token's light value = the hex it replaced** → light mode pixel-identical, only dark values new. Live switch: `theme.py` rebuilds palette + global QSS from tokens; a **`tokens.themed(widget, "color:{muted};…")` registry** (WeakKeyDictionary) re-formats per-widget inline styles on switch (`_format` = regex over `{token}`, literal QSS braces pass through); `tokens.notifier.changed` for charts. Persisted `ui_theme` in `setting` (no migration); `__main__` applies on launch; **View ▸ Dark Mode** toggle in register_window. Two regex sweeps converted single- + multi-line `setStyleSheet` across ~30 files; register/sidebar/Home/all dialogs/saved-report windows theme fully. **Deferred to B2 (readable light islands):** paintEvent charts + `account_summary_window` (`_COLOR_*` constants). Owner forks (`AskUserQuestion`): design-system foundation + dark-mode toggle. Verified offscreen on live-DB copy (tokens per theme; probe restyles on toggle; register + 3 report windows + Rules dialog build in dark; `ui_theme` round-trips) | **Accepted 2026-06-15** |

| ADR-077 | Bank feeds (Arc H) — pluggable providers, GoCardless first (round 1: foundation). No free universal bank API + owner has UK+US accounts → a **pluggable provider framework** (`mfl_desktop/feeds/`, Qt-free/stdlib) where **each user supplies their own key** (no central cost), feeds **reuse the existing import pipeline** (normalise→`_classify_and_stage`→FITID/hash dedup→review→`commit_import`). Round 1: `feeds/gocardless.py` (token/institutions/requisition-consent/poll/transactions over urllib, injectable transport), `feeds/normalize.py` (GoCardless booked txn→raw-txn: sign→tx_type, `transactionId`→`fitid`; pending skipped), **migration 0026** `feed_account` (MFL↔provider account, cascade; creds in `setting`, tokens never persisted), Repository `FeedAccount` CRUD, `ImportService.stage_feed`, `cli.py feeds-check` probe. Owner forks (`AskUserQuestion`): GoCardless-first, manual "Update accounts" refresh; consent via browser + **poll** (no local web server). Deferred: Manage ▸ Bank Feeds UI (H2 connect/link/update), re-consent/scheduled/US providers (H3). Rejected: Plaid/TrueLayer (central cost), auto-commit, persisting tokens. Verified offscreen (normalize, client request-building + 401→error, feed CRUD+cascade, stage_feed re-fetch idempotency); live connectivity owner-tested via feeds-check | **Accepted 2026-06-15** |
| ADR-048 (amend) | Investment txn dialog — Buy/Sell tri-field **Quantity ⇄ Price ⇄ Total cost** (enter any two) + a **Commission** term (`Total = qty×price ± commission`). Amount already nets the fee in (basis = `abs(amount)`), engines unchanged; `TransactionRow` gains `commission` (4 invest SELECTs read it). Verified offscreen | **Accepted 2026-06-16** |
| ADR-078 | Packaging & distribution — **direct signed + notarised downloads for 1.0; App Stores deferred to 1.1+** (decision K0). Notarised ≠ App Store (direct passes Gatekeeper/SmartScreen); MAS sandbox is at odds with the `.mfl`/`Snapshots/`/`Library/` file model. PyInstaller per-OS, macOS universal2 DMG + Sparkle, Windows x64 signed installer + WinSparkle. **Closes ADR-050 Tier-3, amends ADR-003.** | **Accepted 2026-06-16** |
| ADR-079 | Licensing & pricing — **one-time perpetual, everything included; paid 2.0 upgrades; offline signed license key** (decision C1). Local-first + BYO feeds = no recurring cost → no subscription. Merchant-of-Record (Paddle/Lemon Squeezy) handles UK/EU VAT + key delivery. Rejected: tiered unlock, subscription | **Accepted 2026-06-16** |
| ADR-080 | Enable Banking production & application-key custody (planning) — frames F1a (**BYO** vs MFL-hosted registration). EB Production needs contract + KYC + billing + privacy/ToS URLs. **Decision: defer custody to after the B3 compliance read; ship the BYO cores in 1.0** (feeds are 1.1). Constraints: key never in binary/`.mfl` (→ keychain), ≈90-day re-consent, Restricted→Unrestricted activation | **Proposed 2026-06-16** |
| ADR-081 | MFL → MRL data exchange — one-way, user-initiated **file-based RDF/Turtle export** over the shared ontology (workstream M, bundle offering, 1.1 fast-follow). Point-in-time balances (not the ledger); each account's `mrl:` IRI verbatim with `rdf:type` mapped to the right MRL subclass; the **authority-predicate merge contract** (MFL owns balances/currency/limit, never the growth/drawdown assumptions). Qt-free `export/mrl_rdf.py` + Export verb + CLI; stdlib Turtle writer. Rejected: shared DB / live store read, JSON/CSV | **Proposed 2026-06-16** |
| ADR-083 | Report drill-down completion (launch P2) — the four **dead-end** reports (Sankey, Income & Expense, Investment Returns, Net Worth) become clickable, reusing the shared `TransactionsListWindow` (ADR-034). Two new `TxnListFilter` dims + `DrillDownFilterProxy` filters: **kind** (`for_kind`, Income & Expense — mirrors `income_expense_series`'s exact predicate so the drill reconciles with the bar) + **security** (`for_security`, Investment Returns). Sankey: `SankeyNode.category_id`→`node_clicked`→`for_category`(+descendants). I&E: `segment_clicked(kind,bucket_key)` + new pure `income_expense.bucket_bounds`→`for_kind`. Returns: chart is portfolio-level so the per-security **table** drills (sid on row, dbl-click→`for_security`). Net Worth: `DonutChild.account_id`→`account_clicked`→window `account_activated`→`register_window._open_account_summary` (**owner fork: Account Summary page, not the flat register**). **Budget already satisfied** (matrix dbl-click→`BudgetDrillDownWindow`, a txn-id-set editable register; kept over the shared window — perimeter bucketing can't be expressed by the shared filters). Kind/security chips non-removable; account scope mirrors ADR-066. Limits noted/deferred: split fidelity, include_transfers, archived cats. Verified offscreen (all five drills + bucket_bounds round-trip + donut paint + app import) | **Accepted 2026-06-18** |
| ADR-082 | Date & period standardisation (launch P1) — new Qt-free **`mfl_desktop/periods.py`** (one period vocabulary/bounds/labels; per-context preset SETS, keys unchanged) + Qt **`ui/date_widgets.py`** (`make_date_edit`/`make_period_combo`). `account_summary`+`reports/filters` re-export the vocab; register/Sankey/Investment drop inline maths; all 10 `_PERIOD_LABELS` dicts removed; month/year windows calendar-accurate everywhere (fixes the `"6m"`=180d-vs-calendar bug). **Amendment: P1a → ISO `yyyy-MM-dd` inputs** (owner reversal); Sankey gains Last 6/12 months | **Accepted 2026-06-16** |
| ADR-084 | De-overlap (launch P3a) — shared `ReportFilterDialogBase` toolkit across the six report filter dialogs; ~40% copy-paste (period/custom-date plumbing, `_set_combo_to`/`_initial_custom_dates`, granularity list, transfers tooltip, all-checked→`[]`, OK/Cancel scaffold) consolidated; each keeps its specials (rollup/securities/top-N/category tree). Sankey partial (no period, tuple return). Constructor signatures + saved `filters_json` unchanged. Rule: consolidate duplicates, keep distinct affordances (schedules' 4 entry points kept). Latent fix: seeder guards `period_bounds` `None` start for `max`/`all`. Verified across all six. **P3b (transfer-match chips) + P3c pending** | **Accepted 2026-06-18** |
| ADR-085 | Cross-source, count-aware import dedup — exact-hash check missed the same txn from a different source (OFX FITID vs Banktivity composite vs feed id) → ~46 June rows duplicated. **Multiplicity-preserving** pairing (each existing row claimed once, reuses `transfer_reconcile.greedy_pair`) so 1-in-register absorbs only 1 of N (the £8.90-ScotRail-3×/week case stays intact). Pure `import_engine/dedupe.py` (exact amount + date ±window; same-day/payee-overlap = strong/default-skip, else weak/opt-in); `Repository.list_dedupe_candidates`; `_classify_and_stage` batch pass; confirm = merge manual placeholder or skip already-imported. New `ImportReviewDialog` shown only when dupes flagged (clean imports still silent); feeds auto-confirm strong only. Verified (pure matcher + stage→commit + dialog) | **Accepted 2026-06-19** |
| ADR-086 | Editable categories on investment transactions, scoped to **cash income/expense actions only** (Div/interest/cap-gains/fees/manual Cash — not Buy/Sell/shares/split, which would distort cashflow reports). Owner forks: reports&budget goal; dialog picker + inline column. New `qif_actions.is_categorisable` (+`CASH_EXPENSE_ACTIONS`) gates everything. `InvestmentTransactionDialog` category picker (income default, edit pre-selects); `COLUMNS_INVEST` gains a Category column inline-editable only for categorisable rows (per-row `flags()`/`setData()`; its double-click edits inline not dialog). No report/budget change — kind aggregation already handles it. Verified | **Accepted 2026-06-19** |
| ADR-072 (amend 2026-06-19) | Payee→category memory now also applies on **inline payee edits** (not just import): setting a known payee fills its remembered category when the row is Uncategorised (non-transfer); repaints payee+category without re-prompting. Shipped with a register nicety (no ADR): Tab in the payee editor commits + advances to Category (accepting the completion first) | **Accepted 2026-06-19** |
| ADR-040 (amend 2026-06-19) | Reconciled edit-guard now requires an **actual statement** (`statement_id IS NOT NULL`), not just `status='Reconciled'` — matches the docstring's intent. Banktivity-migrated rows carry the status with no statement (owner file: **27,977** orphan vs **1,337** statemented), so the guard fired a false "may unbalance the statement" confirm and **blocked opening a reconciled split** (Smile Current 2026-03-03). One-line `is_reconciled` fix; statemented rows still protected. Supersedes the §8 bulk-edit workaround for the edit-blocking symptom | **Accepted 2026-06-19**
| ADR-087 | Home dashboard budget card measures spend against envelope **available** (allocation + rollover), not bare `allocation` — it falsely flagged "over" while the Budget window said "under" for the same month; `cell.available` matches the ADR-058 envelope model. Amends ADR-075 | **Accepted 2026-06-20** |
| ADR-088 | **Income Over Time** report — income-side mirror of Spending Over Time (strict inflow: `kind='income'` AND `amount>0`); generalises `SpendingReportWindow` over a `_Direction` descriptor (`IncomeReportWindow` = one-attribute subclass), new `income_aggregates` + `IncomeOverTimeFilters` + migration 0028 (CHECK widen) + menu/catalog/dispatch. **Also amends ADR-064:** category filter on Income & Expense (`category_ids` clause, parent→descendant expansion) | **Accepted 2026-06-20** |
| ADR-089 | Reinvested dividends (DRIP) as income — reinvests become user-categorisable (amends ADR-086); Income Over Time gains an *Include reinvested dividends* toggle valuing them at quantity × price (`income_aggregates.include_reinvested`); new reinvests auto-tag on import via a self-seeding `reinvest_dividend_category_id`. Adds nothing to the cash totals (reinvests are zero-cash) | **Accepted 2026-06-20** |
| ADR-090 | Exclude investment portfolio-move trades (buy/sell/share-move/reinvest/split) from the cashflow reports (Spending, Payee, Category & Payee, Income & Expense, Sankey — plus the Home Recent-activity feed) via a shared `_portfolio_move_exclusion` keyed on txn id — a Buy is `amount<0` on Uncategorised(`expense`), so the strict-outflow rule had been counting £1.85M of buys as spending + "(No payee)" noise. Cash distributions + manual Cash stay | **Accepted 2026-06-20** | |
| ADR-093 | Bonds & options as first-class securities — the equity-only engine broke for the two classes the owner trades: **bonds** quote as **% of par** in par multiples (coupon/maturity/**accrued interest**) and **options** trade in **contracts** of a multiplier (×100). Fix: one per-security **`price_multiplier`** (stock 1; bond face/100; option contract_size) so the engine's `shares × price` is preserved by × multiplier at value sites; descriptive metadata rides on `security`. **Accrued interest** (`txn.accrued_interest`) is cash but subtracted out of basis (buy) + proceeds (sell). **Migration 0029** (instrument_type + 11 metadata cols + price_multiplier on `security`; accrued_interest on `txn`), all defaulted/nullable → an equity portfolio is byte-for-byte unchanged. `compute_holdings_view`/`compute_value_history`/`compute_returns` gain optional `multipliers` + accrued; the 5 callers pass `security_multipliers()`. Dialog gains an Instrument combo (Stock/Bond/Option) + per-class fields + accrued + multiplier-aware tri-field solver; Stock Record shows the metadata. Basic options only (long buy→sell/expire-worthless; no exercise/assignment). Reuses Buy/Sell. Verified headless + offscreen Qt | **Accepted 2026-06-21** |

| ADR-094 | Budget bills + stepped burn-down — a **bill is a budget line backed by a real `scheduled_txn`** (schedule owns date/cadence/amount/account). **Migration 0030** adds `budget_line.scheduled_txn_id` (NULL = ordinary; `ON DELETE SET NULL`). `compute_burndown` is now **bill-aware + stepped**: ideal = staircase (planned bills as steps at due days + linear discretionary remainder); projection = actual + **unpaid** bills (amount-matched, overdue→step at today) + discretionary run-rate on non-bill spend only → a **paid bill flattens**, weekly/twice-monthly bills step per occurrence. `burn_down_chart.py` rebuilt as a filled **staircase** (2b). Owner forks: setup pulls existing schedules in (adjustable); new bill from a line → full Schedule dialog; reverse schedule→budget **asks each time**; paid = **amount-match**. New pure `bill_occurrences_in_month`/`BillSchedule`/`BillOccurrence`; repo `add_bill_line_from_schedule`/`set_budget_line_schedule`/`list_bill_schedules_for_budget`/`list_schedules_not_in_budget`; UI: setup "Add bills from schedules…" picker, matrix-context "Make this a bill…", reverse ask-to-add. Verified headless + offscreen Qt | **Accepted 2026-06-21** |

| ADR-095 | Loan accounts & amortization — new **`loan` account family** (`loan_std`, liability) + 1:1 **`loan` terms table**. **Migration 0031** widens the `account.type` CHECK (ADR-032 recreate recipe; preserves `credit_limit`) + adds `loan`. Pure **`loan_calc.py`**: `compute_schedule`/`required_payment` (annuity, rounded **up**)/`monthly_rate` (monthly/annually/daily)/`split_payment`/neg-amort flag. Repo: `Loan` + CRUD, `create_loan_account` (opening balance = −current principal), `loan_current_balance`/`effective_payment`/`loan_schedule`, **`post_loan_payment`** (split-aware: whole = full transfer; split+interest-in-loan = transfer + interest txn in loan account; split+interest-from-payment = split on paying account w/ principal transfer line + interest line), `create_loan_paydown_goal` (reuses ADR-058 R4b). UI: `loan_dialog.py` (all fields, calc-or-typed payment, live preview, budget-goal opt-in); the **amortization schedule lives on the Account Summary** (family dispatch → `LoanScheduleWidget` in `loan_schedule_view.py`: summary + paintEvent balance chart + schedule table + Record payment/Edit; **no Top-Payees panel** — a loan has none), reached via double-click / Account ▸ Summary / context menu; register **New Loan…** opens it; Net Worth/Home `loan` family maps (family-driven inclusion). Deferred: what-if scenarios + recurring auto-post split. Verified headless (engine vs £200k/6%/30yr = £1,199.11×360; 3 payment modes; migration preserves data) + offscreen Qt | **Accepted 2026-06-21** |

_ADRs 096–118 aren't tabulated here — see the session narrative at the top of this file and the full index below. The most recent session (2026-06-28/29):_

| ADR-119 | **MRL-style chrome redesign** (delivered on a branch, now merged to master). New `ui/app_header.py` (flat `QToolButton` menus + utility cluster + person chip) **replaces the native `QMenuBar` + the ADR-116 toolbar**; new `ui/page_header.py` (title/subtitle); design-token **primary/ghost** button system; Home net-worth **hero card** + clickable card action links; airier sidebar nav + publisher footer. Follow-ups: editable profile name behind the avatar chip; use-after-free Home-card fix. **Report windows retrofitted** with the page header (2026-06-29). macOS loses its native top menu bar (Windows-first). | **Accepted 2026-06-28** |

| ADR-120 | **Net Worth: one big donut + Assets/Debts toggle** — replaces the cramped fixed-190px debts donut (its total elided to nothing) with one large donut + an Assets\|Debts segmented toggle (ADR-113 pill pattern); both sides' segments cached per refresh; legend shows the active side; **constant donut size** across the toggle (reserved legend height). View-layer only. | **Accepted 2026-06-29** |

| ADR-121 | **Historical net worth (net worth over time)** — a **Now \| Over time** toggle on the Net Worth screen + a **stacked-area + net-line** chart (asset families above zero, debts below). Computed-not-stored: new pure `net_worth_history.py` (`gather_net_worth_history`: per-account cash replay + `compute_value_history` holdings, FX-converted per date per ADR-055, bucketed by family) + paintEvent `net_worth_history_chart.py` + repo `earliest_posted_date()`. Property/vehicle valuation is **transaction-driven** (revaluation entries tracked over time). Verified: final sample reconciles to point-in-time net worth (£0.00 delta). | **Accepted 2026-06-29** |

| ADR-122 | **Windows installer + signing-ready pipeline** (completes ADR-104's Windows leg). New `packaging/installer.iss` (Inno Setup, **per-machine** Program Files, stable AppId, Start-menu + uninstaller); `build_windows.ps1` single-sources the version + signs the app exe & the installer when configured; CI builds the installer. User data stays in `%APPDATA%` so uninstall never touches `.mfl` files. | **Accepted 2026-06-29** |

| ADR-123 | **Distribution reversal → Microsoft Store-only for 1.0.1** (**supersedes ADR-078 K0**). Direct downloads were all fixed cost (annual code-signing cert + sales-tax accountant + a legal entity with merchant policies) for a small app; the Store is one variable **15%** cut + Merchant-of-Record (signs, tax, payments, refunds). One codebase / two channels: **1.0 = local Inno `.exe`** (early access, unsigned OK — **no cert bought**) / **1.0.1 = Store MSIX** (MS-signed, paid GA). Promo codes give the app free to early-access volunteers; Store auto-updates close ADR-078's WinSparkle gap. **Largely shelves ADR-079** (Store owns purchase + entitlement). Next round (own ADR): MSIX packaging + file-access validation. | **Accepted 2026-06-29** |
| ADR-125 | **macOS → Mac App Store (sandboxed), reversing ADR-123's macOS-out-of-scope** (implements ADR-078 K3's macOS leg). Owner: **MAS-only** on Mac (Windows stays MS Store), **first-run "choose a folder"** for the working `.mfl` (security-scoped bookmark), **paid up-front** (no IAP; ADR-079 key/trial dormant in store build). Spine = **App Sandbox**: audited conflicts — plain-path reopen → **security-scoped bookmarks** (new macOS-only PyObjC dep); Data Library `Library/` **beside file** → relocate to container (mirrors ADR-109 snapshots); `cloud.py` `brctl` subprocess → no-op under sandbox. Arc A–F: `sandbox.py` (`is_sandboxed()`+bookmark store) → bookmark file persistence → relocate Library → sandbox degradation → MAS packaging (entitlements + Apple Distribution signing + `.pkg`/Transporter) → store build flag. A–D + entitlements testable on Mac **without** the Apple account. **Increments A+B+C SHIPPED 2026-06-30** — A: `mfl_desktop/sandbox.py` + `test_sandbox` 10/10 + PyObjC dep pinned. B: bookmark persistence wired additively into `app_session`/`launch` (`begin_main_file_access` + `Resolution.location`); `test_launch_resolution` 11/11 + `test_app_session_sandbox` 6/6. C: Data Library relocated off beside-the-file to the app-data root (`app_session.library_root()`; `MFL Library` folder; legacy beside-file union); `test_data_library_current` 6/6. D: sandbox-aware degradation (`cloud` skips `brctl`, `launch` skips cwd dev-bridge when sandboxed); `test_cloud` 8/8, `test_launch_resolution` 12/12. E (scaffold): MAS packaging — `packaging/MyFinancialLife.entitlements` + env-gated `packaging/build_mas.sh` (inside-out sign + `productbuild` `.pkg` + `altool` upload); verified plutil/bash-n, needs owner's Mac+certs to run. F: store-build licensing dormancy — `version.is_store_build()` + `current_status()` short-circuit (owned, no trial/nag) + Enter-License/Buy hidden; `test_store_build` 6/6 + offscreen smoke. **Arc A–F COMPLETE + first-run folder-picker built** (`Resolution.first_run_default` + `__main__._prompt_first_run_location`). **2026-06-30 ad-hoc build finding: a live SQLite DB can't run on iCloud Drive under the sandbox** → first-run folder-picker RE-OPENED (it leads this owner into iCloud); live file must be local (container recommended). `build_mas.sh` gained `--adhoc` + an iCloud-xattr staging fix; `__main__` gained a container fallback (both uncommitted). Remaining = owner's file-location choice + "import existing file" flow + Apple account/certs to run `build_mas.sh`. | **Accepted 2026-06-30 (file-location sub-decision re-opened)** |
| ADR-126 | **Frozen-app TLS trust store — point OpenSSL at certifi's CA bundle** (owner-reported: every price update failed with `CERTIFICATE_VERIFY_FAILED … unable to get local issuer certificate (_ssl.c:1010)` in the packaged macOS `.app`). All HTTPS in the app is stdlib `urllib` on the **default** SSL context; inside a PyInstaller bundle OpenSSL's default CA paths point at the missing *build-machine* prefix and macOS OpenSSL never falls back to the Keychain → no CA to verify against (Windows loads the OS store, so it was unaffected). certifi's `cacert.pem` was already in the bundle but nothing pointed at it. Fix: new `mfl_desktop/net_certs.py` `ensure_ca_bundle()` sets **`SSL_CERT_FILE=certifi.where()`** once at the top of `main()`, before any network I/O — one chokepoint covering Tiingo prices, FX, and all bank feeds. Override-safe (never clobbers an existing value), idempotent, exception-safe, no-op in dev / on Windows / when certifi absent. certifi promoted to an explicit pinned desktop dep + `collect_data_files("certifi")`/`hiddenimports` in `mfl.spec` for a deterministic bundle. Reproduced the exact `_ssl.c:1010` error and verified the fix against `api.tiingo.com`; `test_net_certs.py` 5/5. | **Implemented 2026-07-01** |
| ADR-127 | **Lockable API-key field — read-only until "Change"** (owner-reported: the API key sits editable at the top of the Securities edit screen and looks too easy to change by accident). A stored provider key now opens **locked** — read-only + greyed via ADR-076 tokens (`surface_alt` fill / `muted_strong` text, so it reads as non-editable in light **and** dark) — with a small **Change** button beside it that unlocks it (restores the editable look, focuses + selects-all for a clean paste-over); a fresh/empty field opens **unlocked** so a first-time user pastes straight in with no extra click. Implemented once as shared `mfl_desktop/ui/secret_field.py` `LockableSecretField` (a `QWidget` wrapping a password `QLineEdit` + Change button, `autoDefault(False)` so it never steals Enter from Save/Refresh) and reused by **both** twin provider dialogs — Securities (Tiingo token) and Currencies (OpenExchangeRates app_id) — mirroring ADR-096's `transfer_chips` extraction. `.text()`/`.setText()` proxied, and each dialog keeps `self._key_edit = self._key_field.line_edit`, so the existing refresh/backfill/save handlers read the value **unchanged** (a locked field still submits the stored key). `setReadOnly(True)` chosen over `setDisabled` (which dims too hard + blocks select/copy). View/dialog layer only; no migration; **no change to key storage** (`setting.tiingo_api_key`/`oxr_api_key`). `tests/test_secret_field_smoke.py` 8/8 offscreen (seeded→locked, empty→unlocked, Change unlocks + preserves value, both dialogs lock a stored key while exposing it via `_key_edit`); screenshotted locked/unlocked in light + dark. | **Implemented 2026-07-01** |
| ADR-128 | **Consistent rounded bar corners across report charts** (owner-reported: on Spending Over Time some bars have a nice rounded top and others a dated square top, inconsistently from bar to bar). Root cause: each bar chart rounded its **topmost segment** as a rounded-top `QPainterPath` but only when it was tall enough (`spending`: `is_top and rect.height() > radius*1.4`; `income_expense`: same `< radius*1.4 → fillRect` fallback) — so a bar capped by a **thin** segment (a few pounds of Misc on a tall stack) fell through to a **square** `fillRect`, while a bar with a tall top segment rounded. The rounding was a property of the top segment's height, not the bar; a per-segment path can't round a cap thinner than the radius, so the guard was load-bearing. Separately `investment_income` never rounded (plain `fillRect`) and the three charts used three radius formulas (`min(6,w/4)` / `min(5,w/3)` / none). **Fix:** round the **whole bar**, not a segment, with one shared radius. New `mfl_desktop/ui/chart_helpers.py` `BAR_CORNER_RADIUS=6.0` + `bar_corner_radius(bar_w)=min(6,bar_w/3)` + `round_bar_corners(painter, rect, radius, bg, *, top, bottom)` — which rounds a just-drawn bar by **carving its corner wedges to the plot-background colour** (`chart_surface`). Carving (not clipping, not a per-segment rounded fill) is what makes it uniform: composes over an already-painted stack without needing each segment's colour, works at **any height** so a thin cap rounds like a tall bar (applied to the full bar rect), and stays crisp under antialiasing (a filled `quadTo` arc — `setClipPath` would give aliased corners). Wired: **spending** (stacked) draws each segment as a plain rect + the existing 1px separators, then carves the full bar's top once; **income_expense** fills each bar then carves — `top` for the up-growing income bar, `bottom` for the down-growing expense bar; **investment_income** fills then carves the top (was square). Removed the two ad-hoc rounding blocks, the `_BAR_RADIUS_MAX` constant, and now-dead `QPainterPath` imports. The horizontal **payee** chart already rounds uniformly via `drawRoundedRect` and is left as-is (different orientation). Accepted minor: the carve paints bg over a ≤6px corner at each bar's apex — bars sit below the top gridline so it never lands on one, and the average/zero lines are drawn after bars. View layer only; no migration. `tests/test_bar_corners.py` 7/7 **pixel-level** (corner pixel → background while top-edge centre stays bar-coloured, incl. a **height-4 cap**, + radius clamping); screenshotted all three reports in light + dark. | **Implemented 2026-07-01** |
| ADR-129 | **Net-expense definition — reimbursements/refunds reduce spend across the flow reports** (owner-reported: a work-expense "Reimbursed" category showed **£1,730.79** on the Spending Over Time chart, but drilling in the net was only **−£45.04** because reimbursement credits offset the expenses). Cause: five report aggregates defined expense as **strict outflow** — `c.kind='expense' AND t.amount < 0`, `SUM(-t.amount)` — so **positive** amounts on an expense category (refunds/reimbursements) were silently dropped: `spending_aggregates`, `payee_spending_aggregates`, `category_payee_matrix`, `sankey_category_totals`, `income_expense_series`. For the owner's category the only stored-negative line was the £1,730.79 card payment; the credits were positive and excluded → the bar showed **gross outflow** and couldn't reconcile with its own drill-down (all signed txns → net −£45.04). The docstrings had flagged strict-outflow as a deliberate v1 simplification ("refund handling deferred to a future Cash Flow report"), but it produced a misleading number for reimbursement-style categories and made the report families disagree with each other + with drill-downs. **Owner chose net (all flow reports).** Fix: **net-expense** everywhere — every `kind='expense'` line contributes its **signed** amount (`SUM(-amount)` over *all* the category's lines), so a refund reduces its category; a category/payee/(category×payee) cell that nets **≤£0** is **clamped to £0** (dropped) since a stacked bar can't draw a negative segment. Same **per-category floor** in all five so they agree + reconcile with a category drill-down; `income_expense_series` was **regrouped to include `category_id`** so its expense side floors per-category (not per-bucket) and the I&E expense bar equals the Spending total. **Verified reconciling to the penny on the demo:** Spending == Income&Expense == Sankey expense = £30,878.94. **Income side left strict-inflow** (`kind='income' AND amount>0`) — reimbursements are an expense-side workflow, income clawbacks rare; the asymmetry (expense nets, income doesn't) is documented on both methods. Rejected: keep strict + only fix the drill (hides reimbursements); net only Spending+Payee (other three keep £1,730.79); bucket-level net in I&E (per-category-floor gap vs Spending). SQL-only; **no schema, no migration, no data change**. Portfolio-move exclusion (ADR-090) still drops buys, now more important. `tests/test_net_expense_refunds.py` 5/5 (Qt-free): net-positive keeps £70, net-negative dropped, payee/cell clamp, income ignores a −£10 correction, I&E expense == spending total. Accepted lossiness: a net-credit category reads £0 in the stacked charts (true signed net still on the drill-down/register). | **Implemented 2026-07-01** |
| ADR-130 | **Transaction status lifecycle (confidence ladder), status-driven reconciliation, precise OFX matching** — *Proposed* (design + phased plan). Prompted by the June reconcile audit: a £16.17 (then £13.39) variance that was entirely **status/selection**, not arithmetic — `pending` 30-June card purchases and duplicate entries (a day-trip entered on the 19th and 22nd; TV-licence and a Pret entered twice) were reconcilable and got ticked, and the reconcile screen (which pre-ticks `Cleared` via `list_cleared_unreconciled_txns`) trusted status+ticks with nothing flagging "not on the bank". Today's statuses `Pending/Uncleared/Cleared/Reconciled` are **copy-pasted in ~8 UI files** + a `CHECK` in migration 0001; import already has FITID→`import_hash` dedup + a ±2-day manual match ("acceptable but not precise"). Owner workflow: enter at spend (Pending) → mark when the bank-app says posted (today Uncleared, counter-intuitive) → periodically OFX-import so entries **match** + missed ones add → reconcile+lock. **Decision:** four-state lowercase **confidence ladder** — **pending** (spent, not posted) → **cleared** (you saw it post) → **matched** (an OFX download matched/added it) → **reconciled** (statement-locked). Mostly a rename (`Uncleared→cleared`, `Cleared→matched`, case-lowered to match `statement.status`). State machine: pending→cleared (manual); pending/cleared→matched (OFX match); new-from-bank inserted as matched; matched→reconciled (locked, reopen to edit); an unconfirmed `cleared` **stays cleared + is flagged** (the anomaly signal that catches the duplicates/phantoms). **Reconcile eligibility = `matched`, with an optional "include cleared" toggle** (owner's explicit call — some institutions offer no OFX/download, so a user may legitimately reconcile off eyeballed `cleared`; ADR-050 first-class-everywhere); `cleared`-in-range otherwise shown as a warning; `pending` never eligible — this alone makes both June bugs impossible. **Keep the spend date, add `bank_posted_date`** (set from OFX on match, used for reconcile date-ranging, fallback `posted_date`) — fixes the boundary/scramble. **Auto-match** = exact amount + canonical payee + ≤2 days; looser → needs-review (amount-differs offers **adopt-bank-amount**, which would've caught the £13.39). **Centralise** the enum into new `mfl_desktop/txn_status.py` (label/colour/icon/order), replacing the 8 duplicated tuples; register shows pending=hollow/cleared=amber/matched=blue✓/reconciled=green🔒 + legend/tooltips (not standard-accounting vocab). **Phased, each shippable+testable:** (1) status foundation — `txn_status.py` + migration **0033** (rename + `CHECK` rebuild via the ADR-032 table-rebuild recipe) + repository status-validation sites + `csv_parser` map + register colours; (2) reconcile-by-confidence — candidate=`matched`, include-cleared toggle, cleared-in-range warning (no schema change); (3) precise import — migration **0034** `bank_posted_date` + scored matcher (amount+date-window±5+payee-fuzzy) + import review screen (auto / needs-review / new-from-bank / unmatched-entries) + status transitions. Ship 1→2→3; phases 1–2 deliver the safety win without the import work. Semantic swap (Cleared now = matched) mitigated by legend/tooltips; old locked-`reconciled` statements unaffected. | **Proposed 2026-07-02** (Phases 1–3 core shipped 2026-07-02/03 — see header lead) |
| ADR-131 | **Sidebar balance: Today vs Projected toggle.** Owner-reported: the left-panel account balances sum the whole ledger, so future-dated ("forwarded") transactions inflate the figure — they wanted today's actual balance vs projected. A segmented **Today | Projected** pill on the ACCOUNTS header row (column 1) switches every balance at once (owner's pick over show-both-figures); **default Today** (`posted_date <= today`, excludes future-dated), remembered app-level in QSettings (`sidebar/balance_mode`). `compute_account_balances`/`compute_account_values` gained an `as_of_date` — cash sum filters `posted_date <= as_of` and the investment holdings set is likewise date-filtered (future trades don't count); prices always latest, only the txn set is dated. Sidebar owns the mode (`saved_balance_mode()` static so the register computes the initial balances before the widget exists), renders the pill, persists on change, emits `balance_mode_changed`; register recomputes (`today`→`date.today()`, `projected`→None) + reloads via a `_sidebar_balances()` helper threaded through both reload paths. A `minimumSectionSize` bump keeps the pill un-clipped when balances are short (ResizeToContents ignores cell widgets). No schema, no migration — a date filter + a toggle. **Net Worth + the register running-balance intentionally unchanged.** `tests/test_sidebar_balance_mode.py` 3/3 (QSettings-hermetic) + suite 27/27; screenshotted light/dark. | **Implemented 2026-07-03** |
| ADR-138 | **Budget funding model (balances vs income) + drop credit-limit-as-funds.** Owner feedback on budget setup. (1) New `budget.funding_mode` — **`balances`** (default: pool = perimeter accounts' balances) or **`income`** (pool = only `kind='income'` into the non-excluded perimeter accounts over the whole budget period, incl. future-dated; ignores starting balances), chosen via a radio pair on the setup dialog's Accounts tab + persisted with `set_budget_funding_mode`. (2) Dropped `budget_account.contribution='available_credit'` (= `credit_limit + balance` — counting the limit as funds, "not good practice"): a credit card now contributes its **signed balance** so its debt *reduces* the pool (owner's pick over excluding the card). Migration **0035** adds `funding_mode` (CHECK) + rebuilds `budget_account` to migrate `available_credit`→`balance` and tighten the CHECK to `('balance','excluded')` (ADR-032 recipe). `compute_perimeter_pool` branches on the mode; signature unchanged so the budget window + home card (ADR-136) are unaffected. Excess-balance sweep deferred to a later ADR. `tests/test_budget_funding_mode.py` 6/6 + suite 28/28; screenshotted. | **Implemented 2026-07-05** |
| ADR-132 | **Launch-time dialogs must escape the always-on-top splash.** Owner-reported (Windows): when the data file can't be opened at launch, the error is invisible — hidden *behind* the splash — so the app looks frozen. Root cause: the ADR-103 splash uses `Qt.WindowStaysOnTopHint`, which on Windows puts it in the OS **topmost z-order band** above every **no-parent modal dialog** — and all launch dialogs are parentless (no main window yet): the ADR-109 file-recovery prompt, the ADR-125 first-run folder picker + container-fallback warning, and the ADR-099 crash box. Two of those sites were added *after* the splash, so per-site fixes kept rotting. **Fix once, centrally:** `ui/splash.py` tracks the splash it creates (`_active_splash`) and exposes idempotent **`dismiss_active_splash()`** (fully `close()`s + clears the ref, safe with no splash); every launch-dialog site calls it first — recovery via the injected `dialog_factory` wrapper in `__main__` (so `launch.resolve_database` stays window-free/unit-testable, ADR-109), both ADR-125 sites (`_prompt_first_run_location`, `_open_repository_with_fallback`), and `diagnostics._show_crash_dialog` (which also forces the box topmost, belt-and-suspenders). Once any dialog fires we're in an interactive recovery flow, so retiring the branded splash for the rest of that launch is the right trade. **Happy path unchanged** — no dialog fires, nothing calls the helper, splash stays up through open + window build until `splash.finish(win)`. Rejected: dropping the stay-on-top hint (regresses ADR-103; two-topmost ordering still unreliable), per-dialog topmost+raise (topmost-vs-topmost race), inline `splash.hide()` per site (the pattern that already rotted). Verified headless (`QT_QPA_PLATFORM=offscreen`): splash tracked on create, `dismiss_active_splash()` closes it + idempotent, recovery/crash paths leave no visible splash; launch-resolution tests unaffected (they inject their own `dialog_factory`). | **Implemented 2026-07-03** |
| ADR-124 | **Budget annual matrix — drop the inline rollover annotation** (owner: the carried-over amount was cut off). A Budget cell with a rollover rendered `1,100.00  (+205.00)`, overflowing the fixed **86px** month column → elided. Fix: matrix `DisplayRole` shows just the figure; the carry is signalled by the existing **yellow `rollover_bg` background + reconciliation tooltip + ↻ label glyph**. View-layer only, no column-width change, no migration. **Scope = annual matrix only** (the monthly view's flexible-width label was never truncated, unchanged). | **Accepted 2026-06-30** |

Full index and summaries: [`docs/adr/README.md`](docs/adr/README.md).

---

## Known pitfalls

Most legacy-specific pitfalls only matter while maintaining the v0.1 web app; the cross-cutting ones carry into the rewrite.

**Carry forward:**
1. **Windows date formatting** — `%-d` doesn't work. Use `f"{d.day} {d.strftime('%b %Y')}"`.
2. **Full file replacements lose manually-added code** — verify imports survive after any full Write. The `import hashlib` / `compute_hash` pair was lost more than once in v0.1; don't repeat the mistake on the rewrite.
3. **IRI namespace discipline** — Transactions are MFL namespace, accounts/person are MRL. Carrying this into SQLite as stored strings means the mistake travels silently if you generate the wrong prefix on insert.
4. **CSV import hashes can collide within one batch.** The composite `date|amount|payee_raw` hash isn't unique across a single file — two coffees on the same day at the same price, or any rows with an unmapped payee column, will collide. `_classify_and_stage` resolves this with a deterministic `:N` suffix; any future rewrite of the staging path must preserve this or the `UNIQUE(account_id, import_hash)` constraint will fire at commit.
5. **Transfer parent row must always be inserted alongside both half-rows.** Per ADR-035, every transfer pair has three writes — two `txn` rows + one `transfer` row — and they must travel together in one SQL transaction. `_insert_transfer_parent` is the single entry point; `create_transfer`, `_convert_to_transfer_unbatched`, `_link_transfer_unbatched`, and the transfer branch of `post_scheduled_txn` all call it. Any future code path that writes a transfer (e.g. a CSV import that detects pairs at staging time, or a split-transaction feature that creates a transfer leg) must call `_insert_transfer_parent` too, or reports that read `transfer.rate` will silently miss the rate-of-record. The existing tables have no FK from `txn.transfer_id` to `transfer.iri` — by design, matching ADR-020's "id space is conceptual" — so SQLite won't catch the omission.
6. **FX rate lookup direction matters.** `Repository.get_fx_rate_nearest(date, base, quote)` walks six steps including inverse and USD-pivot fallbacks because openexchangerates' free tier stores everything `USD → X`. Asking for `X → USD` without the inverse fallback returned no rate before this was fixed — the symptom was a fully cross-currency matcher silently returning zero candidates. Any future provider integration that bypasses `get_fx_rate_nearest` (e.g. a direct ECB feed that stores `EUR → X`) needs to either populate both directions or extend the lookup chain.
7. **`_UNSET` sentinel must be referenced via `self._UNSET` inside Repository method bodies.** The `Repository` class has a class-level `_UNSET = object()` (used by `bulk_update_transactions`, `update_report`, future `update_*` methods). When you write `def update_x(self, *, name=_UNSET, ...)`, the *default value* is evaluated at class definition time using class-body scope → resolves to the class attribute. But inside the method body, bare `_UNSET` resolves to *module* scope (Python class scope isn't visible to nested function bodies). If the module also has a `_UNSET` defined, you get two different `object()` instances and `name is not _UNSET` is **always True** — silently treating every defaulted parameter as "set to None" and binding mystery `object` instances to SQL. Always use `self._UNSET` inside the body. Hit during ADR-039 implementation (`update_report`) — symptom was `sqlite3.ProgrammingError: type 'object' is not supported`.
8. **Reconciliation hides already-`Reconciled` transactions, and bulk edit only touches the *visible* register window — these compound into a "reconcile won't show old transactions" false alarm.** `list_reconcilable_txns` deliberately excludes rows whose `status = 'Reconciled'` (unless they belong to the statement being viewed — `WHERE status != 'Reconciled' OR statement_id = sid`), so the reconcile check-off lists only *unreconciled* rows (of any date — it does NOT apply the register's date window). Separately, **Banktivity CSV import preserves the per-row status** (`_BANKTIVITY_STATUS_MAP` → `effective_status = tx.status_override or import_status`), so migrated data can arrive already `'Reconciled'` and is then correctly invisible to the reconcile wizard. The trap: to make such rows reconcilable you change their status via **bulk edit** — but `_on_bulk_edit` operates on `_selected_txn_ids()` (the rows selected in the register), and the register defaults to a **90-day "Rolling quarter" window**, so `Cmd+A` + bulk-edit only un-reconciles the last ~3 months; older rows stay `Reconciled` and keep not appearing. This *looks* like a 3-month limit in the reconcile screen (and looks register-window-related) but is none of those — it's working as designed. Fix is operational: set the register window to **All** *before* selecting all, then bulk-edit status. (Investigated 2026-06-13; confirmed not a bug. If it ever needs softening: a bulk-edit "editing N visible rows, older ones excluded" notice, or letting reconciled-but-unattached rows into the reconcile list.) **Update 2026-06-19 (ADR-040 amend):** the *edit-blocking* half of this is now code-fixed — `Repository.is_reconciled` requires a non-null `statement_id`, so these orphan-reconciled rows (status-only, no statement; 27,977 of them in the live file) are freely editable and their splits open on double-click without the false "may unbalance the statement" confirm. The status string can stay `Reconciled`; only the statement linkage governs the edit guard now.
9. **Cross-platform rule set (macOS + Windows are equal targets — ADR-050).** The app must stay byte-for-byte portable; the codebase is clean today and the job is keeping it that way. Binding rules for all `mfl_desktop/` code: **(a)** paths are always `pathlib.Path` — never string-concat, `/`-or-`\`-literals, drive letters, `~`, or `%APPDATA%`. **(b)** User data resolves via `QStandardPaths.writableLocation(QStandardPaths.AppDataLocation)` — never the cwd (this is the one current drift: `__main__.py:85` still defaults to `mfl_dev.db` in cwd; `--db` override stays for dev). **(c)** Keyboard shortcuts use `QKeySequence.StandardKey` where one exists; otherwise a single per-OS modifier helper that emits ⌘ on macOS / Ctrl elsewhere — **a literal `"Ctrl+N"` string is NOT auto-mapped to ⌘ by Qt; only `StandardKey` is.** **(d)** About/Preferences/Quit `QAction`s set `MenuRole` so Qt relocates them into the macOS app menu. **(e)** Font stacks lead with `-apple-system`/`system-ui`, never a single-OS family like `"Segoe UI"` first; never a bare `QFont("Segoe UI")` without fallback. **(f)** Keep the **Fusion** style + QPalette (ADR-026) — no native platform style. **(g)** No OS-shell coupling: no `os.startfile` (Windows-only), no `subprocess`/`QProcess` to shell commands, no tray icons — use `QDesktopServices.openUrl` if "open this file/folder" is ever needed. **(h)** No `%-d`/`%#d`/`%-m` strftime directives — keep `f"{d.day} {d.strftime('%b %Y')}"` (SQLite `strftime` is fine). **(i)** No `sys.platform`/`os.name` branches in feature/UI/Repository code — OS checks live only inside small isolated shim helpers (the shortcut-modifier helper, the data-dir resolver, the cli UTF-8 console shim). **(j)** Repo stays LF (`.gitattributes`). **Build:** PyInstaller can't cross-compile — `.app`/`.dmg` builds on macOS, `.exe` on Windows (ideally a GitHub Actions `macos-latest`+`windows-latest` matrix); macOS needs **code-signing + notarization** (Apple Developer Program, $99/yr) or non-technical users hit the Gatekeeper wall.

**Legacy-only:**
4. **pyoxigraph ASK returns bool** — `bool(store.query("ASK {...}"))`.
5. **`@app.on_event` deprecated in FastAPI** — use lifespan context manager.
6. **Starlette 1.0 TemplateResponse** — `request` is first positional arg, not in context dict.
7. **mfl-ontology.ttl must not be empty** — was created as 0-byte placeholder originally.

---

## Development workflow

### Run the desktop app (the live target)
```powershell
# from the project root, with an activated venv
pip install -r mfl_desktop\requirements.txt

# one-time: create the database and seed a first account
python -m mfl_desktop.cli init

# optional: add more accounts
python -m mfl_desktop.cli add-account "Joint Savings" --type savings
python -m mfl_desktop.cli add-account "Amex Gold"     --type credit

# launch the GUI
python -m mfl_desktop
```

### CLI smoke tests
```powershell
python -m mfl_desktop.cli import path\to\statement.qfx
python -m mfl_desktop.cli list --limit 30
python -m mfl_desktop.cli categories
```

### Run the v0.1 web app (legacy, still works on Oxigraph)
```powershell
python main.py
# open http://127.0.0.1:8000
```

### Run the original PySide6 prototype (reference only)
```powershell
cd prototype_register
.\.venv\Scripts\Activate.ps1
python register_proto.py
```

### Starting a new session
1. Read this file.
2. Skim [`docs/adr/README.md`](docs/adr/README.md) for decision context.
3. Check memory under `~/.claude/projects/.../memory/` for owner preferences and project state.
4. Confirm where work is in flight before editing.
5. Deliver complete files; verify imports survive.
6. New architectural decisions need a new ADR (even if owner says "your call").
